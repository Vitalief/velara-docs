---
scope: LLM cost tracking re-architecture: LangSmith as pricing source of truth, persisted in Postgres
purpose: Build substrate for replacing app/core/pricing.py + hand-rolled Anthropic tracing with pure langsmith.wrappers, while keeping invocation_results.cost_usd as the queryable billing/analytics system-of-record
altitude: epic (Epic 17 observability / cost-tracking re-architecture)
updated: 2026-07-29T14:25
---

- (version) langsmith==0.10.10 (already a dep). Run schema exposes total_cost/prompt_cost/completion_cost + prompt/completion/total_tokens; Client.read_run/list_runs fetch them. wrap_anthropic available in langsmith.wrappers (handles streaming + per-model native cost).
- (constraint) [VERIFIED] LangSmith computes run cost ASYNCHRONOUSLY after ingest — total_cost is NOT available synchronously when the worker finishes and writes cost_usd. Any read-back is eventually-consistent, not immediate.
- (direction) User directive: LangSmith is the SINGLE SOURCE OF TRUTH for LLM pricing (delete pricing.py rate table + compute_cost_usd). Replace all hand-rolled Anthropic tracing (velara_trace custom wrappers, tracing.py _emit_span) with langsmith.wrappers.wrap_anthropic — no home-grown wrapping. STILL persist cost_usd in Postgres (invocation_results) so analytics/billing/offline reads are unchanged.
- (constraint) [VERIFIED brownfield] cost_usd is a first-class column in invocation_results (migration 0024, Numeric(12,6)). analytics_service.py does SUM(cost_usd) SQL aggregates for client-value/per-skill/per-user dashboards. jobs/client APIs read it. This DB column stays — only its SOURCE changes from pricing.py to LangSmith.
- (constraint) [VERIFIED brownfield] pricing.py imported by 5 modules: core/tracing.py, api/v1/skills.py, workers/execution_tasks.py, services/code_driven_executor.py (+ pricing.py self). Test env runs LANGSMITH_TRACING=false — cost currently works with tracing OFF; that path must not silently break.
- (decision) PARADIGM: deferred cost reconciliation. Worker writes tokens+model+langsmith_run_id at job completion with cost_usd=NULL (pending); an async reconciler reads LangSmith total_cost and backfills cost_usd. Binds: every LLM-using invocation. Prevents: blocking job completion on async external cost, and fabricating a local price.
- (decision) TRACING: all Anthropic access goes through langsmith.wrappers.wrap_anthropic — one platform helper returns a wrapped client; NO hand-rolled RunTree/_emit_span/_TracedClient/_TracedStream. Binds: platform LLM calls AND sandbox skill calls (via monkeypatch of anthropic.Anthropic -> wrap_anthropic(real())). Prevents: divergent tracing impls + the stream/multi-model capture bugs.
- (decision) COST OWNERSHIP: invocation_results.cost_usd stays the billing/analytics system-of-record (SQL-queryable, works offline). Its VALUE is sourced from LangSmith (not pricing.py). pricing.py rate table + compute_cost_usd DELETED. cost_usd states: NULL=pending-or-unpriceable, populated=reconciled-from-langsmith. Binds: analytics_service, jobs/client APIs. Prevents: two pricing sources diverging.
- (decision) RUN LINKAGE: invocation_results gains a langsmith_run_id (the trace/run id the reconciler queries). One invocation may make N LLM calls (locator+extraction) => N LangSmith runs under one trace; reconciler sums total_cost across the trace's llm runs. Binds: sandbox multi-call skills. Prevents: capturing only one call's cost (the exact 32013/110 bug).
- (decision) TRACING-OFF FALLBACK: when LANGSMITH_TRACING is off/unconfigured (test env, air-gapped), NO run is created => no reconcilable cost => cost_usd stays NULL (honest 'unknown'), tokens still recorded from the wrapped client's local usage. Binds: test/offline envs. Prevents: silent breakage of the tracing-off path + fabricated $0.
- (question) [ASSUMPTION] Reconciler = a Celery task self-scheduled with countdown ~60s after job completion (bounded retries if LangSmith cost not yet computed), NOT a global periodic sweep. Confirm vs a periodic sweep of NULL-cost rows.
- (question) [ASSUMPTION] token counts (input/output) still stored per-invocation from the wrapped client's aggregate usage; but with multi-model runs the single input_tokens/output_tokens/model columns are lossy — LangSmith holds the per-model truth. DB tokens become an approximate display; authoritative cost is the summed LangSmith total_cost. Confirm this is acceptable.
- (constraint) [VERIFIED] LangSmith does NOT expose its price map via SDK (no list_models/get_model_price_map/cost method on Client). An instant local cost cannot use 'LangSmith rates' — a local estimate necessarily uses our own table.
- (direction) User decision: estimate-then-reconcile. Instant local cost_usd estimate at completion (shown immediately, flagged estimated) from a SLIMMED pricing.py; async reconciler OVERWRITES with LangSmith total_cost when ready. pricing.py is DEMOTED to a throwaway estimate, not deleted. LangSmith remains the authoritative final value.
- (decision) SUPERSEDES AD-3/AD-4 pending model: AD-3 now writes an ESTIMATED cost_usd instantly (cost_is_estimated=true) from the local rate table; job never blocks. AD-4 reconciler reads LangSmith total_cost and OVERWRITES cost_usd, sets cost_is_estimated=false. Binds: InvocationResult (+cost_is_estimated column). Prevents: the 'pending/blank cost' UX the user rejected, while keeping LangSmith authoritative.
- (decision) AD-1 REVISED: LangSmith is the authoritative/FINAL pricing source, but a slimmed local rate table (pricing.py, retained) provides the interim ESTIMATE. Not two competing sources — a strict precedence: estimate is provisional, LangSmith overwrites. compute_cost_usd stays for the estimate only; its output is always subject to reconciliation. Prevents: a permanent local price that never gets corrected.
- (decision) AD-5 REVISED: cost states are now (estimated) provisional-nonNULL, (reconciled) authoritative-nonNULL, (NULL) genuinely unpriceable/untraced. cost_is_estimated boolean distinguishes provisional from final. Analytics MAY sum estimated+reconciled (both are real dollar figures); a dashboard may optionally show 'includes N estimated'. Prevents: treating an estimate as final or as $0.
- (event) spine revised to estimate-then-reconcile per user decision; lint clean
- (constraint) [VERIFIED live SDK] (1) wrap_anthropic patches create/stream/beta.create, run_type=llm, usage_metadata per call. (2) run id/trace_id readable via get_current_run_tree() INSIDE traced ctx; worker must open its own trace() to capture rt.id; None when tracing off. (3) list_runs(trace_id=,run_type=llm,select=[total_cost]) sums across a trace. (4) total_cost async, None until computed, NO ready signal. (5) trace context is a ContextVar — does NOT cross into sandbox subprocess automatically; bridge = parent.to_headers() -> from_headers()+tracing_context() (verified shares trace_id).
- (decision) AD-7 (closes Holes 1,2): INVOCATION-SCOPED TRACE. Worker opens ONE langsmith trace() per invocation before the first LLM call; every span (all platform turns + sandbox calls) nests under it. The sandbox inherits it via to_headers()->injected_env->from_headers()+tracing_context() (verified cross-process bridge). Persist the TRACE_ID (not a per-span run id). Reconciler queries list_runs(trace_id, run_type=llm) and sums total_cost. Binds: tracing.py emitter (stop minting root runs), velara_trace shim, execution_tasks. Prevents: single-call undercount + unlinked sandbox trace.
- (decision) AD-8 (closes Holes 3,4,6): SYMMETRIC never-regress + reconcile candidate predicate. The estimate WRITE is a guarded upsert: set is_estimated=true ONLY when the row is absent/already-estimated — never clobber a reconciled(false) row. The reconciler is a candidate-gated compare-and-set: touches ONLY rows where is_estimated=true AND runtime made a platform LLM call AND a trace exists; UPDATE ... WHERE is_estimated=true (CAS, so redelivery no-ops); reconciles ONLY when LangSmith cost is finalized (all child runs present), never on first non-null read. Binds: both writers. Prevents: reconciled->estimate regression, self-race double-write, undercount on partial cost.
- (decision) AD-9 (closes Hole 5): FAN-OUT & CODE-RUN EXEMPTION. Reconciliation operates on LEAF LLM-runtime invocation rows only. fan_out=true parent rows stay cost_usd=0-terminal (never assigned a trace, never reconciled) — preserves the existing analytics leaf-sum-with-parent=0 invariant. Genuine code runs stay 0-terminal. is_estimated=false for both. Binds: reconciler candidate predicate, analytics_service leaf sum. Prevents: parent double-count.
- (decision) AD-2 REVISED (Hole: contradicts HEAD): 'one wrap seam' means one WRAP MECHANISM (wrap_anthropic), NOT one provider factory. The shipped get_llm_provider + get_adapter_llm_provider (17.2, different MODELS) both stand and both return wrap_anthropic-wrapped clients. The invariant is: no client bypasses wrap_anthropic; NOT a single factory. Prevents: a dev ripping out the adapter-model factory to satisfy a literal 'one factory' reading.
- (constraint) [VERIFIED brownfield] fan-out parent row is written cost_usd=Decimal('0') by design (no runtime key -> explicit-$0 branch) with in-code comment 'avoiding double-counting children's already-priced cost'. analytics_service _token_cost sums LEAF cost_usd and depends on parent=0. The reconciler MUST NOT break this.
- (event) reviewer gate complete: 6 holes found, closed with AD-7/8/9 + AD-2 revision; SDK claims verified live; spine finalized
- (decision) STORY BREAKDOWN (approved, creation deferred): 17-4 DB migration (langsmith_run_id, cost_is_estimated) [AD-5]; 17-5 wrap_anthropic seam, delete hand-rolled tracing [AD-2] (dependency-free, fixes current wrong-cost bug); 17-6 invocation-scoped trace + sandbox header propagation [AD-7] dep 17-5; 17-7 estimate write + slim pricing.py [AD-1,3,6] dep 17-4; 17-8 reconciler task [AD-4,8,9] dep 17-4,6,7; 17-9 consumer updates [AD-5,9] dep 17-7,8.
- (event) architecture run closed; spine final; story breakdown approved, creation deferred to a future create-story session per user
