# generated: 2026-06-02
# last_updated: 2026-06-30  # Epic 8 kicked off: create-story 8-1 (hierarchy-scoped RBAC enforcement) → ready-for-dev; epic-8 backlog→in-progress. Epic 7 complete (all 4 stories done). Prior: 2026-06-27 Epic 5.5 retrospective → done.
# project: Velara
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts/stories
#
# 2026-06-05: RESEQUENCED via correct-course (see planning-artifacts/sprint-change-proposal-2026-06-05.md)
#   - Epics renumbered to feature-first order. Cloud provisioning relocated to Epic 7.
#   - Mapping: old E3→E2 (Registry), old E4→E3 (Execution), old E2→E4 (Hierarchy),
#     new E7 (Infra/Deploy/Cloud Auth = old 1-3..1-6), old E7→E8 (Access Control), old E8→E9 (Audit).
#   - Story 7-1 (was 1-3 AWS Infrastructure, was ready-for-dev) reverted to backlog (deferred).
#   - Two new Epic 1 stories added: 1-3 (Local Dev Env & Provider Abstractions), 1-4 (Dev Auth Shim).
#
# 2026-06-08: CLIENT DESIGN CHANGES + COMPLIANCE via correct-course (see planning-artifacts/sprint-change-proposal-2026-06-08.md)
#   - Epic 9 expanded to "Audit Log, Usage & Analytics": + 9-4 (Usage & Value Analytics API), 9-5 (Analytics UI).
#   - Epic 1: + 1-5 (Per-Route Browser Tab Title, app shell).
#   - ACs added (no new stories): Epic 4 (search/filter mock, Project breadcrumb), Epic 6 (e-signature manifestation),
#     Epic 7 Story 7-4 (HIPAA + 21 CFR Part 11 compliance mapping + validation plan), Epic 9 (audit date-range, Part 11 trail).
#   - HIPAA + 21 CFR Part 11 named as compliance frameworks; formal Part 11 validation (IQ/OQ/PQ) deferred.
#
# 2026-06-09: V3 BRAND RE-THEME via correct-course (see planning-artifacts/sprint-change-proposal-2026-06-09.md)
#   - Client brand guidelines (design/uploads/Brand Colors.png) → V3 design tokens (styles_v3.css).
#   - Epic 1: + 1-6 (Apply Vitalief V3 Brand Theme — teal/navy palette, Poppins/Open Sans; re-themes the done 1-2 scaffold).
#   - Reference updates: architecture design-system, UX-DR-02/03/04, OUT-02, Epic 3 branded output, PRD §13 (v2→v3).

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-06-02
last_updated: 2026-06-30  # create-story 8-2 (IP Protection — Client API Surface Enforcement) → ready-for-dev (backlog→ready-for-dev; story FILE now written, prior chain-entry was planning-only). BACKEND-only, NO migration. 3 source-verified codebase/arch audits (invocation/job/skill schemas+routes; router/deps/role-guard surface; arch IP-protection decisions). REFRAME confirmed in source: skill internals are S3-ONLY (SkillVersion.artifact_key, no DB content) → 8.2 = router-omission + schema-omission (structural), not permission filtering. 3 decisions LOCKED w/ PM: D1 ClientJobRead = minimal+safe-timestamps, an INDEPENDENT BaseModel (NOT a JobRead subclass) exposing only job_id/status/output_files[presigned url]/error.message/created_at/completed_at — excludes skill_id/skill_version/org_id/hierarchy_path/created_by_user_id/result_metadata/parent_job_id/location_id; D2 reject_client guard → 404 NOT_FOUND (not 403) on ALL 8 internal routers (skills/certifications/hierarchy/ingest/invocations/invoke/jobs/access-grants via single dependencies=[RejectClient] kwarg on each APIRouter decl — verified all are single-declaration routers; auth+health UNGUARDED); D3 client error surface = message only (user-safe error_message_for(code), never error_code). Build NEW app/api/v1/client.py: POST /api/v1/client/invocations/{skill_id} REUSING queue_invocation (log_prefix="client_invocation") + GET /api/v1/client/jobs/{job_id} reusing get_job + the presign_download loop. Reuse InvocationRequest/InvocationAccepted as-is (already IP-safe). FULL client invoke+job-poll built NOW (per PM). #1 RISK: 404 guard must not break internal-token 200s or no-token 401s (guard depends on CurrentUser → 401 first; client→404; internal→pass) — explicit tests assert all three + 404-not-403. TEST NUANCE: client.user=org_client_001 but seed skills=org_vitalief → success-path client invocation test must seed skill + node + access grant in org_client_001. Extend test_invoke.py:184-233 body-search lock + test_openapi.py ClientJobRead schema lock. Out: skill discovery/visibility filter=8.4, portal FE=8.3/8.4, migration. Memory: project-epic8-ip-client-surface.md. # code-review 8-1 (Hierarchy-Scoped RBAC) → in-progress (NOT done): 10 patches applied incl. wiring scope into invocations/jobs/create-routes (3 leak surfaces EdgeCase found); Blind Hunter's CRITICAL ltree-bind bug empirically disproven; `unrestricted` internal-role bypass OPEN for architect. ruff ✅ 90+7 tests ✅. See 8-1 entry. # code-review 7-4 (Cloud Observability + HIPAA Compliance) → DONE; EPIC 7 COMPLETE. 3-layer adversarial (Blind+EdgeCase+Auditor, 0 failed). ALL THREE caught the DEAD BEAT (worker no -B, no beat service, false comment → publish_queue_depth never fired → queue-depth metric/widget/IAM dead). EdgeCase found 2 net-new HIGH (kombu rediss→CERT_NONE TLS downgrade; missing CeleryQueueDepth alarm). Auditor verified ALL compliance-doc facts vs real TF = accurate. 3 decisions resolved + 7 patches applied + 2 deferred + 4 dismissed. Patches: -B beat wiring (+staging/prod>1-replica double-fire DEFERRED), kombu CERT_REQUIRED, fan-out job_id tag, real _tag_sentry_job helper + de-tautologised 3 tests, dashboard/alarm period align, NEW queue-depth alarm, throughput incl task_failed. Gates: ruff ✅ pytest 38/38 ✅ tf validate+fmt ✅. T7 terraform apply still OPERATOR-GATED. See 7-4 development_status + deferred-work.md. # create-story 7-4 (Cloud Observability + HIPAA Compliance) → ready-for-dev. Source-verified: AC5 structlog done, AC3 reinterpreted (Sentry tracing), AC4 gap (Sentry skill_id/job_id tags in execution_tasks.py + invocations.py), AC1/2 greenfield TF (dashboard+alarms+SNS), AC6/7/8 greenfield compliance docs in velara-api/docs/. X-Ray dead-dep decision LOCKED (aws-xray-sdk in pyproject but zero imports; 7.1 D1 = Sentry-only). Operator-gated: terraform apply.  # code-review 7-3 (Cognito Authentication) → done. create-story 7-3 (Cognito Authentication) → ready-for-dev (backlog→ready-for-dev; story FILE now written — prior chain-entry was planning-only). FULL-STACK auth-provider cutover + Cognito IaC. 3 parallel source-verified audits (velara-api auth seam, velara-web auth surface, Cognito Terraform). KEY FINDING: the swap seam is PURPOSE-BUILT — AuthProvider Protocol→AuthPrincipal(user_id,org_id,role); single get_current_user/CurrentUser dependency (NO route changes); CognitoAuthProvider STUB at integrations/auth.py:196-212 carries the exact recipe (PyJWKClient+RS256+pyjwt[crypto]+normalise custom:org_id/custom:role); get_auth_provider() @lru_cache keyed on AUTH_BACKEND; AC5 401 envelope ALREADY byte-exact (VelaraHTTPException(401,UNAUTHORIZED,"Authentication required.")); aws-amplify^6 + @aws-amplify/auth^6 ALREADY in package.json (no dep add). 2 decisions LOCKED w/ user: D1 scope = FULL CODE + dev Cognito APPLIED (author all TF [cognito.tf user-pool+app-client+domain+gated SAML/OIDC slot AC7], implement BE CognitoAuthProvider + FE Amplify, apply a real dev pool on personal acct 068858795262/us-east-1/profile hozefa NO-BAA, flip dev AUTH_BACKEND=cognito [ecs.tf:65,148 today = dev?dev:cognito], exercise E2E — terraform apply + seed-users + VITE_COGNITO_* build-vars + dev cutover are OPERATOR-GATED, mark honestly per 7.1/7.2; RETAIN DevAuthProvider for local/CI); D2 login UX = Amplify SDK signIn() from the existing BRANDED LoginPage (username+password), NOT Cognito Hosted UI (matches arch "token refresh via Amplify SDK"). MUST BUILD: pyjwt[crypto] (RS256 unavailable today = HARD blocker); COGNITO_* config fields + extend the config.py fail-fast validator; fill the stub mapping all failure modes→InvalidTokenError (else 500 not 401); FE blast radius = auth.ts login/token/logout + NEW Amplify.configure (main.tsx) + NEW VITE_COGNITO_* env (.env/.env.example/vite-env.d.ts) + LoginPage password field + AC3 single error string; 2 NET-NEW GAPS — session-expired message on 401 (interceptor redirects w/o msg today, AC2) + a logout UI control (logout() called nowhere today, AC6); cognito.tf (NO Cognito TF exists) + COGNITO_* in ecs.tf environment block (non-secret). DEV-only; dev-shim retained; staging/prod tfvars authored-not-applied (BAA-gated). 3 decisions LEFT-TO-DEV w/ recommendation: ID-token-vs-access-token (recommend ID token — carries custom claims, no Lambda); sync→async token bridge (recommend cache token to sessionStorage so getToken() stays sync, client.ts untouched); Amplify.configure in main.tsx. Epic 6 forward-deps (FR-SEC-12 e-sig re-auth + printed-name resolution) CAPTURED to deferred-work.md (T13) NOT built. Out: 7.4 compliance docs/CloudWatch, Epic 8 RBAC, Azure AD activation (Phase 2). Memory: project-epic7-cognito-auth-seam.md. Baselines: web ~313 / BE unit. ||| Prior: code-review 7-2 (CI/CD Pipeline Setup) → done. 2 decisions resolved + 7 patches applied (HEADLINE: added DB-migration run-task to deploy.yml — the missed migrations step) + 9 deferred + 6 dismissed. Patches in working tree, not committed. Prior: dev-story 7-2 (CI/CD Pipeline Setup) → in-progress (ready-for-dev→in-progress). Implementing deploy automation: 2 deploy.yml pipelines + NEW Terraform (OIDC provider + per-repo least-priv deploy roles + ECS deployment_circuit_breaker) + VITE_API_URL build injection. AC1/AC2 verify-only (CI as-built). AC3/AC4/AC5 config-complete-but-operator-gated (terraform apply + repo vars + first main-push). Prior: create-story 7-2 (CI/CD Pipeline Setup) → ready-for-dev (backlog→ready-for-dev; story FILE now written — prior chain-entry was planning-only). DEPLOY-automation + CI→AWS-auth + small TF, DEV-only on personal acct (us-east-1), NO app code change. 3 parallel source-verified audits (velara-api/velara-web/architecture). AC1/AC2 ALREADY satisfied (both ci.yml complete) = verify+retain; the work is replacing the 2 deploy.yml workflow_dispatch stubs ("Story 1.4"). 5 decisions LOCKED: D1 CI verify-only (api lint/test[PG16+Redis7 svc-containers]/build/openapi on push+PR-to-main; web verify=node20 typecheck/lint/vitest-run/build); D2 CI→AWS auth = NEW GitHub OIDC provider (account-global → create ONCE in velara-api, reference from velara-web; dup=EntityAlreadyExists) + per-repo least-priv deploy roles scoped to repo:Vitalief/<repo>:ref:refs/heads/main, NO access keys (role ARN via repo vars); D3 AC5 rollback = NEW deployment_circuit_breaker{enable,rollback} on BOTH ecs services (ABSENT, grep=0) — api rolls back on /health/ready, worker only on task-start failure (no ALB/healthCheck — asymmetry documented); D4 AC4 CloudFront invalidation CONDITIONAL (dist defined in cloudfront.tf but NOT live — 7-1 new-account gate, deferred-work.md:205; s3 sync UNCONDITIONAL to velara-dev-frontend, invalidate only if CLOUDFRONT_DISTRIBUTION_ID resolves, never hard-fail); D5 image tag dev-<sha> NOT :latest (task defs pin :latest but ECR lifecycle manages only v/dev-/staging-/prod- prefixes — :latest unmanaged + useless for rollback). Reuses 7-1 deploy seam: ecs ignore_changes=[task_definition,desired_count] (won't drift CI deploys), /health/ready ALB gate (checks DB+Redis+S3), ECR outputs, frontend_bucket_name/cloudfront_distribution_id. FLAG (not deploy-scope, don't propagate): velara-api CI test-job DB env (velara@localhost) vs conftest force-override (velara_test@postgres) — two-fold name+host mismatch; deploy runs no pytest. FE build MUST inject VITE_API_URL=dev-ALB-DNS (NO .env.production; .env=localhost:8000 → broken bundle otherwise). Operator-gated (mark honestly, 7-1 precedent): terraform apply (OIDC+role+circuit-breaker) + repo vars (AWS_DEPLOY_ROLE_ARN/VITE_API_URL/CLOUDFRONT_DISTRIBUTION_ID) + first main-push deploy. Trigger choice (push-to-main vs workflow_run on CI success) left to dev (recommend workflow_run). Both remotes github.com/Vitalief/{velara-api,velara-web} on main. Out: Cognito=7.3, CloudWatch/X-Ray/compliance=7.4, staging/prod apply (BAA-gated). Memory: project-epic7-cicd-deploy-seam.md. Prior: create-story 9-2 (Audit Log Query API) → ready-for-dev. BACKEND-only, NO migration. Greenfield read — NEW GET /api/v1/audit (internal-only), filters + pagination, scoped by 8.1 hierarchy_scope, reads the 9.1 partitioned audit_log_entries. Source-verified. Mirror the list_certifications+PageMeta template. 5 decisions LOCKED: D1 [SAVED Q] AC5 page_size/total_count/next_page don't exist → follow PageMeta (page/per_page, data.page); D2 ltree `<@` filter is NET-NEW → text() CAST to ltree[] (asyncpg no codec), EMPTY scope→zero rows (critical safety); D3 [SAVED Q] outcome stored `failure` not AC4's `failed` → Literal Query of actual values; D4 AuditRead = full row (internal-only) + skill_name outer join; D5 internal-only mount + hierarchy_scope (8.1) hard prereq. AC2 client_id→get_client.hierarchy_path; AC3 partition pruning auto (created_at in WHERE). Depends 9.1+8.1 (contexted, not built). Memory: project-epic9-audit-query-api.md. Prior: create-story 9-1 (Audit Log Write Path) → ready-for-dev; epic-9 backlog→in-progress (first story, the FINAL epic — Audit Log, Usage & Analytics). BACKEND-only, migration-heavy. EVOLVES the existing invocation-only audit_log_entries (Epic 3) into a partitioned general event log. Source-verified audit. 5 decisions LOCKED: D1 month-partition via DROP+recreate (no real audit data; composite PK (id,created_at); DEFAULT partition; no inbound FKs); D2 rename record_entry→record_invocation (5 callers + 9 tests) + derive event_type (fan_out→invocation.fan_out else invocation.<outcome>); D3 rename invocation_id→parent_invocation_id + ADD job_id (own job) + event_type + metadata JSONB; make runtime_type/outcome NULLABLE for admin entries; D4 record_admin_action() NEW + wire transition_lifecycle + record_certification (2 unaudited admin paths; 8.1 access-grant deferred to 8.1); D-gotcha `metadata` reserved on SQLAlchemy Base → map via event_metadata attr. Append-only preserved (row trigger auto-propagates to partitions; TRUNCATE statement-trigger does NOT → re-apply + test). Migration 0016. #1 regression surface: rename+job_id+partitioned table+NOT-NULL event_type touch all 5 invocation callers + raw-insert immutability tests. Out: query API=9.2, UI=9.3, analytics=9.4/9.5. Memory: project-epic9-audit-write-path.md. Prior: create-story 8-4 (Client Portal — Skill Discovery & Invocation) → ready-for-dev. FINAL Epic 8 story → Epic 8 fully contexted (8-1..8-4 all ready-for-dev). FE + small BE (client skill-list endpoint + AC6 path fix), NO migration. Source-verified audit. 5 decisions LOCKED: D1 [crux, SAVED Q] NO project↔skill attachment model exists (skills org-global, coarse scope field, FE useProjectSkills is a mock ignoring projectId) → client availability = scope-filtered org skills gated by 8.1 grant + visibility (NOT a per-engagement attachment table — deferred); D2 NEW GET /api/v1/client/skills + narrow ClientSkillRead (id/name/description/scope only) + AC5 visibility filter MUST-BUILD (client_facing+client_ready, internal list has none); D3 RunConsole NOT reusable (leaks visibility/lifecycle/job-internals + /internal nav) → client-only run/output reusing only download pattern + NEW client hooks vs 8.2 /client routes; D4 [SAVED Q] AC6 audit VERIFY-ONLY except hierarchy_path="org" for non-LD invocations → resolve engagement path for client invocations (or accept "org" placeholder); D-deps 8.1/8.2/8.3 (ready-for-dev, not built). Memory: project-epic8-client-skill-discovery.md. Epic 8 retrospective after 8.4. Prior: create-story 8-3 (Client Portal Authentication & Shell) → ready-for-dev. FE + small NEW BE read endpoint, NO migration. Source-verified audit. 5 decisions LOCKED: D1 [crux, SAVED Q] engagement-name + granted Clients/Projects aren't in the JWT and the internal hierarchy API is the WRONG tenant (client org=org_client_001, engagements live in org_vitalief; + 8.2 404s clients) → NEW GET /api/v1/client/engagements (on the 8.2 client router) resolving 8.1 GRANTS → Vitalief-side granted nodes = cross-org-BY-GRANT read (scope by grants not org_id), IP-safe ClientEngagementRead; D2 [SAVED Q] SEQUENCING — backend depends on 8.1 grants + 8.2 client router (both ready-for-dev NOT built) → 8.1→8.2→8.3 or land pure-FE half first; D3 split RequireAuth into RequireClient/RequireInternal + role-aware login redirect + shared isClient() helper; D4 NEW minimal ClientAppBar/ClientShell (don't reuse internal AppBar — role-switcher/⌘K/M.Maxwell forbidden by AC2); D-cognito role-redirect orthogonal to 7.3. Preserve title isolation. Out: skill discovery/run=8.4, visibility filter=8.4. Memory: project-epic8-client-portal-shell.md. Prior: create-story 8-2 (IP Protection — Client API Surface Enforcement) → ready-for-dev. BACKEND-only, NO migration (routing + Pydantic-serialization). Source-verified. KEY REFRAME: skill internals are S3-ONLY (not DB columns) → 8.2 excludes the pointer/metadata fields via separate client schemas + a /api/v1/client router (no skill routes) + a reject_client 404-guard on internal routers (404-not-403, cross-org no-existence-oracle convention). 4 decisions LOCKED: D1 [crux, SAVED Q] IP guarantee = schema-omission+router-omission (structural); 404 = access expression (both trees in one app → "404-as-policy"); D2 NEW /client router reusing queue_invocation + InvocationAccepted (don't repurpose /invoke); D3 ClientJobRead = fresh NARROW whitelist (job_id/status/output-URLs/error.message), not exclude=; D4 no migration (visibility filter=8.4). Depends on 8.1 role primitives (adds 404 guard itself if 8.1 not merged). #1 RISK: 404 guard on internal routers → confirm internal tokens still 200. Out: portal=8.3/8.4, visibility filter=8.4. Memory: project-epic8-ip-client-surface.md. Prior: create-story 8-1 (Hierarchy-Scoped RBAC Enforcement) → ready-for-dev; epic-8 backlog→in-progress (first story, Access Control & Client Portal). BACKEND-only. FOUNDATIONAL authZ — first story where role is ENFORCED. 2 source-verified audits. Hierarchy IS real ltree GiST-indexed (Epic 4 done); BUT skills/certs are org-GLOBAL not pathed, non-LD jobs carry literal hierarchy_path="org". 5 decisions LOCKED: D1 [crux, SAVED Q] AC2 "all ORM queries AUTOMATICALLY filter" is OVER-REACH (NO central query-filter seam; contextvars don't reach Celery workers; skills/certs not pathed) → enforce at the ACCESS BOUNDARY (hierarchy_scope dep resolves grants→request.state; hierarchy CRUD + invocation study_id/location_id node-check path<@scope→403), NOT a global do_orm_execute auto-filter; D2 cross-org STAYS 404 / in-org-out-of-scope = NEW 403 FORBIDDEN (reuse reserved-never-raised HierarchyScopeError hierarchy_service.py:70-74); D3 grant authority=`consultant` (NO admin role; roles captured-never-enforced) → platform's FIRST role-gate (require_role) [SAVED Q]; D4 grant stores node_id+node_type (resolve path at request time), NO caching (AC5). NEW: access_grant model+migration 0016 (head 0015) + access_grant_service + hierarchy_scope+require_role deps + /access-grants router (router-level dependencies on data routers, skip health/auth) + boundary checks (closes deferred-work.md:130 invocation skill-scope). #1 REGRESSION RISK: existing tests have NO grants → dep must RESOLVE-only + seed grants in node tests + mint a custom same-org-no-grant principal (seed users give only 2 orgs). Out: /client-vs-/internal split=8.2, portal=8.3/8.4, skill-visibility filter=8.4, global auto-filter, grant UI. Memory: project-epic8-rbac-foundation.md. Baselines: BE suite (0015→0016). Prior: create-story 7-4 (Cloud Observability, HIPAA & 21 CFR Part 11 Compliance Baseline) → ready-for-dev. FINAL Epic 7 story. IaC + small BE Sentry-tag + verify test + 3 compliance docs. Source-verified audit per-AC: AC1 dashboard GREENFIELD TF (ALB metrics free for req/latency/error; Celery queue-depth+throughput need custom PutMetricData/log-metric-filter + worker IAM = only non-trivial part); AC2 alarms+SNS GREENFIELD TF (error>1% metric-math, P95>3s percentile, 5-min→new topic); AC3 X-Ray REINTERPRET via Sentry tracing (aws-xray-sdk==2.14 DEAD dep, no working ASGI middleware — 1.1 removed it, 7-1 D1 Sentry-only; FastApi+Celery integrations already give the API→service→DB→Celery span chain) [DEVIATION—saved Q]; AC4/AC5 Sentry 80% DONE (env tag+PHI before_send+integrations wired) — ONLY add skill_id/job_id set_tag in execution_tasks task path; AC6 logs no-PHI DONE Epic 1 (structlog JSON+request_id+sanitize_phi) → VERIFY+test (message key non-dev only); AC7/AC8 docs AUTHOR-FROM-SCRATCH. 5 decisions LOCKED: D1 AC3=Sentry not X-Ray; D2 docs in velara-api/docs/ (parent not git repo; reference requirements-inventory.md:100-103,185-203; validation-plan records FR-SEC-12 IQ/OQ/PQ+non-repudiation DEFERRAL) [saved Q]; D3 greenfield dashboards/alarms + cheap custom Celery metric (no agent/ADOT); D4 add skill_id/job_id tags + AC6 verify-only (don't touch sanitizer/env tag); D-dev-only operator-applies TF+SNS-sub. TRAPS: don't instrument the dead aws-xray-sdk; don't rebuild Sentry/structlog/sanitizer; Celery metrics not free; test cloud JSON path; don't fabricate tags; Container Insights groups bypass 90d. DEV-only; staging/prod+IQ/OQ/PQ+RBAC(Epic8)+audit(Epic9) out. Epic 7 now fully contexted (7-1 done; 7-2/7-3/7-4 ready-for-dev). Memory: project-epic7-observability-compliance.md. Baselines: BE unit / web 293. Prior: create-story 7-3 (Cognito Authentication) → ready-for-dev. FULL-STACK + IaC. 2 source-verified audits. BE seam GENUINELY CLEAN (AuthProvider Protocol→AuthPrincipal; every route uses CurrentUser; CognitoAuthProvider STUB exists; AC5 401 envelope ALREADY byte-exact). MUST BUILD: fill the stub (PyJWKClient+RS256+leeway+manual token_use; map sub/custom:org_id/custom:role from ID token); add pyjwt[crypto] (RS256 unavailable today = HARD blocker); add COGNITO_* config; keep DevAuthProvider (tests default AUTH_BACKEND=dev). FE blast radius MORE than auth.ts (epic over-claims): auth.ts + client.ts (async Amplify token + silent refresh; existing 401→/login = AC2) + NEW Amplify.configure() + NEW VITE_COGNITO_* env + LoginPage (password + AC3 error). Routes/guards/feature screens UNTOUCHED (AuthUser shape preserved → cert tests green). INFRA GAP: NO Cognito pool in TF → NEW cognito.tf (pool+client+INACTIVE IdP slot AC7+seed users). 4 decisions LOCKED: D1 fill-stub; D2 FE not-contained-to-auth.ts; D3 PROVISION+PROVE Cognito but KEEP dev on the SHIM (live cutover=deliberate operator flip, not auto-on — SAVED Q vs flipping dev→cognito for true E2E); D4 Epic-6 forward-deps capture-but-defer (printed-name only if name on current ID token else own story; FR-SEC-12 re-auth DEFER P2). 2 STALE deferred-work flags now FIXED (client.ts getToken not storage; LoginPage redirect already correct). Memory: project-epic7-cognito-auth-seam.md. DEV-only; cutover+RBAC(Epic8)+CloudWatch/compliance(7.4)+Azure-AD(Phase2) out. Baselines: web 293 / BE unit. Prior: create-story 7-2 (CI/CD Pipeline Setup) → ready-for-dev. DEPLOY-only (PR-validation CI already built in BOTH repos: api ci.yml lint/openapi/test/build = AC1, web ci.yml typecheck/lint/test/build = AC2). Both deploy.yml are workflow_dispatch stubs saying "implemented in Story 1.4 (CI/CD setup)" = THIS story. 2 source-verified audits confirmed the 7-1 deploy seam (ECS ignore_changes=[task_definition,desired_count], /health/ready ALB gate, ECR repos+outputs, S3 velara-dev-frontend + cloudfront_distribution_id output). 5 decisions LOCKED: D1 verify+retain CI / implement deploy stubs; D2 CI→AWS auth = NEW GitHub OIDC provider + least-priv deploy roles (no access keys; NONE exists today in either repo); D3 AC5 rollback = NEW terraform deployment_circuit_breaker (absent); D4 AC4 CloudFront invalidation CONDITIONAL (distribution defined in TF but NOT live — 7-1 new-account gate, deferred-work.md:205 → s3 sync unconditional, invalidate only if id resolves, never hard-fail); D5 inject VITE_API_URL=dev-ALB-DNS at build (else bundle ships localhost). Tag images dev-<sha> not :latest (rollback needs immutable tags). DEV-only personal acct us-east-1; staging/prod BAA-gated. Operator-gated: terraform apply + repo vars + first main-push deploy (mark honestly, 7-1 precedent). NO app code change. Out: Cognito=7.3, CloudWatch/X-Ray/compliance=7.4. Memory: project-epic7-cicd-deploy-seam.md. Prior: retrospective epic-6 → done (epic-6-retro-2026-06-29.md; killed milestone-framing carry, attached Epic 6 compliance deferrals to Story 7.3 Cognito). Prior: code-review 6-4 → done (1 patch applied: toUTCStr "Invalid Date" guard; 2 decisions dismissed; 1 deferred: per_page:200 cap; AC1–AC4 + locked decisions verified PASS). Prior: create-story 6-4 (Certification History & Re-Certification on New Version) → ready-for-dev. FRONTEND-only (+1 optional BE verification test). Source-verified subagent probe: ALL 4 AC areas already built+tested on BE (AC4 history list = cross-version certified_at-ASC per_page≤200; AC2 create_version resets client_ready→draft + version-FK'd certs preserved; AC3 review_required flag + DERIVED_SKILL_REVIEW_REQUIRED block + POST /skills/{id}/acknowledge-parent-update). NO endpoint/migration/model/schema change. 3 decisions LOCKED: D1 history+re-cert+derived-warning live on SkillDetail.tsx (cert screen is internal_ready/current-version-only — can't show a re-versioned draft); D2 add a real Acknowledge button (FE shows the review error today but can't clear the flag — half-built AC3); D3 group history by version client-side from the existing flat list + raise per_page→200 (closes 6.3 page-1 truncation defer). NEW FE: CertificationHistory.tsx (reuses useCertifications) + acknowledgeParentUpdate fn/hook (mirror transitionLifecycle) + SkillDetail edits (history mount, "Requires re-certification" badge gated on 0-current-certs AND ≥1-prior-cert, "Parent skill was updated…" warning + Acknowledge). DO NOT fix the RECERTIFICATION_REQUIRED discriminator (spec-sanctioned, deferred). Baselines: web 293 / cert integration ~24 / BE unit 497. Prior: code-review 6-3 → DONE (AC1/AC2 PM-signed-off). Prev: code-review 6-3 → in-progress (AC1/AC2 amendment pending John). Prev: create-story 6-3 (Methodological Certification Workflow UI) → ready-for-dev. FULL-STACK: the SECOND-key half. AC4 auto-advances skill to client_ready on the methodological key. 3 decisions LOCKED: D1 auto-advance SERVER-SIDE in record_certification (reuse evaluate_certification_eligibility + call already-wired transition_lifecycle w/ for_update lock + two-key gate; FR-CRT-01 invariant for any caller; accepted cost = 2 non-atomic commits, self-heals via manual PATCH); D2 FE gates meth-button on technical-first but BE stays order-agnostic; D3 derived review_required auto-advance failure swallowed (cert stays valid, skill stays internal_ready). REUSE-not-recreate (6.2 shell already methodological-aware): CertKeyBadge dual-type, getBucketLabel, api/hooks/errors/route all cert-type-agnostic. NEW: RecordMethodologicalCertModal (clone tech modal — 3-item FR-CRT-03 checklist, "methodology and voice" wording, certification_type:'methodological') + extend CertificationDetail (hoist methCert, modal discriminator, 2nd button+manifestation) + FIX useRecordCertification to invalidate ['skills']+['skills-page'] (auto-advance left Registry/⌘K stale). NO migration/model/schema/new-endpoint. Out: cert-history+re-cert = 6.4; RBAC = Epic 8; RECERTIFICATION_REQUIRED discriminator stays deferred. 3 questions saved for John re: D1/D2/D3 (see 6-3 story). Prior: code-review 6-2 (Technical Certification Workflow UI) → DONE. 3-layer adversarial; all 6 ACs verified. 2 decisions resolved + 9 patches applied + 1 defer + 7 dismissed. 🔴 CRITICAL caught & empirically proven against live PG: GET /certifications/pending 500'd (MissingGreenlet — lazy skill.versions in async, no selectinload); the pending tests skip when infra is down so it slipped through dev. Fixed via explicit SkillVersion id→version query; rebuilt the api image + re-ran GREEN. + stale-version-cert disable fix, SKILL_NOT_CERTIFIABLE lifecycle guard (BE+FE), AC6 signature_meaning render, null-user e-sig block, focus-trap fix. Gates: web 279 (+3) typecheck/lint/build green; api ruff clean / 19 cert integration (+2) / 612 regression 0 regressions. See 6-2 Review Findings + deferred-work.md. Prior: create-story 6-2 (Technical Certification Workflow UI) → ready-for-dev. FULL-STACK (Decision D1): new BE GET /api/v1/certifications/pending (no migration/model change — reuses evaluate_certification_eligibility) + FE Certification screen (unified pending list + detail panel + Record-Technical-Cert modal w/ checklist + Part-11 e-sig) consuming the 6.1 API. D2 signer=user_id+role (no display name until Epic-7). FE scaffolding pre-stubbed (tab/route/api/types) — FILL not recreate. See the 6-2 development_status entry. Prior: code-review 6-1 (Certification Data Model & State Machine API) → DONE. 3-layer adversarial (Blind Hunter + Edge Case Hunter + Acceptance Auditor): all 7 ACs + 14 dev-traps + D1-D3 verified satisfied; migration chain + both skill_service edits (gate, version-reset) confirmed correct. 5 patches applied (retired-skill cert guard → 422 SKILL_RETIRED; DB-trigger immutability test [migration-0015 append-only now exercised, not just HTTP-405]; certified_at request-window assertion; eligibility-query org/version-filter test; TOCTOU for_update lock in transition_lifecycle). 1 deferred (AC4 RECERTIFICATION_REQUIRED discriminator = "any prior cert" simplification — spec-sanctioned Task 5:114, revisit 6.3 w/ state-history table → deferred-work.md). 3 dismissed. Re-verified post-patch: 13 cert integration + 20 cert unit + 158 skills/lifecycle tests pass (0 regressions), ruff clean. Prior: create-story 6-1 (Certification Data Model & State Machine API) → ready-for-dev; epic-6 backlog→in-progress (kicked off out-of-sequence, chosen over continuing Epic 7). 3 decisions locked w/ user (records-resource shape + gate on existing /lifecycle; CertificationRecord only; store user_id now / printed-name=Epic-7). See the 6-1 development_status entry. Prior: 7-1 (AWS Infrastructure Foundation) code review → DONE (6 patches applied incl. Redis CERT_REQUIRED + terraform .gitignore state-leak fix; AC4/AC1-HTTPS honestly marked deferred-in-dev; 6 deferred, 5 dismissed). terraform apply remains an operator gate (config-verified, not live-verified). See the 7-1 development_status entry. Prior note: Epic 7 KICKED OFF out-of-sequence (user: "epic 7 need to do that one first" — before Epic 6). create-story REACTIVATED the preserved 7-1 (AWS Infrastructure Foundation): epic-7 backlog→in-progress, 7-1 backlog→ready-for-dev. SCOPE NARROWED w/ user (new Decision D4): DEV environment ONLY, on the user's PERSONAL AWS account, NO BAA; staging/prod explicitly gated on the client's AWS account + signed BAA. Verified vs real velara-api source: config.py:199-212 fail-fast guard relaxes when ENVIRONMENT=dev (so personal-account dev runs without forcing prod secrets discipline), but the story still builds Secrets Manager + S3 backend wiring so the later staging/prod cutover stays config-only (STORAGE_BACKEND=s3/SECRETS_BACKEND=aws/AUTH_BACKEND=cognito). celery_app.py:19-20 uses REDIS_URL directly w/ NO broker_use_ssl → D3 rediss:// app-side gap still live. baseline_commit NO_VCS→281bba7 (velara-api is a git repo on main). Greenfield IaC (no terraform/ dir anywhere). D2 already RESOLVED (S3+CloudFront, no velara-web Fargate). Prior: Story 5.5-6 (Adapt & Run Client Protocol-Extractor E2E — HEADLINE GATE) → done. HEADLINE GATE PASSED. ARROW NCT03037385 (149pp) → job 6635015d, completed, schema_version 0.3.0, 10 arms/16 encounters/25 activities/102 instances/0 QA flags. Epic 5.5 closed. Fixes: writable worker volume, dispose_engine() event-loop teardown, EXECUTION_MAX_MEMORY_MB=4096. 722 BE tests pass, ruff clean. Prior: (create-story 2026-06-26; the INTEGRATION PROOF that exercises every 5.5 story against the REAL ~/Downloads/velara-protocol-extractor, not a stub. 3 locked decisions: D1 skill-side adapter shim for the named-kwargs-vs-params-dict mismatch [NO platform runner change]; D2 no Phase-1 cert gate [all code-driven trusted, "certified"=Epic-6 forward-dep]; D3 full adapt+corrected manifest+real-PDF live E2E. CRUX = the install seam: manifest IS the artifact [no zip extract], so velara_extractor must pip-install via a bare-local-path requirements line [_validate_requirements allows it]. FE 100% done by 5.5.5; envelope already reconciled by 5.5.5; expect ~0 platform code change beyond tests. See full Dev Notes in the story file + the 5.5-6 development_status entry below). Prior: Story 5.5-5 (Multi-Artifact + Canonical Output + blocked Job State) → done (code review 2026-06-26, uncommitted working-tree diff across BOTH sub-repos [velara-api 882a19a + velara-web 5cb902f; parent dir not a git repo]: 3-layer adversarial, all ran. AC1–AC4 SATISFIED, AC5 deferred, no DO-NOT-TOUCH crossed [aggregate_results/jobs READ API/InvocationResult schema+migration/InvocationRequest untouched], no migration. NO Critical/High actionable. 3 patch / 3 defer / 8 dismissed. 3 PATCHES APPLIED: jobs.py:36 JobStatusLiteral +"blocked" [GET /jobs?status=blocked was 422 though FE sends it]; audit.py:85 stale outcome comment +"| blocked"; RunConsole.tsx blocked panel given the same single-file output_file_url/key fallback the completed panel uses. 3 DEFERRED [D1 HIGH-but-UNREACHABLE: aggregate_results:421-431 has no blocked-child branch → blocked fan-out child silently rolls up to completed, but code-driven∧location_dependent intersection is empty today + story L116 forbids touching aggregate_results → log for the future code-driven-fan-out epic; D2 unbounded artifact read OOM risk, mitigated by RLIMIT_AS; D3 mid-loop upload failure orphans bucket objects, no orphan-GC]. 8 DISMISSED false positives [Blind's "missing url" — READ API jobs.py:132-178 presigns output_files[].url at fetch time, VERIFIED; "is_blocked UnboundLocalError" — same-success-path; "ineffective mock target" — executor imports run_in_threadpool function-locally so origin-module patch works]. Gates: ruff clean, BE 422 unit pass 0 regressions, FE typecheck 0, eslint clean, 54 RunConsole tests pass. Findings in story Review Findings + deferred-work.md). Prior 5.5-5 → ready-for-dev (create-story; FULL-STACK; the OUTPUT half of the code-driven path. 3 locked decisions: REUSE result_metadata["output_files"] JSONB (no InvocationResult migration — 3.6 mechanism + jobs READ API + Run Console already presign/render it); DEFER consumes-refusal to the pipeline epic (no upstream_job_id mechanism exists; logged); FULL-STACK (BE bulk + small FE blocked badge). Reshapes CodeDrivenResultEnvelope.artifacts list→dict to match the REAL skill (closes 2 BLOCKS-5.5.6 items); uploads all artifacts from output_dir; new JOB_STATUS_BLOCKED terminal + mark_blocked (no migration); worker branches on result_metadata["status"]. ⚠️ audit outcome is a validated frozenset — MUST add OUTCOME_BLOCKED. See sprint entry for 5.5-5). Prior: Story 5.5-4 (Raw-File Input to Code-Driven Hybrids) → done (dev-story + code review 2026-06-26: 3-layer adversarial, all 4 ACs verified [raw ingest_key/ingest_storage NOT the parsed text seam; PHI-safe log; AC3 text seam untouched + inspect.signature guard; multi-file argv layout in lockstep], no Critical/High/Medium; 3 doc-accuracy patches, 1 deferred [pre-existing cross-runtime UUID-parse], 5 dismissed; 437 unit + 6 integration pass). Prior: Story 5.5-3 (Code-Driven Hybrid Executor — Phase-1 Venv) → done (code review 2026-06-26, uncommitted diff: 3-layer adversarial + Docker integration suite RUN against live PG+MinIO. 11 patches applied & verified — 3 HIGH security [os.environ leak; pip requirements arbitrary-code-exec; unbounded-stdout OOM] + 3 found only by running the never-before-executed tests [non-existent monkeypatch target + nested asyncio.run; macOS RLIMIT_AS crash now tolerated + non-timeout subprocess errors → failed; output-cap inline read that blocked the wall-clock timeout]. 3/3 integration tests pass [9.3s], 392+26 unit 0 regressions, ruff clean. Details in story Review Findings + sprint entry below). Prior: Story 5.5-2 (Schema-Versioned Output Contract) → done (code review 2026-06-26, uncommitted diff: 3-layer adversarial [Blind/Edge/Auditor]. All 3 ACs VERIFIED; scope boundary fully honored; no "Do NOT touch" files modified; all claimed tests exist; NO Critical/High. 3 decisions → all accepted as-is [D1 asymmetric consumes validation (schema_ref unchecked, {} accepted) = format-only schema_version matches AC2, strict shape deferred to 5.5.5/Epic 6; D2 PATCH consumes diverges from current_version snapshot = intended, version snapshots creation-time-immutable; D3 unbounded consumes JSONB = consistent w/ input_schema/output_schema]. 1 patch APPLIED & ruff-clean: create_version now snapshots new_version.schema_version = skill.schema_version [was keyed off code_driven_manifest = two sources of truth; now mirrors create_skill]. 7 dismissed [migration revision-id≠filename = documented VARCHAR(32) workaround; _SEMVER_RE dup = spec-authorized circular-import guard; concurrent-PATCH stale snapshot = same-session no window; update_metadata generic field-loop verified correct; "15 unit tests" cosmetic framing; 2 needs-verify items confirmed handled by reading skill_service.py (schema_version set L331 before version row L350; PATCH-clear writes NULL via exclude_unset loop)]. Findings in story Review Findings section). Prior 5.5-2 → review (dev-story; BACKEND-only; migration 0014; 632 tests pass, 26 new, 0 regressions; ruff clean). Story 5.5-1 (Register Code-Driven Hybrid Bundle Manifest) → done (code review 2026-06-26: 3-layer adversarial; all 3 ACs satisfied. 3 decisions + 10 patches. HIGHs fixed: VERIFIED IP leak [field values echoed into 422 → now value-free] + stale schema_version/egress [D1 forbids cross-shape version switching via new HybridShapeMismatchError]. Strengthened manifest detection so an entrypoint-less manifest gets the targeted INVALID_CODE_DRIVEN_MANIFEST error [AC2 — original detect-by-entrypoint-key couldn't satisfy "missing entrypoint → named field"]. Also: D2 documented new fail-fast LLM-hybrid registration validation; D3 narrowed except→domain-only; removed duplicate _SEMVER_RE [lazy-import canonical]; tightened entrypoint regex; fixed tautological IP test; +egress/cross-shape/missing-requirements tests. Gates: ruff clean, 398 unit [+39], 92/92 test_skills [+8, 0 regressions], migration 0013 round-trip clean. 2 deferred [egress bound; UTF-8 BOM routing], 2 dismissed). Epic 5.5 → in-progress. Prior: Story 5-9 (Skill Registry Search — Wired to Server) → done (code review 2026-06-26: 3-layer adversarial, cleanly scoped to the 5-file 5.9 File List. All 6 ACs verified + whole-catalog useSkills/listSkills/AppBar ⌘K/useProjectSkills confirmed untouched. Both diff-only + source-aware layers converged on one root cause: pageHasFilters overloaded "server-side search active" with "client-side Type/Visibility filter active" — the same thing pre-5-9, not anymore. 3 patches applied & verified: (1) MEDIUM — split pageHasFilters → searchActive + pageLocalFilters; empty-state no longer says "No skills match your search." when a Type/Vis filter (no search) empties the page [now "No skills on this page match your filters."], +regression test; (2) indicator "Showing N of M on this page" wording now gated on pageLocalFilters only, so a clean server search shows the honest "Showing X–Y of N"; (3) debounce test timer setup investigated — shouldAdvanceTime:true is REQUIRED for user-event under fake timers [removing it timed out 5 tests], KEPT + documented why. 6 dismissed [immediate-page-reset vs debounced-q intermediate request = inherent to the spec-mandated pattern that avoids the "Page 3 of 0" flash; weak coalescing test already backstopped by the 300ms-gate test; "Axios drops undefined" verified true; mount setPage(1) no-op]. Gates: typecheck 0, lint clean, 252 web tests (+1 from 251, 0 regressions), build ✓. Closes the HIGH ?q= follow-up logged in the 5-7 review [5-8 backend + 5-9 FE]). Prior → review (dev-story; FRONTEND-only; 251 web tests, +6, 0 regressions; gates green). Story 5-8 (Skills Free-Text Search API) → done (code review 2026-06-26: 3-layer adversarial; production faithful to all 5 ACs. 2 decisions + 6 patches applied — escape LIKE metacharacters %/_/\ + escape="\\" so q is a true literal substring [raw interpolation broke the documented contract: q="100%"/q="%"]; q.strip() in pattern; Query(max_length=255)→422; fixed case-insensitive tautology + absent-unchanged self-comparison tests; real NULL-desc coverage via direct-engine seed [create API forbids NULL desc, so the AC1 NULL path was unreachable via API]. +4 tests. Gates: ruff clean, 84/84 test_skills [+4, 0 regressions]. 1 deferred [seq-scan/no trigram index, spec-sanctioned at ~3,485 rows], 1 dismissed [repeated ?q= last-wins]). Epic 5 retrospective complete. Story 5-7 (Skill Registry UI — Pagination) → done (code review 2026-06-26: 3-layer adversarial, scoped to the 6-file 5.7 File List [working tree also held uncommitted 5-4 WIP, excluded by user]. All 4 ACs + the #1 regression fix [listSkills unwraps .items, keeps Skill[] — ⌘K + useProjectSkills preserved] verified. 5 patches applied & verified: (1) MEDIUM regression — skill create/update/transition mutations now also invalidate ['skills-page'] [the migration to the paged cache had left the registry stale ~30s]; (2) listSkills defensive .items ?? []; (3) empty-trailing-page effect clamps to last valid page instead of decrement-by-one; (4) "Showing X–Y of N" indicator now honest when page-local filters active; (5) USER-RAISED — inline registry search is current-page-only & misleading under pagination: verified against source that velara-api has NO free-text param [skills.py:88-95 + skill_service.py:341-362 = only page/per_page/status/tag], so a true server-side search needs a BACKEND change [out of this FE-only story]; fixed the honesty in-scope [relabelled box "Filter this page…", added ⌘K whole-catalog hint] and logged the real ?q= fix as a HIGH backend follow-up in deferred-work.md. 1 deferred [internal.test.tsx carries some 5-4 mocks/tests from the mixed working tree]; 8 dismissed [self-masking pagination concerns — no placeholderData so controls unmount during fetch; correct-by-design mocked-hook test boundaries]. Gates: typecheck 0, lint clean, 245 web pass (+3 from 242, 0 regressions), build ✓). Prior → review (dev-story 2026-06-26: FE-only; fixed 5-6 listSkills break + added listSkillsPage/useSkillsPage; SkillRegistry server-side pagination w/ first Prev/Next + "Showing X–Y of N"; 242 web pass +18, typecheck 0, lint clean, build ✓). Prior → in-progress (dev-story start). Prior → ready-for-dev (create-story; FRONTEND-only, consumes 5-6 contract; no backend/migration/dep). MIRROR the 5-4 FE pagination precedent (api/jobs.ts PageMeta/JobListData/listJobs + useJobs + JobsHistory; JobsHistory has NO page controls → 5-7 adds the first Prev/Next). 2 in-story decisions LOCKED w/ user: (1) Prev/Next + "Showing X–Y of N" (not load-more/infinite); (2) ⌘K palette stays whole-catalog via own useSkills() path, only the registry TABLE paginates, inline search = current-page-only (documented; no backend free-text search). ⚠️ 5-6 ALREADY BROKE the FE: listSkills(skills.ts:4-6) reads data.data as Skill[] but data is now {items,page} → 3 useSkills() consumers broken (SkillRegistry:102, useProjectSkills:13→RunConsole, AppBar:41→⌘K). FIX: keep useSkills()/listSkills→Skill[] (unwrap .items, per_page:200, ≤200 whole-catalog cap) so RunConsole+⌘K unbroken; add NEW useSkillsPage→{items,page} for registry only. Server filters: ONLY ?status=(State→reset page1)+?tag=(no UI); Type/Visibility/search = client-side-within-page (no backend param — documented). Tests: update SkillRegistry.test (mock useSkillsPage) + prove 3 consumers still arrays. Baseline ~234 web. Prior: Story 5-6 (Skills List API Pagination) → ready-for-dev (create-story; BACKEND-only, no migration/dep/FE). MIRROR the 5-4 jobs pagination precedent (verified as-built; JobListData docstring names 5-6 as next adopter): SkillListData{items:list[SkillRead]; page:PageMeta}; list_skills→tuple[rows,total] w/ func.count()(same filters)+.limit/.offset; handler adds page/per_page Query, keeps status:LifecycleState(auto-422)/tag + empty-tag guard + created_at desc. ⚠️ BREAKING shape (data array→{items,page}) — FE consumer skills.ts listSkills:4-6 adapted by STORY 5-7 (after); do NOT touch velara-web; "backwards-compatible"=param defaults only. 1 backend caller (skills.py:99). Tests: update 5 existing list/tag assertions + add pagination tests. Baseline 58 test_skills/~570. Prior: Story 5-5 (REST API Documentation & OpenAPI Spec) → in-progress (code review 2026-06-25: 3-layer adversarial [Blind/Edge/Auditor], all 3 layers ran. AC1/AC2/AC3 SATISFIED [Swagger@/api/v1/docs, valid OpenAPI 3.1.0@/api/v1/openapi.json, InvokeRequest/InvocationRequest = exactly 5 safe fields + additionalProperties:false, no internals — verified]; contract/scope clean [no field change, no migration/dep/FE/behavior change]. NOT done because AC4 is UNMET: docs/api-spec.json (+scripts/export_openapi.py, +test_openapi.py) are UNTRACKED (?? in working tree) — `git diff --exit-code` cannot fail on an untracked file, so the committed-spec deliverable AND its CI staleness gate are INERT until the 3 files are committed (REPRODUCED: injected version:9.9.9-STALE, gate returned 0/passed). Spec CONTENT is correct/byte-identical to a fresh export. 3 PATCHES APPLIED & verified [P1 ci.yml: `git add --intent-to-add` before diff so untracked/new spec also fails the gate; P2 test asserts Swagger HTML references the moved /api/v1/openapi.json; P3 new test for relocated /api/v1/docs/oauth2-redirect 200 + default 404] → 8 tests pass [was 7], ruff clean, ci.yml well-formed. 2 DEFERRED [AC4-commit → dev's own commit step per user; unpinned `pip install -e .[dev]` in openapi job = pre-existing project-wide CI choice, uv.lock exists]. 5 DISMISSED [schema-vs-runtime IP lock = false positive: test_invoke.py:281 already POSTs system_prompt→422; tautology-after-exact-set; determinism-test redundant w/ CI gate; top-level import; .gitattributes]. NEXT TO DONE: commit the 3 untracked 5-5 files. Findings in story Review Findings + deferred-work.md.). Prior → review (dev-story: docs/spec relocated to /api/v1/*, deterministic export script, generated docs/api-spec.json [OpenAPI 3.1.0, 27 paths] NOT yet committed, openapi CI diff-gate, 7 new tests; ReDoc kept; 574 pass/0 regressions — 3 ingest fails pre-existing env). Prior → in-progress (dev-story start). Prior → ready-for-dev (create-story; BACKEND-only, no migration/dep/FE). FastAPI auto-generates the OpenAPI 3.1 spec — work = relocate docs to /api/v1/docs + /api/v1/openapi.json (main.py:43-48 FastAPI(); +swagger_ui_oauth2_redirect_url which does NOT follow docs_url — verified gotcha), scripts/export_openapi.py (deterministic via sort_keys; needs NO DB/Redis — lazy engine + Sentry-no-op verified), commit velara-api/docs/api-spec.json, new 'openapi' CI job (export + git diff --exit-code), tests (spec@v1/valid-3.x/default-404/Swagger-HTML/invoke-no-internals lock). EXHAUSTIVE research (subagent empirically ran app.openapi()) surfaced TWO epic-AC corrections: (1) AC3 "context+inputs only" names a PHANTOM field — no `context` exists; real InvokeRequest=file_ref_ids/inputs/location_id/study_id/fan_out + additionalProperties:false; IP-protection already satisfied → AC3 = verification test NOT a schema change; adding `context` would BREAK the load-bearing 5-1..5-4 invocation contract (extra=forbid); (2) AC4 "velara hub repo docs/" is stale — hub root is NOT a git repo & has no CI; real home = velara-api/docs/ + velara-api/.github/workflows/ci.yml. 1 open decision (keep/disable ReDoc). Baseline ~570 BE tests. No migration/dep/FE. Prior: Story 5-4 (Job Status Polling & Output Display) → ready-for-dev (create-story). SCOPE LOCKED w/ user → FULL-STACK. Closes the Run Console loop (5-2/5-3 stubbed it). 3 deliverables: live 3s polling (replace JobStatusPanel STUB RunConsole.tsx:783-807 w/ useJob — FIRST refetchInterval in app, stops at terminal status via fn form); Jobs History screen — REQUIRES a NEW GET /api/v1/jobs list endpoint that does NOT exist (jobs router only has GET /{job_id}+POST /{job_id}/cancel; verified via 2 BE subagents) → user chose full-stack over deferring; localStorage persist of activeJobId (zustand persist, FIRST in app). Verified job-read contract: failed job=HTTP 200+error{code,message}; output = S3 files only (NO inline text), presigned 24h URLs may be null, direct-to-S3 download; fan-out parent has inline children[] (no completed_count → count client-side), each child location_name may be null; no skill_name on job (outer-join Skill). NEW BE: GET /api/v1/jobs org-scoped/paginated(page/per_page 50/max200)/newest-first, light JobSummary, FIRST PageMeta consumer (recommend JobListData{items,page} in data), mirror list_children+list_skills, no migration, idx already support org+status+created_at. NEW FE: useJob poll hook, live JobStatusPanel (status+output+failed+TryAgain), fan-out X-of-N + per-location grouped (net-new UI, no prototype), JobsHistory screen+/internal/jobs route+jobs NavTab (edit navTabsData), friendlyJobError map. Reuses JobStatusBadge/JOB_STATUS_META (already V3-correct, RunConsole.tsx:59-81). 3 open impl decisions logged. Baselines: ~560 BE / 24 test_jobs.py integration, 204 web. No migration. Prior: Story 5-3 (Run Console — Skill-First Mode) → done (code review 2026-06-13: 3-layer adversarial; all 5 ACs + #1 contract truth verified; 4 patches applied & verified [retired/no-version skill pre-flight gate in canRun + inline reason; 0-location dead-end message; ▶ glyph aria-hidden so Run button accessible name is "Run"; replaced tautological tab-active test with a real toHaveClass('font-bold') assertion in internal.test.tsx]; 4 deferred [inputs/file_ref_ids→later story; runMode-on-success→5-4; friendlyInvocationError non-string-message coercion; EngagementsScreen +95 = 5-2 wiring, not 5-3]; 7 dismissed [incl. useProjectSkills MOCK SEAM = INV-09 by design, and false double-submit asymmetry]. Gates: typecheck 0, lint clean, 204 web tests, build ✓). Prior → review (dev-story; FRONTEND-only; 203 web tests, +28, 0 regressions; gates green). Extends as-built 5-2 shell in place: skill-first = locked green skill card (useSkill) + editable Client→Project→Study cascade (port of design/internal3.jsx HierarchyPicker; useClients/useProjects/useStudies all exist, gated by enabled). Wires SkillDetail.tsx:210-221 disabled Run button live → recommended new skills/run/:skillId route (keeps Skills tab via useActiveTab segment[2]; entityPath/validOrigin only know project|study so a skill origin would need special-casing). Makes RunConsole runMode-aware (5-2 hardcodes setRunMode('context-first') at :388). Reuses LocationSelector(:188-264)/JobStatusPanel stub(:516-540)/useCreateInvocation/friendlyInvocationError(all invocation 422 codes already mapped). EXHAUSTIVE backend research (2 subagents) surfaced the #1 disaster to prevent: InvocationRequest (schemas/invocation.py:41-47, extra=forbid) has NO client_id/project_id — sending them = 422; for non-location-dependent skills the user's Client→Project→Study pick is INVISIBLE to backend (hierarchy_path='org', payload {}), only location_id/study_id reach it for LD skills (study_id ignored+not stored for non-LD). 1 open decision (route placement). Baseline 175 web tests. No backend/migration/dep. Prior: Story 5-2 (Run Console — Context-First Mode) → done (code review 2026-06-12: 3-layer adversarial, all 5 ACs, 2 decisions resolved + 9 patches applied & verified + 9 dismissed; corrected false "design prototype not found" dev claim; typecheck was RED→fixed; gates all green: typecheck 0, lint clean, 175 tests, build ✓). Prior: 5-2 → review (Run Console — Context-First Mode) -> ready-for-dev (create-story; FRONTEND-only). First Run Console story: reusable shell + context-first slice consuming 5-1 API. Decisions locked w/ user: project-skill attachment mocked via useProjectSkills seam (no backend model yet); build shared shell + status-indicator STUB, defer 3s polling/output→5-4 and skill-first→5-3. Adds location_dependent to FE Skill type; greenfield src/features/run/. No backend/migration/dep. Prior: Story 5-1 (Invocation API & Job Polling) -> done (code review). 3-layer adversarial (Blind/Edge/Auditor) fresh review of sonnet-4-6's impl: NO Critical/High. AC4 async-202 proxy + AC5 error map correct, all dispatch invariants preserved, error map complete (17/17 codes), scope honored, no migration (head=0012). 3 decisions resolved→patch (extract queue_invocation shared helper so /invoke can't drift from /invocations; InvokeRequest now subclasses InvocationRequest; extra='forbid' on both surfaces → unknown fields 422) + 3 patches (gate error-block on status=='failed' not error_code truthiness; AC3 test locks expires_s=86400; new /invocations create-202 shape test) = 6 applied & verified. Gates: ruff clean, 560 Docker tests pass (0 regressions, +2 net), no migration. Backend-only; baseline_commit 63efcd1. Prior 5-1 -> ready-for-dev (create-story); epic-5 -> in-progress (first story). EXTENSION story: AC1/2/3 already exist (Epic 3); NET-NEW = /api/v1/invoke proxy (async thin alias, 202+job_id) + error:{code,message} on failed-job poll. Decisions locked w/ user: proxy async-not-sync; output-only IP protection (RBAC→Epic 8). No migration. Prior: Story 3-7 (Location-Dependent Skill Fan-Out) -> done (code review). 3-layer adversarial (Blind/Edge/Auditor): all 6 ACs + 6 invariants + locked Celery-chord decision confirmed, but the suite MASKED a Critical defect. 3 decisions resolved (2 patch, 1 defer→Epic 8 RBAC) + 6 patches = 8 applied & verified. CRITICAL (Blind+Edge): chord body aggregate_results never fired when any child failed — run_skill re-raised, and Celery 5.4 runs the chord body only if every header task succeeds → parent stranded queued, no roll-up/audit (AC4/AC6); the _drive_fan_out test swallowed child exceptions, hiding it. Fixed: run_skill returns a failure-marker dict (Sentry-captured) for fan-out children instead of re-raising; new test drives the REAL task body (fresh-loop/engine thread) asserting return-not-raise. Other patches: rollup loop distinguishes completed/failed/cancelled/non-terminal (+FAN_OUT_CANCELLED/FAN_OUT_INCOMPLETE); parent started_at via mark_running; parent-audit double-write closed (we_transitioned gate); DISPATCH_FAILED net only fails still-queued children; location PII (pi_name/address) narrowed OUT of LLM context + code-stdin; single-location location∈study validated (LOCATION_STUDY_MISMATCH); AC5 cross-org test seeds a real org_other parent. Deferred (1, logged): skill scope enforcement → Epic 8 RBAC. Gates: ruff clean, migration 0012↔0011 round-trip clean, 537 Docker tests pass (0 regressions, +2 net). Backend-only; baseline_commit 74b296a. Prior 3-7 -> ready-for-dev (create-story). Deferral cleared (Epic 4 Locations landed); critical-path bridge to Epic 5 Story 5-4. Locked decision: Celery chord + aggregate_results. Migration 0012 (Skill.location_dependent + InvocationJob.parent_job_id/location_id/fan_out). Prior: Epic 4 retrospective -> done (doc: implementation-artifacts/epic-4-retro-2026-06-12.md). 4/4 stories delivered, 32 review patches, 522 backend + 154 web tests, 0 regressions. Decisions: (1) build Story 3-7 location fan-out BEFORE Epic 5 (critical path — unblocks 5-4 fan-out ACs, closes Epic 3 retro item 4); (2) add backend cascade-delete endpoint (retires non-atomic client cascade + 4-1 child-guard friction); (3) write Execution Engine Patterns doc as part of 3-7 (closes Epic 3 item 1); (4) epic-list milestone framing before Epic 5 kickoff (closes Epic 3 item 2). Prior: Story 4-4 (Hierarchy Navigation & Breadcrumb Context) -> done. Code review 2026-06-12: 3-layer adversarial (Blind/Edge/Auditor), all 7 ACs + both locked decisions + scope boundaries PASS. 2 decisions resolved (both → patches: ⌘K keyboard nav + project-filter-to-live-clients) + 8 review findings = 10 patches applied & verified: F1 (HIGH, 2 layers) ancestor-error no longer hides an existing leaf (context hooks expose leafError; routes gate not-found on it; ancestor errors → `…` crumb); deleted study/location removeQueries on singular ['study'|'location',id] (no stale-on-Back); fetchStudyLocations staleTime:0 (live cascade count, no 409); guarded delete-count .finally; EmptyDetail clears active-path store; Study/Location routes wait on loading ancestors (parity w/ Project route); leaf-name `|| '…'` fallback; dead SearchResultItem.parentId removed. Gates: typecheck 0, lint clean, 154 tests pass (AppBar palette-row button→option for combobox a11y), build ✓. 1 deferred (non-atomic client-side cascade → backend cascade endpoint out of scope, logged), 3 dismissed. NOTE: working tree still carries uncommitted 4.3+4.4 code (baseline_commit f90d9f5 = 4.2). Prior: 4-4 -> review (dev-story; frontend-only). URL-routed per entity (nested engagements/* Routes, tree never remounts), new Breadcrumb.tsx clickable trail (org layer hidden FR-ORG-06), per-id + ancestor-context hooks walking *_id parents for deep-link/refresh resolution (AC7) + 404 friendly state, per-entity document titles, navigate-on-delete (replaced sel clearing), extended the SINGLE AppBar CmdPalette with an Engagements section (clients always + cached projects via getQueriesData, no fan-out, FR-ORG-07 client-side mock). Preserved all 4.3 behaviors (collapsed-by-default + toggle-only expand, cache+sameList search, study-delete cascade R-F1/R-F2, full active-path store writes R-F5). Removed sel state + stale-selection effect + clearSelectionFor. Tests rewritten to routed model + breadcrumb depth/deep-link/AC6 ancestor-nav/AC4 collapse/AC5 Cmd-K/tab-active+title regression. Gates: typecheck 0, lint clean, 154 tests (+9, 0 regressions), build OK. NOTE: working tree still carries uncommitted 4.3 code (baseline_commit f90d9f5 = 4.2). Prior: 4-4 -> ready-for-dev (create-story).
project: Velara
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts/stories

development_status:

  # ─────────────────────────────────────────────────────────
  # Epic 1: Platform Foundation & Local Dev Environment
  # ─────────────────────────────────────────────────────────
  epic-1: done
  1-1-velara-api-project-scaffold: done
  1-2-velara-web-project-scaffold: done
  1-3-local-dev-environment-and-provider-abstractions: done   # NEW — story file created 2026-06-05; code review passed 2026-06-09
  1-4-dev-authentication-shim: done                               # story created 2026-06-09; code review passed 2026-06-09 (4 patches applied, 6 deferred)
  1-5-per-route-browser-tab-title: done   # NEW 2026-06-08; implemented 2026-06-09 — useDocumentTitle hook + static titles on all routes; code review passed 2026-06-09 (3 test-hardening patches applied, 5 deferred, all 5 ACs satisfied)
  1-6-apply-vitalief-brand-theme: done   # NEW 2026-06-09 (re-theme tokens to V3 brand; re-themes done 1-2); implemented 2026-06-09 — @theme token swap, brand-* rename, Open Sans/Poppins fonts; code review passed 2026-06-09 (2 patches applied — wordmark font-bold + dep version normalize; 1 deferred, all 5 ACs PASS)
  epic-1-retrospective: done

  # ─────────────────────────────────────────────────────────
  # Epic 2: Skill Registry & Lifecycle   (was Epic 3)
  # ─────────────────────────────────────────────────────────
  epic-2: done   # first story created 2026-06-09 (auto via create-story)
  2-1-skill-data-model-and-registry-api: done   # implemented 2026-06-09; code review 2026-06-09 (10 patches applied, 2 deferred); integration re-run 2026-06-09 — 131/131 pass (flush fix applied in 2-2)
  2-2-skill-metadata-tags-and-visibility-designations: done   # implemented 2026-06-09: tags (JSONB+GIN), paired_with (null), MISSING_DESCRIPTION, scope guard, metadata PATCH; code review 2026-06-09 (D1 resolved, 8 patches applied, 2 deferred, D2 dismissed); Docker re-run GREEN — 151 passed (131+20 new), ruff clean. Caught+fixed a TypeError→500 regression in the patch itself.
  2-3-paired-skill-derivation-lineage: done   # implemented 2026-06-09: /derive (client_facing child + derived_from), lineage on GET, version-publish→review_required flag (in-txn), client_ready guard, /acknowledge-parent-update; migration 0004. Code review 2026-06-10 PASSED (all 5 ACs satisfied): 2 patches applied (paired_with nulled on >1 child + new multi-child test; DerivedFrom.model_validate hardening), 3 deferred, 6 dismissed. 114 skills+unit tests pass, ruff clean
  2-4-skill-registry-ui-browse-and-detail: done   # story created 2026-06-10; implemented 2026-06-10; code review passed 2026-06-10 (all 6 ACs satisfied; 2 patches applied — SkillRow keyboard a11y + Paired chip glyph; 2 low findings deferred; 91/91 tests pass, typecheck+lint clean)
  2-5-skill-create-and-edit-ui: done   # code review 2026-06-10: 3 decisions resolved (scope/schema clear → inline-error+block; null-desc edit → keep blocking) + 9 patches applied (scope-null PATCH, empty-{} PATCH, ConfirmDialog pending-dismiss + focus-trap, VALIDATION_ERROR details mapping, edit-route test, double-submit guard, notice timer re-arm). Gates: typecheck/lint/build clean, 123/123 tests pass
  epic-2-retrospective: done

  # ─────────────────────────────────────────────────────────
  # Epic 3: Skill Execution Engine   (was Epic 4)
  # ─────────────────────────────────────────────────────────
  epic-3: done   # first story created 2026-06-10 (auto via create-story)
  3-1-async-job-infrastructure: done   # code review 2026-06-10: 2 decisions resolved (TRUNCATE trigger + enum validation → both patched in-scope) + 9 patches applied & verified — worker-Sentry init (AC6), terminal-state guard + UNIQUE(invocation_job_id), cancel row-lock + cancelled audit entry, fresh-session failure handler, presign-failure graceful degradation, org-scope WHERE-clause + comment fix. Gates: ruff clean, 220 Docker suite + 111 unit pass, migration round-trip + UNIQUE/TRUNCATE confirmed in PG. Deferred (2, logged): running-job reaper, ltree-path validation
  3-2-document-ingest-pipeline: done   # code review 2026-06-11: 1 decision resolved (zip-bomb cap → _MAX_EXTRACTED_CHARS 50MB + PARSE_TOO_LARGE) + 11 patches applied & verified — confirm terminal/status guard (dead _guard_not_terminal), atomic dispatch (no confirmed-orphan), ClientError-code 404 detection, narrowed get_range fallback, zero-byte rejection, parse success-commit terminal guard, single-pass parser metadata (extract_document/ParsedDocument), threadpool-wrapped broker publish + build_context_input get, removed unused output_storage param. Gates: ruff clean, 273 Docker suite pass (+4 new tests), migration round-trip verified. Deferred (1, logged): bucket-default SSE → Epic 7 IaC
  3-3-prompt-based-skill-execution: done   # code review 2026-06-11: 3-layer adversarial review (Blind/Edge/Auditor), all 6 ACs satisfied (AC3 IP-protection + AC4 single-retry-layer confirmed). 1 decision dismissed (queue-then-retire race accepted), 2 decisions → patches, 8 patches applied & verified: atomic dispatch (no stranded queued job — mark_failed on broker failure), job-None guard in execution block, synchronous SKILL_NO_CURRENT_VERSION reject, 413→PROMPT_EXECUTION_ERROR mapping, ANTHROPIC_* Field validation, narrowed runtime_type except, request-time file_ref validation (assert_file_ref_ready), stop_reason surfaced in result_metadata. Gates: 313 Docker tests pass (+6 new), ruff clean, migration 0008 round-trip verified. Deferred (2, logged): unbounded context size, dev empty-key late-401.
  3-4-code-based-skill-execution: done   # 2026-06-11: Phase-1 subprocess sandbox; code routing in execution_service; EXECUTION_TIMEOUT; Celery limits 1260/1320; all 6 ACs. Code review applied 11 patches (trusted inherited-pipe control channel defeating forged success/network signals, capped streaming stdout read preventing worker OOM, bounded reaps, RLIMIT_NOFILE 512, EXECUTION_TIMEOUT_S le=1200, SoftTimeLimitExceeded→EXECUTION_TIMEOUT, genuine end-to-end AC2/AC3/AC6 integration tests). 344 Docker pass, ruff clean, no new dep/migration
  3-5-hybrid-skill-execution: done   # 2026-06-11: implemented (hybrid artifact parse, ToolServer persistent subprocess, manual Claude tool-use loop, agentic result_metadata, IP/PHI discipline). Code review 2026-06-11: 3-layer adversarial (Blind/Edge/Auditor), all 6 ACs satisfied. 3 decisions resolved (empty/max_tokens→fail HYBRID_EMPTY_OUTPUT; tool allow-list enforced; required-key Dev Note relaxed) + 10 patches applied & verified (empty-output guard, allow-list dispatch, empty tool_results 400 guard, per-call timeout killpg, HYBRID_TOOL_CALL_TIMEOUT_S le+budget validator, duplicate-tool-name reject, _json_safe allow_nan=False, dead try/except removed, tool_turns round-trip count, free_inputs is-not-None+JSON). 389 Docker pass (+6 new tests), ruff clean, no new dep/migration. Deferred (3, logged): tool-result size cap, _send_frame write timeout, 3.4-bundled-in-diff commit hygiene
  3-6-branded-output-generation: done   # code review 2026-06-11: 3-layer adversarial (Blind/Edge/Auditor), all 6 ACs + R1/R2/R3/R5 satisfied. 2 decisions resolved → patch + 6 patches = 8 applied & verified: PDF XML-escape (saxutils) preventing markup corruption on ordinary text, get_job never-500 guard (skip non-dict/missing-key output_files entries + type-guarded OutputFileRef), brand-asset fail-closed-in-prod (_opt required=True raises BRAND_ASSET_MISSING for empty Open Sans/Poppins/logo keys when not dev), logo image rendered into PDF/DOCX/PPTX headers (best-effort), XLSX Open Sans (Calibri superseded), render SoftTimeLimitExceeded re-raise → EXECUTION_TIMEOUT, XLSX real column width + blank-row skip + _load_asset_optional exc_info, R4/AC1/AC5 e2e PDF + brand-asset-missing integration tests. Incidental: PPTX empty-slide_layouts guard. 5 deferred (lru_cache poison, PPTX template fragility, hex-token validation, output size cap, edit-surface output_format validation), 10 dismissed. POST-REVIEW (live ARROW-protocol testing surfaced flat Markdown rendering): swapped the text-dumper for markdown-it-py 3.0.0 token AST → real PDF/DOCX structure (styled headings, bold/italic, bullet/ordered lists, GFM tables w/ teal header+grid+zebra, HR dividers); XML-safety preserved (escape-before-markup; code/html_inline literal). XLSX/PPTX unchanged. 446 Docker pass, ruff clean. Deps: reportlab==4.5.1, python-pptx==1.0.2, openpyxl==3.1.5, markdown-it-py==3.0.0.
  3-7-location-dependent-skill-fan-out: done   # 2026-06-12: code review PASSED after 8 patches (1 Critical: chord body never fired on child failure — run_skill now returns-not-raises for fan-out children). 537 Docker tests pass, ruff clean, migration round-trip clean. 1 deferred (skill scope → Epic 8). story created (create-story). DEFERRAL CLEARED — Epic 4 (Hierarchy + Location entities) done. Critical-path bridge to Epic 5 Story 5-4. Backend-only. Locked decision: Celery chord + aggregate_results callback (first chord in codebase; result-backend required; service-level _drive_execution testing). Migration 0012 adds Skill.location_dependent + InvocationJob.parent_job_id/location_id/fan_out. Audit fan_out/invocation_id columns already exist (prepositioned in 3-1). Includes Task 9: write Execution Engine Patterns doc (closes Epic 3 retro action item 1).
  3-8-external-api-credential-and-connector-framework: done   # story created 2026-06-12 (create-story; 3-7 deferred-skipped per Epic-4 dependency). 3 design decisions locked: framework+1 reference connector each (no SharePoint/Slack), code/hybrid env-var injection via new sandbox extra_env (prompt fetch+validate only), new Skill.requires JSONB column (mig 0010). SecretsProvider already built (1.3) — consumed not rebuilt; connectors greenfield (mirror document_parser._PARSERS registry)
  epic-3-retrospective: done   # 2026-06-12: retrospective completed; doc saved to implementation-artifacts/epic-3-retro-2026-06-12.md

  # ─────────────────────────────────────────────────────────
  # Epic 4: Engagement Hierarchy Management   (was Epic 2)
  # ─────────────────────────────────────────────────────────
  epic-4: done   # first story created 2026-06-12 (auto via create-story)
  4-1-hierarchy-data-model-and-api: done   # story created 2026-06-12; implemented 2026-06-12 — 5 hierarchy tables + ltree paths, full CRUD API (clients/projects/studies/locations), 409 child-guard, org-layer hidden; migration 0011 raw DDL with native ltree type. Code review 2026-06-12: 9 patches applied & verified (422-on-non-string, ltree-safe org_segment, org-upsert race, delete FK→409, cross-org→404, dead-code removal, +7 tests); 522 Docker pass, ruff clean
  4-2-engagements-screen-client-and-project-management: done   # implemented 2026-06-12: two-panel EngagementsScreen (client tree + entity detail), ClientNode expandable nodes, EntityDetail (client shows Projects card + Add Project; project shows placeholder Studies/Skills), EntityModal (add/edit client or project), client-side search via projectsCache state pattern (avoids hook-in-loop). Error envelope helpers inline (SkillForm pattern). Route wired internal.tsx engagements→EngagementsScreen. 10 new tests, 133 total pass, 0 TS errors, build ✓
  4-3-study-and-location-management: done   # code review 2026-06-12: 3-layer adversarial (Blind/Edge/Auditor), all 7 ACs PASS, only 6 declared files changed, no scope violations. 1 decision resolved + 6 patches applied & VERIFIED, 2 dismissed. F1 (High, AC6) study-delete now fetches the LIVE location list (queryClient.fetchQuery→listLocations) on dialog-open + at confirm — fixing the hole where a study selected-but-never-expanded reported 0 locations (empty expansion-gated cache → no warning, no cascade, 409 on study delete); F2 cascade idempotent (404→success on retry); F3 stale-selection clear extended to project/study/location; F5 selectStudy/selectLocation set the full active-id chain (4.4); F6 honest always-on search hint; F9 aria-controls re-added to tree rows. Dismissed: address/city/pi_name clear-no-op (documented), EntityModal missing key (latent). Gates: typecheck 0, lint clean, 145 tests (0 regressions), build ✓. — implemented 2026-06-12: extended the as-built 4.2 EngagementsScreen in place to 4 levels — new ProjectNode/StudyNode tree, StudyDetail/LocationDetail, shared ChildListCard/DetailActions, EntityModal study+location branches (postal_code required), search over studies/locations, first delete-with-confirm flow (ConfirmDialog; delete-children-then-study to honor AC6 copy since API 409s not cascades), entity-aware friendlyError. Fixed a latent 4.2 expand bug (double setExpanded cancelled out) + a cache-effect render loop (depend on stable cb + sameList id-guard). All 7 ACs met; typecheck 0 errors, lint clean, 145 tests (+8, 0 regressions), build ✓
  4-4-hierarchy-navigation-and-breadcrumb-context: done   # code review 2026-06-12 PASSED (all 7 ACs + Decisions A/B + scope boundaries). 2 decisions → patches + 8 findings = 10 patches applied & verified (HIGH ancestor-error-hides-leaf fix via leafError gating; singular-cache removeQueries; live cascade count staleTime:0; guarded delete-count finally; EmptyDetail clears store; Study/Location loading-ancestor parity; leaf-name fallback; dead parentId removed; ⌘K keyboard nav + live-client project filter). Gates: typecheck 0, lint clean, 154 tests pass, build ✓. 1 deferred (non-atomic cascade), 3 dismissed. — story created 2026-06-12 (create-story; frontend-only). Refactors the as-built 4.3 EngagementsScreen from selection-state to URL-routed per entity (nested /internal/engagements/{clients,projects,studies,locations}/:id), adds a clickable breadcrumb trail (org layer hidden per FR-ORG-06), deep-link/refresh ancestor resolution via 4.1 GET-by-id, and extends the EXISTING global ⌘K palette in AppBar.tsx with an Engagements (clients/projects) section. 2 architectural decisions locked w/ user: (A) URL routes per entity; (B) extend the single AppBar ⌘K palette. Exhaustive as-built analysis: line-referenced every sel→route seam in the ~1672-line screen, confirmed tree already collapsed-by-default (AC4), reused the existing AppBar ⌘K handler/CmdPalette (no dupe), verified GET-by-id + *_NOT_FOUND codes wired into friendlyError + the usePageTitle(name,type) seam. Scoped out: backend, server-side search, new CRUD, RBAC, skill attachment, studies/locations in ⌘K (optional)
  epic-4-retrospective: done   # 2026-06-12: retrospective completed; doc saved to implementation-artifacts/epic-4-retro-2026-06-12.md. Decisions: build Story 3-7 (fan-out) before Epic 5 (critical path, unblocks 5-4); add backend cascade-delete endpoint; write Execution Engine Patterns doc with 3-7; epic-list milestone framing before Epic 5 kickoff.

  # ─────────────────────────────────────────────────────────
  # Epic 5: Run Console & Invocation UX
  # ─────────────────────────────────────────────────────────
  epic-5: done   # first story created 2026-06-12 (auto via create-story)
  5-1-invocation-api-and-job-polling: done   # code review 2026-06-13 PASSED (no Critical/High): 6 patches applied & verified — extracted shared queue_invocation() helper (/invoke can't drift from /invocations), InvokeRequest subclasses InvocationRequest + extra='forbid' (unknown fields→422), error-block gated on status=='failed', AC3 locks expires_s=86400, new /invocations create-202 shape test. 560 Docker tests pass (0 regressions), ruff clean, no migration (head=0012). — implemented 2026-06-13: Claude proxy POST /api/v1/invoke (async thin alias), ErrorInfo{code,message} on GET /jobs failed-job poll, AC1/AC2/AC3 envelope regression tests. Backend-only. Baseline commit: 63efcd1.
  5-2-run-console-context-first-mode: done   # code review 2026-06-12: 3-layer adversarial (Blind/Edge/Auditor), all 5 ACs satisfied (AC2 pre-select gap closed), scope discipline clean (no deferred/backend work). 2 decisions resolved (1→patch port-the-prototype, 1→accept global useProjectSkills mock as-is) + 9 patches applied & verified, 9 dismissed. KEY: design prototype (design/overrides.jsx) DID exist — the dev "not found" claim was false (corrected); reconciled as-built against it. Patches: forbidden green-*/amber-*/gray-*/red-* + undefined brand-200 → st-*/brand-*/danger tokens (the 1-6 silent-drift trap); typecheck was actually RED (3 TS6133) → fixed; canRun now gates on contextNotFound/contextLoading/locationDependentNoStudy + resolved skill object; AC2 pre-select fallback message; activeJobId reset-on-mount (no stale "Queued"); handleRun/button gate aligned; origin route param validated (reject non project|study); breadcrumb client-id guard (no /clients/undefined); staleTime decision documented. Gates ALL green after patches: typecheck 0 (was 3), lint clean, 175 tests pass (0 regressions), build ✓. — implemented 2026-06-13 (FRONTEND-only). Run Console shell + context-first slice: route /internal/engagements/run/:origin/:originId, locked context panel (Client/Project[/Study]), project-attached skill picker (useProjectSkills MOCK SEAM), location selector (single + run-for-all) for location-dependent skills in Study scope, POST /api/v1/invocations/{skill_id} mutation, job-status indicator STUB (reads useRunStore.activeJobId, shows "Queued"), back-to-origin via entityPath(). Added location_dependent to FE Skill type. Gates: typecheck 0, lint clean, 175 tests (+21 new, 0 regressions from 154 baseline), build ✓.
  5-3-run-console-skill-first-mode: done   # code review 2026-06-13: 3-layer adversarial (Blind/Edge/Auditor), all 5 ACs + #1 contract truth (no client_id/project_id/scope; {}/{location_id}/{study_id,fan_out} payloads) verified. 4 patches applied & verified: (1) retired/no-current-version skill is now pre-flight-gated in canRun with an inline reason under the locked card (was reactively 422-ing); (2) LD skill + 0-location study shows an actionable dead-end message; (3) decorative ▶ glyph wrapped aria-hidden so Run buttons' accessible name is "Run" (SkillDetail + EngagementsScreen ×2; test matcher updated); (4) tautological "Engagements tab active" RunConsole test renamed to what it actually proves + a genuine toHaveClass('font-bold') assertion added in internal.test.tsx. 4 deferred (logged in deferred-work.md): inputs/file_ref_ids unsent (story DEFERS); runMode written only on submit success (5-4 owns store reshape); friendlyInvocationError non-string message coercion (pre-existing 5-2 pattern); EngagementsScreen +95 lines = Story 5-2 context-first wiring captured by working-tree diff, not 5-3. 7 dismissed (incl. useProjectSkills ignoring projectId = documented MOCK SEAM, INV-09 makes project & study skill lists identical by design; "missing" context-first double-submit guard = false asymmetry, both canRun gates already include !isPending). Gates: typecheck 0, lint clean, 204 web tests (+1 from 203, 0 regressions), build ✓. # dev-story 2026-06-12: implemented FRONTEND-only, extended 5-2 shell in place (RunShell, no fork). NEW skills/:skillId/run route (Skills tab stays active); SkillDetail Run button wired live; LockedSkillCard + HierarchyPickerCascade (editable Client→Project→Study, ports design/internal3.jsx); reused 5-2 LocationSelector/useCreateInvocation/JobStatusPanel stub; setRunMode('skill-first') on submit; contract-correct payloads ({} non-LD, {location_id} LD-single, {study_id,fan_out} LD-all — NO client_id/project_id/scope). Route decision: chose :skillId/run over recommended run/:skillId (nests naturally, both keep Skills tab). Gates: typecheck 0, lint clean, 203 web tests (+28 from 175 baseline, 0 regressions), build ✓. No backend/migration/dep. baseline_commit a111997. story created 2026-06-12 (create-story; FRONTEND-only). EXTENDS the as-built 5-2 RunConsole in place (don't rebuild): adds skill-first mode = locked green skill card (useSkill) + editable Client→Project→Study cascade picker (ported from design/internal3.jsx HierarchyPicker; all 4 list hooks already exist). Wires the disabled SkillDetail.tsx:210-221 "Run · Coming soon" button live → new skills/run/:skillId route (keeps Skills tab via useActiveTab segment[2]). Makes RunConsole runMode-aware (5-2 hardcodes setRunMode('context-first') at :388). Reuses LocationSelector/JobStatusPanel stub/useCreateInvocation. CRITICAL contract truth surfaced via backend research: POST body has NO client_id/project_id (extra=forbid→422); for non-LD skills the context selection is INVISIBLE to backend (payload {}), only study_id/location_id reach it for LD skills. 1 open decision: route placement (recommended dedicated skills/run/:skillId). No backend/migration/dep.
  5-4-job-status-polling-and-output-display: done   # code review 2026-06-15: 3-layer adversarial (Blind/Edge/Auditor), all 8 ACs verified. 2 decisions resolved + 8 patches applied & verified: (F2) parent_job_id IS NULL filter added to list_jobs + child-exclusion test; (F4) "Open in Run Console →" link in JobDetailPanel navigates to skills/:skillId/run; (F1) JobSummary.skill_name set via model_copy not post-validate mutation; (F3) handleClose clears activeJobId, handleSelect toggle-close restores prior activeJobId; (F6) fan-out in-flight shows "dispatching locations…" when children null; (F7+F8) useJob stops polling on error + JobStatusPanel shows error state with Dismiss button; (F10) "(link unavailable)" fallback added to JobDetailPanel fan-out child rows. 5 dismissed, 4 deferred. Gates: typecheck 0, lint clean, ruff clean, 224 web tests (0 regressions), build ✓. Backend Docker tests not run (Docker unavailable). dev-story 2026-06-15: FULL-STACK implemented. BE: GET /api/v1/jobs (first paginated endpoint; JobSummary/JobListData/PageMeta; outer-join Skill skill_name; 9 new integration tests; ruff clean). FE: useJob 3s polling (first refetchInterval in app, stops at terminal); live JobStatusPanel (output/failed/fan-out); useRunStore persist (first localStorage); Jobs History screen + /internal/jobs route + jobs NavTab; friendlyJobError map; 20 new tests (+0 regressions, 224 total); typecheck 0, lint clean, build ✓. Backend Docker tests written but not run (Docker unavailable). FULL-STACK implemented. BE: GET /api/v1/jobs (first paginated endpoint; JobSummary/JobListData/PageMeta; outer-join Skill skill_name; 9 new integration tests; ruff clean). FE: useJob 3s polling (first refetchInterval in app, stops at terminal); live JobStatusPanel (output/failed/fan-out); useRunStore persist (first localStorage); Jobs History screen + /internal/jobs route + jobs NavTab; friendlyJobError map; 20 new tests (+0 regressions, 224 total); typecheck 0, lint clean, build ✓. Backend Docker tests written but not run (Docker unavailable). story created 2026-06-15 (create-story). SCOPE DECISION LOCKED w/ user → FULL-STACK (not FE-only): the Jobs History screen (1 of the epic's 3 deliverables) needs a NEW GET /api/v1/jobs list endpoint that does NOT exist (jobs router has only GET /{job_id} + POST /{job_id}/cancel — verified via 2 backend subagents). 5-4 builds: (BE) GET /api/v1/jobs org-scoped/paginated/newest-first, light JobSummary, FIRST PageMeta consumer in codebase (recommend ResponseEnvelope[JobListData{items,page}] — don't touch shared envelope), outer-join Skill for skill_name (FK no CASCADE), mirror list_children+list_skills query, no migration; (FE) replace JobStatusPanel STUB (RunConsole.tsx:783-807) w/ live useJob 3s poll (FIRST refetchInterval — stop on terminal via fn form), output display (presigned 24h S3 URLs may be null, NO inline output text — files only), fan-out X-of-N (count children[] client-side, net-new UI) + per-location grouped results, Jobs History screen + /internal/jobs route + jobs NavTab, useRunStore zustand persist→localStorage (FIRST persist, partialize activeJobId), friendlyJobError map (JOB_*/execution codes unmapped today). Reuses JobStatusBadge/JOB_STATUS_META (already V3-correct). 3 open impl decisions (PageMeta carrier, skill_name join, History→console "view job"/Try-again + reconcile clear-on-mount w/ AC8 resume). Baselines: 24 test_jobs.py + 204 web tests.
  5-5-rest-api-documentation-and-openapi-spec: done   # code review 2026-06-25: AC1/2/3 SATISFIED + 3 patches applied/verified (8 tests pass, ruff clean), but AC4 UNMET — docs/api-spec.json (+export script +test) are UNTRACKED so the committed-spec + CI staleness gate are inert (reproduced: stale spec passed the gate). 2 deferred (commit→dev step; unpinned CI install pre-existing), 5 dismissed. → in-progress until the 3 5-5 files are committed, then done. dev-story 2026-06-25: IMPLEMENTED → review. Relocated docs/spec to /api/v1/{docs,openapi.json,redoc}+oauth2-redirect (main.py); scripts/export_openapi.py (deterministic, byte-identical, no DB/Redis — verified w/ bogus URLs); committed docs/api-spec.json (OpenAPI 3.1.0, 27 paths NOT 28 post-5-4); new 'openapi' CI job (export+git diff --exit-code, staleness gate proven); 7 new tests (AC1 Swagger@/api/v1/docs, AC2 spec@/api/v1/openapi.json + defaults 404, AC3 InvokeRequest/InvocationRequest=5 safe fields+additionalProperties:false+no internals lock, ReDoc). AC3 = verification only, did NOT add context (contract untouched); tightened 2 phantom-context docstrings. ReDoc KEPT (decision). Gates: ruff clean on my files; container full-suite 574 passed/7 net-new; 0 regressions (the 3 test_ingest failures are PRE-EXISTING env — presign upload→localhost:9000 refused inside container, fail identically in baked baseline image). 2 pre-existing UP038 in output_service.py = uncommitted non-5-5 tree changes, NOT touched. No migration/dep/FE. baseline_commit a111997. story created 2026-06-25 (create-story; BACKEND-only, no migration/dep/FE). FastAPI already auto-generates a full OpenAPI 3.1 spec (28 paths) — work = config + tiny export script + 1 CI job + tests. Move docs/spec to /api/v1/docs + /api/v1/openapi.json in main.py:43-48 FastAPI() (+redoc_url +swagger_ui_oauth2_redirect_url GOTCHA: it does NOT follow docs_url). scripts/export_openapi.py (json.dumps sort_keys=True → deterministic, verified byte-identical; needs NO Postgres/Redis — lazy engine + Sentry no-op). Commit velara-api/docs/api-spec.json + new 'openapi' CI job (export + git diff --exit-code). TWO epic-AC corrections verified vs source: (1) AC3 "context+inputs only" → there is NO context field; real InvokeRequest = file_ref_ids/inputs/location_id/study_id/fan_out + additionalProperties:false (extra=forbid) — AC3 is a VERIFICATION TEST not a schema change; do NOT add context or touch the 5-1..5-4 invocation contract; (2) AC4 "hub repo docs/" → hub root isn't a git repo; real home = velara-api/docs/ + velara-api/.github/workflows/ci.yml. Tests: spec@v1 path/valid 3.x/default 404/Swagger HTML/invoke-no-internals lock (no DB → omit skipif guard). Baseline ~570 BE tests.
  5-6-skills-list-api-pagination: done   # code-review 2026-06-25: review → DONE. 4 patches applied (D1 created_at tiebreaker → id.desc() on BOTH list_skills+list_jobs; D2 page upper-bound le=100_000 on BOTH skills.py+jobs.py +test; P1 test_artifact_key_not_exposed false-pass fixed to data["items"]; P2 filter-applies-to-total hardened) + ruff E501 wrap. 1 deferred (COUNT/ROWS concurrency → deferred-work.md), 1 dismissed (status/fastapi.status shadow). Gates: ruff clean, 101/101 test_skills+test_jobs, 585/588 full (3 pre-existing ingest env failures). Also touched jobs.py + job_service.py. | dev-story 2026-06-25: IMPLEMENTED → review. SkillListData{items,page} schema; list_skills→tuple[rows,total] w/ func.count()+LIMIT/OFFSET; handler Annotated[int,Query(ge=1)] page/per_page + ResponseEnvelope[SkillListData]; 5 existing list/tag assertions updated to ["data"]["items"]; 10 new pagination tests. Gates: ruff clean, 68/68 test_skills (+10 net), 585/588 total (3 pre-existing ingest env failures). No migration/dep/FE. story created 2026-06-25 (create-story; BACKEND-only, no migration/dep/FE). MIRROR the 5-4 jobs precedent EXACTLY (JobListData docstring literally names 5-6 as next adopter): new SkillListData{items:list[SkillRead]; page:PageMeta} inside ResponseEnvelope; list_skills(skill_service.py:341-361) → tuple[rows,total] w/ func.count()(same filters) + .limit(per_page).offset((page-1)*per_page); handler skills.py:85-108 adds page Annotated[int,Query(ge=1)]=1 / per_page Query(ge=1,le=200)=50, keeps status:LifecycleState(Literal→auto-422)/tag. PRESERVE org-scope + status/tag filters (incl empty-tag guard :354-358) + created_at desc + SkillRead shape. ⚠️ BREAKING response-shape change (intended): data array → {items,page}; the ONE FE consumer skills.ts listSkills:4-6 (response.data.data as Skill[]) is adapted by STORY 5-7 (builds after 5-6) — do NOT preserve old array, do NOT touch velara-web. "backwards-compatible" = param defaults only, NOT shape. list_skills has ONE backend caller (skills.py:99). Tests: UPDATE 5 existing list/tag assertions (test_skills.py:236,247,405,603,624) to data["items"] + add pagination tests (limit/offset/total/out-of-range-empty/422/filter-applies-to-count). Baseline 58 test_skills / ~570 total. Root cause (correct-course 2026-06-12): unbounded list_skills slow at ~3,485 rows.
  5-7-skill-registry-ui-pagination: done   # code review 2026-06-26: 3-layer adversarial (Blind/Edge/Auditor), scoped to the 6-file 5.7 File List (working tree also held uncommitted 5-4 WIP — excluded by user). All 4 ACs + the #1 regression fix verified (listSkills unwraps .items, keeps Skill[], so ⌘K + useProjectSkills + AppBar are preserved). 5 PATCHES applied & verified: (1) MEDIUM regression introduced by this migration — useCreateSkill/useUpdateSkill/useTransitionLifecycle now ALSO invalidate ['skills-page'] (they invalidated only ['skills'], but the registry moved to the paged cache → stale rows/total ~30s after a create/transition); (2) listSkills unwrap hardened to response.data.data?.items ?? []; (3) empty-trailing-page useEffect clamps to Math.ceil(total/per_page) instead of decrement-by-one (avoids N-1 wasted refetches on a multi-page shrink); (4) "Showing X–Y of N" indicator now reports the honest page-local count when Type/Vis/search are active (was claiming the full server range); (5) USER-RAISED bug — inline registry search is current-page-only, misleading now that the registry paginates. Verified against real source (velara-api/app/api/v1/skills.py:88-95 + app/services/skill_service.py:341-362) that NO free-text param exists (only page/per_page/status/tag), so server-side search REQUIRES a backend change (out of this FE-only story; ?q= was the rejected scope-creep option). Fixed the honesty in-scope (placeholder → "Filter this page…", aria-label, + ⌘K whole-catalog hint on multi-page catalogs) and LOGGED the real server-side ?q= fix as a HIGH backend follow-up in deferred-work.md. 1 DEFERRED (internal.test.tsx carries 5-4 useJob/useRunStore mocks + /internal/jobs tests from the mixed working tree — resolves when 5-4 is committed). 8 DISMISSED (metaPage-vs-page desync + per_page echo = self-masking: no placeholderData → the total>0 control block unmounts during each page fetch; data?.page.total defensive-only; Next/Prev tests assert the request side = correct mocked-hook boundary; etc.). Gates: typecheck 0, lint clean, 245 web pass (+3 from 242, 0 regressions), build ✓. Findings in story Review Findings + deferred-work.md. # dev-story 2026-06-26: IMPLEMENTED → review. FE-only. Fixed the 5-6 listSkills runtime break (unwrap .items, per_page:200, keeps Skill[] for the 3 whole-catalog consumers — useSkills/useProjectSkills/AppBar untouched); added SkillListData/PageMeta/listSkillsPage + useSkillsPage (mirror 5-4 jobs); refactored SkillRegistry to server-side pagination (?status= from State filter + page-reset to 1; Type/Vis/inline-search = client-side-within-page per locked decision; first real Prev/Next + "Showing X–Y of N" in the app, a11y aria-label/disabled/focus-visible; distinguished empty-states; indicator/bounds read off server-echoed PageMeta). ⌘K stays whole-catalog (≤200 cap documented). Tests: rewrote SkillRegistry.test (12→25, mock useSkillsPage) + NEW src/api/skills.test.ts (5; listSkills unwrap/per_page200/filter-forward + listSkillsPage envelope/passthrough) + added useSkillsPage to internal.test mock. Gates: typecheck 0, lint clean (0/0), 242 web pass (+18 from 224 baseline, 0 regressions), build ✓. No backend/migration/dep. baseline_commit a111997. story created 2026-06-25 (create-story; FRONTEND-only, no backend/migration/dep; consumes 5-6 contract). MIRROR the 5-4 FE pagination precedent (api/jobs.ts PageMeta/JobListData/listJobs + useJobs + JobsHistory — but JobsHistory has NO page controls, 5-7 adds the first). 2 in-story decisions LOCKED w/ user: (1) affordance = Prev/Next + "Showing X–Y of N" (not load-more/infinite); (2) ⌘K palette stays whole-catalog via its own useSkills() path, only the registry TABLE paginates, inline table search = current-page-only (documented limitation; backend has NO free-text search). ⚠️ 5-6 ALREADY BROKE the FE: listSkills(skills.ts:4-6) reads response.data.data as Skill[] but data is now {items,page} → all 3 useSkills() consumers broken (SkillRegistry:102, useProjectSkills:13→RunConsole, AppBar:41→⌘K). FIX: keep useSkills()/listSkills returning Skill[] (unwrap .items, per_page:200 — note ≤200 whole-catalog cap) so RunConsole+⌘K unbroken; add NEW useSkillsPage→{items,page} for the registry only. Server filters: ONLY ?status= (State; resets to page1) + ?tag=(no UI); Type/Visibility/search stay client-side-within-page (no backend param — documented). Tests: update SkillRegistry.test (mock useSkillsPage) + prove 3 consumers still get arrays. Baseline ~234 web tests. NOTE: 5-5 (OpenAPI) still in-progress.
  5-8-skills-free-text-search-api: done   # code-review 2026-06-26: review → DONE. 3-layer adversarial; production faithful to all 5 ACs. 2 decisions + 6 patches applied: escape LIKE metacharacters %/_/\ + escape="\\" (raw interpolation broke the "substring" contract — q="100%"/q="%"); q.strip() in pattern (whitespace was literal); Query(max_length=255) → 422; fixed case-insensitive tautology test (asserts cased name); fixed absent-unchanged self-comparison test (seeds 2 disjoint skills); real NULL-desc coverage via direct-engine seed (create API forbids NULL desc → MISSING_DESCRIPTION, so AC1 NULL path was unreachable via API). +4 tests. Gates: ruff clean, 84/84 test_skills (+4, 0 regressions). 1 deferred (seq-scan/no trigram index → deferred-work.md, spec-sanctioned at ~3,485 rows), 1 dismissed (repeated ?q= last-wins). | dev-story 2026-06-26: BACKEND-only. Added ?q= ilike filter (or_ on name+description) to list_skills service + handler. 11 new test_search_* integration tests. ruff clean, 588 Docker pass (+12 new, 0 regressions, 3 pre-existing ingest env failures). No migration/dep/FE. story created 2026-06-26 (retro action item 1; BACKEND-only). Adds ?q= ilike filter on name+description to GET /api/v1/skills. Non-breaking additive param. Composes with ?status=/?tag=/?page=/?per_page=. Closes deferred-work.md HIGH item. Must be done before 5-9.
  5-9-skill-registry-search-wired: done   # code review 2026-06-26: 3-layer adversarial (Blind/Edge/Auditor), cleanly scoped to the 5-file 5.9 File List. All 6 ACs verified; whole-catalog useSkills/listSkills/AppBar ⌘K/useProjectSkills confirmed untouched. ROOT CAUSE both layers converged on: pageHasFilters overloaded "server-side search active" with "client-side Type/Visibility filter active" (identical pre-5-9 when search was client-side; not anymore). 3 PATCHES applied & verified: (1) MEDIUM — split pageHasFilters into searchActive (search.trim()!=='') + pageLocalFilters (type/vis); empty-state ternary now: search→"No skills match your search.", type/vis→"No skills on this page match your filters.", state→"No skills match the selected state.", else→"No skills registered yet." (+regression test for the type/vis-empties-page path); (2) the "Showing N of M on this page (page X, N total)" indicator wording is now gated on pageLocalFilters only — a clean server-side search shows the honest "Showing X–Y of N" (search is reflected in the server total); (3) debounce-test timer setup: investigated removing shouldAdvanceTime (Blind flagged it ambiguous) — it's REQUIRED so user-event's keystroke async resolves under fake timers (removing it timed out 5 tests), so KEPT shouldAdvanceTime:true and documented why; the 300ms-gate test still asserts an explicit advanceTimersByTime(300) boundary. 6 DISMISSED (immediate page-reset vs debounced-q intermediate {page:1,q:stale} request = inherent to the spec-mandated immediate-reset-+-debounced-query pattern that deliberately avoids the "Page 3 of 0" flash, staleTime caches it; the weak coalescing test is backstopped by the passing 300ms-gate test; "Axios drops undefined params" verified true [no custom paramsSerializer in client.ts; existing status param relies on identical behavior]; mount setPage(1) is a React-bailout no-op; useDebounce cleanup verified). Gates: typecheck 0, lint clean, 252 web tests (+1 from 251, 0 regressions), build ✓. CLOSES the HIGH ?q= follow-up logged in the 5-7 review (5-8 backend done + 5-9 FE done). Findings in story Review Findings. # dev-story 2026-06-26: FRONTEND-only. Wired SkillRegistry search box to server-side GET /api/v1/skills?q= (5-8 backend); added useDebounce hook (300ms); page-reset-on-search; removed client-side name/description/tag filter (filtered useMemo now Type+Visibility only); removed ⌘K honesty hint; updated placeholder/aria-label/empty-state copy ("No skills match your search."). ⌘K + useSkills/useProjectSkills whole-catalog path untouched. Consumer audit done (useSkillsPage = 1 caller). 4-file File List + new useDebounce.test. Gates: typecheck 0, lint clean, 251 web tests (+6 from 245 baseline, 0 regressions), build ✓. No backend/migration/dep. baseline_commit a111997. story created 2026-06-26 (retro action item 2; FRONTEND-only). Wires SkillRegistry.tsx search box to ?q= server-side param; adds 300ms debounce; removes client-side name/description filter; removes honesty hint; updates copy. Depends on 5-8.
  epic-5-retrospective: done   # 2026-06-26: retrospective completed; doc saved to implementation-artifacts/epic-5-retro-2026-06-26.md. 3 systemic failures: (1) no consumer audit before 5-6 listSkills shape break → ⌘K + Run Console regressed; (2) deferred-work.md HIGH items not escalated to PM → registry search shipped as current-page-only without a product decision; (3) hybrid runtime designed without client skill in hand → 6-story 5.5 rework. Action items: 2 new search stories (BE+FE ?q=), consumer-audit checklist, workspace temp-dir architecture, HIGH-item PM escalation process.

  # ─────────────────────────────────────────────────────────
  # Epic 5.5: Code-Driven Hybrid Skills & Canonical Output Contract
  # ─────────────────────────────────────────────────────────
  epic-5.5: done   # CLOSED 2026-06-26 — all 6 stories done (5.5.1 register manifest; 5.5.2 schema-versioned output; 5.5.3 venv executor; 5.5.4 raw-file input; 5.5.5 multi-artifact+blocked; 5.5.6 HEADLINE GATE PASSED). ARROW NCT03037385 → completed, schema_version 0.3.0, 10 arms/16 encounters/25 activities/102 instances. Epic 5.5 → done. Original epic note for reference: NEW 2026-06-18 via correct-course (sprint-change-proposal-2026-06-18.md). Code-Driven Hybrid Skills & Canonical Output Contract. Trigger: client delivered first foundation skill (velara-protocol-extractor) — a code-driven hybrid (code drives, LLM is callee; multi-file; deps pdfplumber/PyMuPDF/openpyxl/anthropic/pydantic; needs network egress + raw PDF for vision pass; emits schema-versioned canonical JSON + Excel + QA envelope status ok|blocked) that fits NONE of prompt/code/hybrid as built (our hybrid = platform-drives-LLM-loop + stdlib-only network-BLOCKED ToolServer tools). NO 4th runtime — widen hybrid with a 2nd execution shape (code-driven trusted, certification-gated, not adversarial sandbox). Epics 2&3 STAY done (forward FR amendments only). Strip/keep: KEEP model.py(canonical USDM+SCHEMA_VERSION)+extract/ingest/pipeline/projections; STRIP _client() creds(→platform injects ANTHROPIC_API_KEY via SecretsProvider)+plugin.py/cli.py filesystem I/O(→StorageProvider)+bridges/qa/gold/runs/corpus. Phase-1 isolation=per-skill VENV(deps+egress+secrets+raw-file); container/gVisor deferred to Epic 7 story. HEADLINE GATE (5.5-6): client skill runs E2E. Sequenced after Epic 5. (Renumbered 2026-06-25: Epic 10 → Epic 5.5; Epics 6-9 keep their numbers.)
  5.5-1-register-code-driven-hybrid-bundle-manifest: done   # code review 2026-06-26: 3-layer adversarial; all 3 ACs satisfied. 3 decisions + 10 patches applied (9 triaged + 1 promoted-from-dismissed during testing). HIGHs fixed: VERIFIED IP leak (field values echoed into 422 → now value-free) + stale schema_version/egress (D1: forbid cross-shape version switching via new HybridShapeMismatchError). Also: D2 kept+documented new fail-fast LLM-hybrid registration validation; D3 narrowed except→domain-only (no artifact delete on infra err); removed duplicate _SEMVER_RE (lazy-import canonical); tightened entrypoint regex; strengthened manifest detection so entrypoint-less manifest gets targeted INVALID_CODE_DRIVEN_MANIFEST (AC2); fixed tautological IP test; +egress/cross-shape/missing-requirements tests. Gates: ruff clean, 398 unit (+39), 92/92 test_skills (+8, 0 regressions), migration 0013 round-trip clean. 2 deferred (egress bound; UTF-8 BOM routing), 2 dismissed. | dev-story 2026-06-26: code-driven hybrid manifest registration (parser + create_skill/create_version hooks + migration 0013 + error code + tests). ruff clean, 359 unit (+24), integration written. story created 2026-06-26 (create-story; BACKEND-only; migration 0013 adds skills.schema_version+egress; new app/services/code_driven_hybrid.py; is_code_driven_manifest detection; hooks create_skill+create_version; hybrid_artifact.py untouched).
  5.5-2-schema-versioned-output-contract: done   # dev-story 2026-06-26: IMPLEMENTED → review. BACKEND-only. Added schema-versioned output contract as registry metadata + `consumes` declaration. SkillVersion.schema_version + SkillVersion.consumes per-version snapshots (AC3 independently resolvable via GET /{id}.current_version); Skill.consumes (JSONB, PATCH-able + clearable via null, NOT in _PATCH_NULL_REJECTED); consumes.schema_version semver-validated (model_validator on SkillCreate + SkillMetadataUpdate → 422); _SEMVER_RE duplicated locally in schemas/skill.py (circular-import guard: skill_service imports schemas). create_skill() gained consumes param + snapshots version row; create_version() snapshots manifest schema_version (None for non-code-driven) + inherits skill.consumes. schema_version stays non-PATCH-able (manifest-only; regression-tested). Migration 0014 (revision id `0014_skill_output_contract` — shortened from `0014_skill_version_schema_contract` which overflowed alembic_version VARCHAR(32); descriptive filename kept) adds skills.consumes + skill_versions.{schema_version,consumes}; round-trip clean. Scope honored: no venv executor (5.5.3), no ?consumes_schema_version= filter, no blocked-state refusal (5.5.5), no cross-skill schema_ref validation, no version-history endpoint, no FE; code_driven_hybrid.py + execution path untouched. Gates: ruff clean, 632 passed/36 skipped (0 regressions, full unit+integration vs compose Postgres+MinIO), 26 new tests (15 unit test_consumes_validation.py + 11 integration test_skills.py). No new dep. baseline_commit a111997.
  5.5-3-code-driven-hybrid-executor-venv: done   # code review 2026-06-26 (uncommitted diff): 3-layer adversarial [Blind/Edge/Auditor]. All 4 ACs implemented; scope boundary honored; no "Do NOT touch" file modified. 1 decision + 7 patches from static review + 3 MORE defects found by ACTUALLY RUNNING the Docker integration suite (live PG+MinIO+real venv/pip) = 11 patches applied & verified. SECURITY (HIGH): (1) full os.environ leaked into untrusted skill subprocess [now literal PATH/HOME+creds+ANTHROPIC_API_KEY only, mirrors code_sandbox.py invariant]; (2) unvalidated manifest.requirements = pip arbitrary-code-exec via --index-url/-e/URL refs [now _validate_requirements rejects pip directives + remote/direct-URL refs; bare local paths kept for Phase-1 install]; (3) unbounded entrypoint stdout → worker OOM [now off-thread bounded reader capped at EXECUTION_MAX_OUTPUT_BYTES]. ROBUSTNESS: output-storage put failure now → CODE_DRIVEN_EXECUTION_ERROR not INTERNAL; stdout sentinel-delimited envelope so skill prints don't corrupt it; params extraction matches codebase idiom. RUN-FOUND: (4) tests monkeypatched a non-existent attr [get_secrets_provider is a lazy import] + nested asyncio.run() under celery_eager → rewrote tests to call run_code_driven_hybrid directly; (5) RLIMIT_AS raised ValueError on macOS [dev platform] crashing every run → now tolerated, + non-timeout subprocess/OS errors caught → CodeDrivenExecutionError [AC4 crash→failed]; (6) output-cap inline read blocked the wall-clock timeout [sleeping child held stdout open, timeout test hung] → off-thread reader so proc.wait(timeout=) is authoritative. Gates: ruff clean; 392+26 unit pass 0 regressions; 3/3 code-driven integration tests pass [9.3s, live infra — e2e completed+canonical.json in MinIO, bad envelope→CODE_DRIVEN_ENVELOPE_ERROR, runaway→EXECUTION_TIMEOUT]; test_invocations 32/32 still green. 3 deferred [zip extraction→5.5.4; venv pre-bake; egress enforcement→Epic 7, all logged in deferred-work.md]. baseline_commit a111997.
  5.5-4-raw-file-input-to-hybrids: done   # code review 2026-06-26 (uncommitted diff): 3-layer adversarial [Blind/Edge/Auditor]. All 4 ACs VERIFIED by the Auditor (raw ingest_key/ingest_storage used — NOT the parsed_content_key text seam; PHI-safe log = file_count+total_bytes+job_id only; AC3 text seam untouched + static inspect.signature guard proves _run_prompt/_run_code carry no ingest_storage; multi-file runner/cmd argv layout lockstep). Locked decisions honored, no scope-boundary violations, File List accurate. NO Critical/High/Medium survived triage. The Blind Hunter (diff-only) raised 1 High + 4 Mediums — ALL overturned as false positives by source verification: (1) "unit tests patch wrong run_in_threadpool" → FALSE, it's a function-local import (executor:169) re-executed per call so patch("fastapi.concurrency.run_in_threadpool") intercepts (52 unit tests pass); (2) "two e2e tests assume contradictory output shapes" → FALSE, executor persists envelope.canonical (line 439), both tests store+read the canonical dict; (3) "path traversal via original_filename" → HANDLED, _sanitize_filename basename + re.sub strips separators (../../etc/passwd→0-passwd, Edge verified by execution); (4) "params in-place mutation dirties DB" → FALSE, InvocationJob.inputs is plain JSONB not MutableDict; (5) "mapper duplicated literals" → match exceptions' ERROR_CODE exactly, no drift. 3 patches APPLIED & ruff-clean (all doc/comment accuracy: executor step-6 docstring no longer says input_path=None stub; step-2 comment no longer mislabels zip-extraction as "deferred to 5.5.4"; get_raw_file_ref docstring now states both seams share the parsed gate but check different keys ingest_key vs parsed_content_key). 1 DEFERRED (deferred-work.md): malformed/non-string file_ref_id escapes _uuid.UUID() as generic SKILL_EXECUTION_ERROR not FILE_REF_NOT_FOUND — unreachable via the validated API (schema list[uuid.UUID]→422; persistence writes str(fid)) AND the sibling _run_code/_run_hybrid paths use the identical unguarded idiom → belongs in a cross-runtime fix, not a 5.5.4-only divergence. | dev-story 2026-06-26 (claude-sonnet-4-6): all 5 tasks; 437 unit + 6 integration pass. story created 2026-06-26 (create-story; BACKEND-only, no migration/dep/FE). Replaces the 5.5.3 `input_path=None` stub with real raw-file materialization. 2 LOCKED decisions w/ user: (1) MULTI-FILE — materialize ALL file_ref_ids; runner gets input_path=first (back-compat w/ singular 5.5.3 runner + single-PDF client skill) + params["input_paths"]=full ordered list (entrypoint sig func(input_path,output_dir,params) UNCHANGED); (2) RAW-ONLY for code-driven — gets raw bytes via input_path/input_paths & does its OWN ingestion (vision/rasterize), NOT build_context_input text; prompt/LLM-hybrid text seam UNTOUCHED (AC3). CRITICAL trap surfaced: raw bytes live at FileReference.ingest_key (INGEST bucket) NOT parsed_content_key (output bucket — that's the text seam); wrong key = skill gets text where it expects a PDF. Plumbing gap: get_ingest_storage() exists (storage.py:207) but is NOT threaded to execute_skill — thread ingest_storage worker(execution_tasks.py:113,186)→execute_skill(297)→_run_hybrid(656)→_run_code_driven_hybrid(951)→run_code_driven_hybrid; do NOT add to _run_prompt/_run_code (AC3 obvious in diff). New ingest_service.get_raw_file_ref helper (status gate=parsed, same bar as build_context_input; returns ingest_key+original_filename+content_type; reuse _sanitize_filename). Runner off-by-one trap: _RUNNER_SCRIPT argv shifts (entrypoint=1,input_path=2,output_dir=3,params=4) + __VELARA_NONE__ sentinel for 0-file; cmd list must match in lockstep (unit-test arg positions). Error map: FileRefNotFound/NotReady (FILE_REF_NOT_FOUND/FILE_REF_NOT_READY, ingest_service.py:64/96) NOT in _map_error_code → fall through to SKILL_EXECUTION_ERROR; add 2 isinstance branches. Consumer audit (Epic 5 retro): execute_skill kwarg add → TypeError any un-updated caller incl. test helpers; grep+update all. NO migration/dep/FE. baseline_commit a111997. ⚠️ VERIFIED against the REAL client skill (~/Downloads/velara-protocol-extractor, deliverable 2026-06-15): 5.5.4's raw-file-by-path premise is CORRECT (ingest.py:150 pdfplumber.open(path) + ingest.py:193 fitz.open(path) need a real PDF on disk; ANTHROPIC_API_KEY env injection correct). BUT found 2 BLOCKING contract mismatches (NOT 5.5.4's scope; logged in deferred-work.md for 5.5.6/5.5.3-followup): (1) entrypoint plugin.py:run(input_path,output_dir,*,model,consensus_runs,enrich,emit_excel) takes NAMED kwargs — the 5.5.3 runner's func(...,params=dict) raises TypeError → client skill never starts; needs **params splat or 5.5.6 adapter (architect decision); (2) real run() returns artifacts as a DICT {canonical_json,excel} + extra billing_grid key, but CodeDrivenResultEnvelope wants list[dict] → 5.5.5 must reshape. Real schema_version=0.3.0 (model.py:33), NOT the stale 0.1.0 in bundled manifest.json.
  5.5-5-multi-artifact-canonical-output-blocked-state: done   # code review 2026-06-26 (uncommitted working-tree diff across BOTH repos — velara-api HEAD 882a19a + velara-web HEAD 5cb902f; the parent dir is not a git repo, each sub-project is): 3-layer adversarial (Blind/Edge/Auditor), all 3 ran, none failed. AC1–AC4 SATISFIED, AC5 correctly deferred, NO DO-NOT-TOUCH boundary crossed (aggregate_results, jobs READ API, InvocationResult schema/migration, InvocationRequest all untouched), no migration, VARCHAR(16) fits "blocked". NO Critical/High actionable. Triage 3 patch / 3 defer / 8 dismissed. 3 PATCHES APPLIED & verified: (P1 MED) jobs.py:36 JobStatusLiteral was missing "blocked" → GET /jobs?status=blocked 422'd even though the FE now sends it [added "blocked"; blast radius = def@36 + query@60 only]; (P2 LOW) audit.py:85 stale outcome column comment → appended "| blocked"; (P3 LOW) RunConsole.tsx blocked panel gated downloads solely on output_files.length>0 → gave it the same single-file output_file_url/key IIFE fallback the completed panel uses. 3 DEFERRED (logged to deferred-work.md + story Review Findings): (D1, HIGH-logic but UNREACHABLE today) aggregate_results child-classification loop (execution_tasks.py:421-431) has no JOB_STATUS_BLOCKED branch → a blocked fan-out child falls through to "contributes nothing" → parent rolls up to completed, silently dropping the QA hold; unreachable because blocked only originates from the code-driven executor and no code-driven skill is location_dependent (empty intersection), and story §Out-of-Scope L116 forbids touching aggregate_results — MUST be fixed when the first code-driven fan-out skill ships; (D2) unbounded artifact open().read() (executor:517) → worker OOM risk, mitigated by subprocess RLIMIT_AS, pre-existing 3.6 pattern; (D3) mid-loop upload failure orphans already-uploaded bucket objects (no orphan-GC project-wide). 8 DISMISSED as false positives — notably Blind Hunter's "missing url breaks downloads/tautological test" [READ API jobs.py:132-178 presigns each output_files[].url at fetch time — executor intentionally omits url; VERIFIED], "is_blocked UnboundLocalError" [assigned :203 inside try, read :251 same success path, except :261 never refs it], and "ineffective mock target fastapi.concurrency.run_in_threadpool" [executor imports it FUNCTION-LOCALLY at :178 so patching the origin module works]. Gates: ruff clean, BE 422 unit pass (0 regressions), FE typecheck 0, FE eslint clean, 54 RunConsole tests pass. Findings in story Review Findings section. Prior: story created 2026-06-26 (create-story; FULL-STACK). The OUTPUT half of the code-driven hybrid path. 3 LOCKED decisions w/ user: (1) REUSE result_metadata["output_files"] JSONB (Story 3.6 shape) for multi-artifact — NO InvocationResult migration (jobs READ API jobs.py:132-178 + Run Console RunConsole.tsx:908-962 ALREADY presign+render it; 5.5.5 just POPULATES it from the executor); (2) DEFER the consumes-refusal of a blocked upstream — no pipeline-consumption mechanism exists (no upstream_job_id on InvocationRequest), logged to deferred-work.md for the pipeline epic (5.5.2 consumes + this blocked-state are the now-in-place prerequisites); (3) FULL-STACK — BE bulk + small FE (add 'blocked' to JobStatus union + jobStatusMeta amber badge + useJob TERMINAL_STATUSES; multi-download UI already built in 3.6). Backend: reshape CodeDrivenResultEnvelope.artifacts list[dict]→dict[str,str|None] (real skill returns {canonical_json,excel} DICT + billing_grid — closes the 2 BLOCKS-5.5.6 items logged under 5.5-4); executor uploads ALL artifacts from output_dir BEFORE rmtree + writes output_files + typed canonical {schema_ref,schema_version,data}; new JOB_STATUS_BLOCKED in JOB_TERMINAL_STATUSES (VARCHAR(16), no migration); new mark_blocked (mirror mark_completed, writes result row, NO error_code); worker branches on result_metadata["status"]=="blocked". ⚠️ Audit outcome is a VALIDATED frozenset {success,failure,cancelled} (audit_service.py:33) — MUST add OUTCOME_BLOCKED or record_entry raises. NO migration (status+audit both VARCHAR, no DB CHECK — verify). baseline_commit a111997.
  5.5-6-adapt-and-run-client-extractor-e2e: done   # HEADLINE GATE PASSED 2026-06-26. ARROW NCT03037385 (149pp) → job 6635015d, completed, schema_version 0.3.0, 10 arms/16 encounters/25 activities/102 instances/0 QA flags. Fixes applied: writable worker volume, dispose_engine() event-loop teardown, EXECUTION_MAX_MEMORY_MB=4096. Tests: 722 passed (7 adapter unit + 1 adapter integration), ruff clean. Epic 5.5 closed. Forward-deps: Epic-6 cert gate; runner-arg-shape unification; DOCX; aggregate_results blocked-child (unreachable). — epic does not close until this passes. story created 2026-06-26 (create-story; the INTEGRATION PROOF — exercises every prior 5.5 story against the REAL client skill, not a stub). 3 LOCKED decisions w/ user: (D1) SKILL-SIDE ADAPTER SHIM for the arg-shape mismatch — NO platform runner change; add velara_extractor/adapter.py(input_path,output_dir,params) that WHITELIST-unpacks params→run()'s named kwargs (model/consensus_runs/enrich/emit_excel) and IGNORES params["input_paths"] (platform always adds it; real run() has no such kwarg → blind **params splat would TypeError); register entrypoint=velara_extractor.adapter:run. (D2) NO CERT GATE in Phase-1 — all code-driven hybrids trusted (already the 5.5.3 behavior; egress+secrets unconditional); AC1 "certified" = documented forward-dep on Epic 6. (D3) FULL adapt + corrected manifest + REAL-PDF E2E w/ live ANTHROPIC_API_KEY. ⚠️ CRUX SEAM (the install): Phase-1 stored artifact IS the manifest JSON, not a zip (code_driven_executor.py:198-205, bundle_dir=workspace) — executor does NOT extract code; it pip-installs manifest.requirements into the venv then imports the entrypoint. So velara_extractor must be pip-installable; _validate_requirements (code_driven_executor.py:676-700) REJECTS --index-url/-e git+/URL refs but ALLOWS bare local paths (it even says "see story Dev Notes" → THIS story) → Phase-1 mechanism = adapted bundle at a stable worker path + a bare-local-path requirements line. Ground truth verified: runner code_driven_executor.py:141 passes params= (UNCHANGED); real plugin.py:23 run() = named kwargs; SCHEMA_VERSION=0.3.0 NOT manifest's stale 0.1.0; consensus_runs IS wired (plugin.py:52→pipeline.py:72→extract.py:196); deps anthropic>=0.69/pydantic>=2.6/pdfplumber>=0.11/PyMuPDF>=1.24/openpyxl>=3.1; model claude-opus-4-8 (env VELARA_MODEL override); PDF-only (manifest's DOCX claim is wrong). FE 100% DONE by 5.5.5 (multi-artifact dl + blocked panel + QA reasons, filename-agnostic) → expect ZERO FE change. Envelope ALREADY reconciled to real skill by 5.5.5 (artifacts dict[str,str|None]+billing_grid). Likely NO platform code change beyond tests (one narrow _validate_requirements local-path allowance only IF no existing form passes). Tasks: adapt bundle(strip cli/bridges/qa/gold/runs/corpus, KEEP model.py+extract/ingest/pipeline/projections+_client as-is) → adapter shim → corrected manifest(schema_version 0.3.0, PDF-only, adapter entrypoint, real reqs, egress api.anthropic.com, no BOM) → register → live E2E vs ~/Downloads/ARROW_NCT03037385_protocol.pdf → blocked-QA path → tests(Docker integration mocks Anthropic for CI determinism + adapter unit test; live full-PDF = documented manual gate proof). Prereq: live ANTHROPIC_API_KEY in dev .env (MissingSecretError→MISSING_CREDENTIAL else). Forward-deps logged: Epic-6 cert gate, runner-contract unification (if many code-driven skills), DOCX, aggregate_results blocked-child (unreachable here). See story file for full Dev Notes.
  epic-5.5-retrospective: done

  # ─────────────────────────────────────────────────────────
  # Epic 6: Certification & Governance
  # ─────────────────────────────────────────────────────────
  epic-6: done   # KICKED OFF 2026-06-27 (create-story 6-1; out-of-sequence — chosen over continuing Epic 7). First Certification & Governance story.
  6-1-certification-data-model-and-state-machine-api: done   # story created 2026-06-27 (create-story; BACKEND-only; migration 0015 certification_records + immutability triggers; new app/models/certification.py + schemas/certification.py + services/certification_service.py + api/v1/certifications.py; two-key gate hooked into EXISTING assert_can_advance; re-cert reset in create_version). 3 decisions LOCKED w/ user: D1 records-resource shape (POST/GET /api/v1/certifications) + gate on EXISTING PATCH /skills/{id}/lifecycle (NO new /certify action verb, NO second advance path); D2 CertificationRecord ONLY (ValidationRecord in project-structure-boundaries treated as legacy — PRD unified cert+validation); D3 store certifier_user_id+role now, printed-name resolution is Epic-7 Cognito forward-dep (AuthPrincipal has no name/email today). Auto-advance-on-2nd-key is 6.3 scope (6.1 supplies the evaluate-eligibility mechanism). dev-story 2026-06-27 (claude-sonnet-4-6): all 9 tasks complete; 724 passed 0 failed (11 integration tests + 19 unit tests added; 0 regressions); migration 0015 round-trip clean; ruff clean. → review. CODE REVIEW 2026-06-27 (3-layer: Blind+EdgeCase+AcceptanceAuditor): all 7 ACs + 14 dev-traps + D1-D3 verified satisfied; 5 patches applied (retired-skill cert guard→422 SKILL_RETIRED; DB-trigger immutability test; certified_at window assertion; eligibility-query org/version filter test; TOCTOU for_update lock in transition_lifecycle), 1 deferred (AC4 RECERTIFICATION discriminator simplification — spec-sanctioned, revisit 6.3), 3 dismissed. Re-verified: 13 cert integration + 20 cert unit + 158 skills/lifecycle tests pass, ruff clean. → done.
  6-2-technical-certification-workflow-ui: done   # code-review 2026-06-27 (bmad-code-review, opus-4-8): review → DONE. 3-layer adversarial (Blind + Edge + Auditor), all ran, all 6 ACs verified. 2 decision-needed (resolved w/ user: deep-link guard → BOTH FE+BE; AC4 grouping → per-row status accepted as 'state') + 9 patches APPLIED & verified + 1 defer + 7 dismissed (incl. 4 false positives: Escape cleanup correct L49, aria-labelledby wired L91, org_id:str correct, methodological-no-recording-UI by-design = 6.3). 🔴 CRITICAL caught & EMPIRICALLY PROVEN: GET /certifications/pending 500'd (sqlalchemy MissingGreenlet) on the NORMAL case — skill.versions lazy-loaded in async w/ no selectinload (Base has no AsyncAttrs); the 3 pending tests 'passed' pre-fix ONLY because they skip when Postgres is down. Fixed via an explicit SkillVersion id→version query (keeps it 3 flat queries); reproduced the live-PG 500 on the old image, then rebuilt the api image + recreated the container and re-ran GREEN. Other HIGHs: stale-version cert wrongly disabled Record (now current_version_id-scoped, +regression test) + new SKILL_NOT_CERTIFIABLE 422 guard rejects deep-link/direct-API cert of draft/client_ready skills (FE hides the action + maps the code). MEDIUMs: AC6 signature_meaning+certifier_role now rendered (badge tooltip + visible manifestation line); e-sig modal blocks under a null user (re-auth prompt, no 'unknown' attestation); focus trap enumerates all focusables + initial focus → first checkbox (not the disabled Submit). LOW: versionless internal_ready skill → 'No version to certify' bucket. Gates: web typecheck 0 / eslint clean / 279 web tests (+3 regression over 276) / build ✓; api ruff clean / 19 cert integration (+2 SKILL_NOT_CERTIFIABLE) / 612 regression-run pass 0 regressions vs LIVE Docker PG+MinIO. 1 deferred (raw certifier_user_id rendered as display name → resolve at Epic 7 identity, per Decision D2 → deferred-work.md). Prior: story created 2026-06-27 (create-story; FULL-STACK per Decision D1). FE Certification screen (unified pending list + detail panel + Record-Technical-Certification modal w/ checklist + Part-11 e-sig) consuming 6.1 API, PLUS one small BE read endpoint GET /api/v1/certifications/pending (no migration/model change — reuses evaluate_certification_eligibility). 2 decisions LOCKED w/ user: D1 backend bulk /pending endpoint (avoids FE N+1; does NOT add cert flags to Skill model — rejected in 6.1); D2 signer = user_id+role ("I, {user_id} ({role}), certify…") w/ Epic-7 printed-name forward-dep note. ⚠️ MOST FE SCAFFOLDING ALREADY STUBBED — FILL don't recreate: cert tab (navTabsData.ts:11), route Placeholder (internal.tsx:94-97), api/certifications.ts + features/certification/types.ts (both `export {}`); --color-key-tech*/--color-key-method* tokens pre-exist (index.css:54-57). REUSE: SkillDetail Card/MetaRow/SchemaBlock + double-submit guard, ConfirmDialog focus-trap (build new modal — too rigid to reuse), useSkills mutation/invalidation, SkillLifecycleBadge token-style. TRAPS: no display name (user_id only, NOT AppBar's hardcoded "M.Maxwell"); deprecated green-/amber-/gray-/red- tokens; both buckets are internal_ready (bucket by cert flags not lifecycle_state); no optimistic updates for cert state (arch rule). Scope-out: methodological recording + auto-advance = 6.3; cert history = 6.4; RBAC = Epic 8. Baselines: ~259 web tests / ~754 BE tests.
  6-3-methodological-certification-workflow-ui: done   # story created 2026-06-27 (create-story; FULL-STACK — small BE auto-advance wiring + FE methodological modal/detail-panel). The SECOND-key half of the two-key workflow: AC4 auto-advances to client_ready on the methodological key. 3 decisions LOCKED w/ user: D1 auto-advance lives SERVER-SIDE in record_certification (NOT a 2nd FE PATCH) — reuses evaluate_certification_eligibility + calls the already-wired transition_lifecycle (for_update lock + two-key gate); FR-CRT-01 invariant must hold for any caller. Accepted cost: cert-insert + advance = 2 non-atomic commits (self-heals via still-available manual PATCH). D2 FE gates methodological button on technical-first; BE does NOT assume ordering (direct-API meth-first records cert, just doesn't advance). D3 auto-advance failure on a review_required derived skill is SWALLOWED (cert already committed/valid; logged; stays internal_ready) — not surfaced as a cert error. ⚠️ MOST of the FE shell from 6.2 already handles methodological — REUSE not recreate: CertKeyBadge already dual-type; getBucketLabel already returns 'Awaiting Methodological Certification'; api/certifications.ts + useCertifications + errors.ts + route all cert-type-agnostic. NEW = RecordMethodologicalCertModal (clone tech modal, 4 changes: title, 3-item FR-CRT-03 checklist, "methodology and voice" e-sig wording, certification_type:'methodological') + extend CertificationDetail (hoist methCert, modal discriminator, 2nd button+manifestation) + FIX useRecordCertification invalidation to add ['skills']+['skills-page'] (auto-advance changes lifecycle_state → Registry/⌘K were stale). NO migration / model / schema / new endpoint. Out of scope: cert history UI + re-cert warnings = 6.4; RBAC (Matt-only) = Epic 8; AC4 RECERTIFICATION_REQUIRED discriminator fix stays deferred (needs state-history table, deferred-work.md:203). TRAPS: version-scope certified state (isCurrentVersionCert — superseded-version cert must not disable button); never set lifecycle_state directly (go through transition_lifecycle); first key must never advance; keep assert_can_advance sync; methodological checklist is 3 items not 5; signer=user_id+role (no display name til Epic 7). Baselines: web 279 / cert integration 19 / BE regression 612. || code-review 6-3 (2026-06-27): 3-layer adversarial across BOTH repos. Backend faithful (D1/D2/D3 + all 11 traps clean). 5 decisions resolved (accept FE redesign scope, amend AC1+AC2 [PENDING John sign-off], relabel criteria as guidance-only, defer advance-signal) + 7 patches (6 applied, 1 amend pending PM) + 4 defer + 6 dismissed. Applied: 🔴 redundant client_ready->client_ready advance swallowed (was a spurious 500 on a committed cert under concurrent 2nd key / manual-PATCH race) + meth-first isLocked fix + Trap-9 hardcoded-name removal + fabricated per-criterion count removed + unit-test double-patch fix. GREEN: FE 292, BE unit 497, cert+skills integration 126 (live PG, +2 new tests). Status review->in-progress (AC1/AC2 amendment blocked on John). Open: PM sign-off on AC1/AC2 rewrite. || PM sign-off (John) 2026-06-27: AC1 amended (per-key chips ACCEPTED + required per-row outstanding-key status `rowCertStatus`; implemented+tested) & AC2 amended ("Turn methodology key"->named modal, as-is). Both rebaselined. Status in-progress->done. FE 293 ✓ / BE unit 497 ✓ / cert+skills integration 126 ✓.
  6-4-certification-history-and-re-certification-on-new-version: done   # code-review 2026-06-27 → done (3 layers: Blind+EdgeCase+AcceptanceAuditor). AC1–AC4 + all locked decisions/traps verified PASS. Triage: 1 patch APPLIED (toUTCStr dead try/catch → explicit Number.isNaN(getTime()) guard so "Invalid Date" can't render on the Part 11 e-sig line; typecheck clean, 11 CertificationHistory tests green), 2 decision-needed DISMISSED (unknown certification_type silently dropped — data emits only the 2 known types; duplicate same-type+version overwrite — unreachable, BE is one row per (version,type)), 1 DEFERRED (per_page:200 cap — history + needsRecert read only page 1; >200 certs silently truncates AND could false-positive "Requires re-certification" — spec-sanctioned D3/Trap2 bound, logged deferred-work.md), 7 dismissed as noise/false-positive (semver comparator + Number()→NaN parsing UNREACHABLE: BE _SEMVER_RE strictly rejects non-canonical X.Y.Z at write time skill_service.py:182; RQ-v5 hashKey structural so query-key literal not a thrash; redundant ['skills'] invalidation mirrors useTransitionLifecycle by design). Prev: dev-story 2026-06-27 → review (claude-sonnet-4-6). FRONTEND-only + 1 BE verification test. Gates: 313 web tests (+20 over 293 baseline) typecheck 0 lint clean build ✓. New: CertificationHistory.tsx (grouped newest-version-first, per_page:200, placeholder for missing keys, UTC Part-11 dates, --color-key-tech/method tokens) + acknowledgeParentUpdate fn/hook (POST no-body, invalidates ['skills',id]+['skills']+['skills-page']) + SkillDetail: mounted history, "Requires re-certification" badge (0 current-version certs AND ≥1 prior cert — mirrors BE discriminator, no false-positive on never-certified draft), "Parent skill was updated…" warning + Acknowledge button (clears review_required via cache invalidation). BE test: test_list_certifications_spans_multiple_versions added to test_certifications.py (proves cross-version AC4 spread). story created 2026-06-27 (create-story; FRONTEND-only + 1 optional BE verification test). EXHAUSTIVE backend probe (subagent, source-verified): ALL 4 AC areas already built + tested on the BE — AC4 GET /certifications?skill_id= already returns all records across ALL versions, certified_at ASC, per_page≤200 (each record carries skill_version → FE groups by version); AC2 create_version already resets client_ready→draft (skill_service.py:623-629) + certs are version-FK'd so old certs persist/don't carry over + RECERTIFICATION_REQUIRED gate (cert_service:446-455); AC3 parent create_version flags derived children review_required (skill_service.py:637-645) + assert_can_advance blocks client_ready (DERIVED_SKILL_REVIEW_REQUIRED 422) + POST /skills/{id}/acknowledge-parent-update clears it (skills.py:290-311). NO endpoint/service/migration/model/schema change. 3 decisions LOCKED: D1 history + re-cert indicator + derived-review warning live on SkillDetail.tsx (NOT the cert screen — that's internal_ready+current-version-only, can't show a re-versioned draft skill); D2 close the acknowledge UX dead-end (FE shows DERIVED_SKILL_REVIEW_REQUIRED error today but has NO way to clear the flag) with a real Acknowledge button → existing acknowledge endpoint; D3 group history by version CLIENT-SIDE from the existing flat list + raise per_page to 200 (closes 6.3-deferred page-1 truncation). NEW FE: CertificationHistory.tsx (reuses existing useCertifications) + acknowledgeParentUpdate fn/hook (mirror transitionLifecycle) + SkillDetail edits (mount history, "Requires re-certification" badge [gate: 0 current-version certs AND ≥1 prior cert — NOT lifecycle alone, else fresh draft false-positives], "Parent skill was updated…" warning + Acknowledge button). TRAPS: re-cert indicator must mirror BE discriminator (don't false-positive a never-certified draft); per_page=50 default truncates history; API order is oldest-first (reorder newest-version-first in component); old certs preserved not deleted (show prior-version rows); acknowledge=POST no-body /acknowledge-parent-update invalidates SKILLS keys not certifications; signer=user_id+role; DO NOT fix the RECERTIFICATION_REQUIRED "any prior cert" discriminator (spec-sanctioned, needs state-history table, deferred-work.md:203). Baselines: web 293 / cert integration ~24 / BE unit 497.
  epic-6-retrospective: done   # retrospective 2026-06-29 → epic-6-retro-2026-06-29.md. 4/4 stories done; DB-level Part 11 immutability landed (Winston's 5.5 flag honored). Key insight: production code solid, but 4 of 6.1's 5 review patches were test-integrity fixes (hollow immutability test = 405 from routing not the DB; mocked-away security filters) — adversarial review IS the control, no new rule added. Decisions: KILLED epic-list milestone-framing carry (5 consecutive deferrals, no consumer); downgraded PM sign-off item to optional note (two-retro rule); attached both Epic 6 compliance deferrals (FR-SEC-12 e-sig re-auth + printed-name resolution) to Story 7.3 Cognito as forward-deps. Epic 7 already in flight (7.1 done out-of-sequence); nothing in Epic 6 blocks it.

  # ─────────────────────────────────────────────────────────
  # Epic 7: Infrastructure, Deployment & Cloud Auth   (NEW — was Epic 1 stories 1-3..1-6)
  # Prerequisite: client AWS account + Vitalief-owned GitHub repos provisioned.
  # ─────────────────────────────────────────────────────────
  epic-7: done   # KICKED OFF 2026-06-27 (create-story 7-1; out-of-sequence per user — Epic 7 before Epic 6). DEV-environment only for now.
  7-1-aws-infrastructure-foundation: done    # code review 2026-06-27 (bmad-code-review, opus-4-8): 3-layer adversarial over the scoped diff (velara-api git diff HEAD@281bba7 + untracked terraform/ + 2 tests; velara-web terraform/ only — unrelated Epic-5 .ts/.tsx WIP excluded). All 3 layers ran, 0 failed. 3 decision-needed (all resolved→patch) + 3 patch + 6 defer + 5 dismissed. 6 PATCHES APPLIED & verified: P1 Redis ssl_cert_reqs None→ssl.CERT_REQUIRED (CERT_NONE = encrypted-but-unauthenticated MITM risk; ElastiCache uses Amazon public CA so cert validation works) + test updated; P2 NEW .gitignore terraform coverage in BOTH repos (**/.terraform/, *.tfstate*, keep !.terraform.lock.hcl) — closes AC3/SEC-04 leak: random_password.db_master.result + DATABASE_URL would land in an untracked-not-ignored tfstate on first apply, committable by a broad git add (verified git check-ignore); P3 REDIS_URL scheme detection hardened (strip+lowercase+split, non-redis/rediss → ValueError) + 3 tests; P4 corrected dishonest Completion-Notes line — AC4 (HTTP→HTTPS redirect) + AC1-HTTPS/TLS are DEFERRED in dev (acm_certificate_arn="" → has_cert=false → HTTP:80 forwards plaintext, HTTPS listener count=0), NOT "code verified"; P5 documented intentional dev db_backup_retention_days=0 (NFR-06 7d floor applies to staging/prod, not the no-data personal dev account); P6 parameterized out operator-specific values — aws_profile default ""→provider null-coalesce + removed ="hozefa" from both dev.tfvars; backend bucket → partial config via -backend-config (backend.dev.hcl.example added, real .hcl git-ignored); both READMEs updated. Gates: 10/10 worker unit tests pass (was 3, +7); terraform validate + fmt ✅ on all 3 roots. 6 DEFERRED (logged in deferred-work.md): staging RDS skips final snapshot on destroy (coupled to deletion-protection, off for staging — gated on BAA); Container Insights log groups bypass 90d AC5 (apply-dependent); no validation on subnet-CIDR list lengths/≥2-AZ; nat_gateway_count=0 indexes [-1]; velara-web unused aws.us_east_1 aliased provider; worker container has no healthCheck. 5 DISMISSED (redundant ssl double-guard correct; conftest migration-override correctly scoped by pytest proximity; default.redis7 param group valid; RDS engine_version "16" major-pin acceptable; IAM kms:GenerateDataKey scoped to key ARNs not wildcard). ✅ terraform apply RUN for dev (2026-06-27, post-review): velara-api applied IN FULL (VPC/RDS/Redis/ECR/IAM/ALB/ECS/Secrets/CloudWatch/S3); velara-web applied EXCEPT CloudFront — frontend S3 bucket created, aws_cloudfront_distribution.frontend FAILED on an AWS new-account verification gate (operator/AWS-side, NOT a code defect) → DEFERRED, re-apply velara-web once AWS lifts the CloudFront restriction (logged in deferred-work.md). AC1/AC2/AC3/AC5 now live on velara-api; AC4 = dev HTTP-only by design (no cert). LIVE-VERIFIED 2026-06-27 (AWS_PROFILE=hozefa, acct 068858795262, us-east-1): RDS PubliclyAccessible=False+encrypted (pg 16.13) & Redis Transit+AtRest encryption=True, both refuse external nc (private); api+worker task defs inject 5 secrets via Secrets Manager ARN valueFrom (env block secret-free); CloudWatch /velara/dev/{api,worker}=90d; ALB single HTTP:80 forward (no 301, dev-plaintext) curl→503 (no ECS target yet, image=7.2); REDIS_URL secret=rediss:// (exercises P1 CERT_REQUIRED); cloudfront list-distributions=None (confirms deferred). Findings in story Review Findings + Live verification block. Note: P6 backend→partial-config means re-init needs `terraform init -reconfigure -backend-config=backend.dev.hcl` (operator-local, git-ignored). Prior: dev-story 2026-06-27 → review. IaC authored (18 velara-api tf files + 8 velara-web tf files); D1 Sentry-only (no X-Ray); D3 celery_app.py patched for rediss:// TLS. terraform validate + fmt ✅ on all 3 roots.
  7-2-cicd-pipeline-setup: done   # code-review 2026-06-29 → done. 4-layer review + migration specialist: 2 decisions resolved (File List corrected; CI/conftest reconciliation-reversal recorded), 7 patches APPLIED+verified, 9 deferred (→deferred-work.md), 6 dismissed. HEADLINE FIX (user ask): deploy ran NO migrations against live RDS + circuit-breaker can't catch drift (/health/ready=SELECT 1) → added `aws ecs run-task` alembic-upgrade-head step BEFORE update-service (private RDS unreachable from runner) + ecs:RunTask/DescribeTasks IAM + ecs_security_group_id output + 2 new repo vars (ECS_PRIVATE_SUBNETS/ECS_SECURITY_GROUP) + expand-contract runbook. Other patches: jq stale-image assertion, register-both-before-update (no version skew), cancel-in-progress:true, workflow_run head_branch/stale-SHA guard, conftest URL-rewrite hardened (urllib.parse, unit-tested), web S3 assets-first/index-last no-cache. Verified: actionlint -shellcheck ✅ all workflows; terraform validate+fmt ✅; ruff ✅; conftest edge-cases ✅. NOTE: patches are in the working tree, NOT committed (awaiting operator commit/push). Prior — was 1-4. dev-story 2026-06-29 → review. Deploy automation built: 2 deploy.yml pipelines + NEW TF (OIDC provider + 2 deploy roles + ECS circuit_breaker) + VITE_API_URL injection. AC1/AC2 satisfied (CI as-built). No app code. 9 files (5 api/4 web). terraform validate+fmt ✅ both roots; actionlint -shellcheck ✅ all 4 workflows. LIVE APPLY (AWS_PROFILE=hozefa, acct 068858795262, us-east-1): velara-api applied IN FULL (OIDC provider + gha-deploy-api role + circuit_breaker on both ECS services — plan=No changes, API-verified); velara-web gha-deploy-web role+policy applied, CloudFront DEFERRED on AWS new-account gate (403, same as 7.1). 2 code fixes the live apply caught (not validate/actionlint-catchable): IAM description em-dash→hyphen; web role cloudfront ARN decoupled from the un-creatable distribution → account-scoped so S3 perms apply. AC3/AC5/AC6(api) infra-LIVE; AC4(web) deploy-role-live + CloudFront honest-deferred. Real ARNs: gha-deploy-api=arn:...:role/velara-dev-gha-deploy-api, gha-deploy-web=arn:...:role/velara-dev-gha-deploy-web; dev ALB DNS=http://velara-dev-alb-1548026043.us-east-1.elb.amazonaws.com. Remaining operator steps: set repo vars AWS_DEPLOY_ROLE_ARN (both) + VITE_API_URL (web) + push main to fire the pipeline.
  7-3-cognito-authentication: done   # was 1-5. story FILE written 2026-06-29 (create-story). FULL-STACK + Cognito IaC. 3 source-verified audits (api auth seam / web auth surface / cognito TF). 2 decisions LOCKED w/ user: D1 scope = FULL CODE + dev Cognito APPLIED (author all TF, implement BE+FE, apply a real dev pool, flip dev AUTH_BACKEND=cognito, E2E — apply/seed/env-set OPERATOR-GATED, mark honestly; retain DevAuthProvider for local/CI); D2 login UX = Amplify SDK signIn() from the existing BRANDED LoginPage (username+password), NOT Cognito Hosted UI. Implemented 2026-06-29 (claude-sonnet-4-6). T12 operator-gated (apply+E2E). CODE-REVIEW 2026-06-30 (bmad-code-review) → DONE. 5 decisions resolved (D1 accept Cognito-required local dev + .env.example warning; D2 silent refresh in 401 interceptor; D3 signOut() in failure path; D4 Email label + loginWith:{email:true}; D5 accept sync PyJWKClient + timeout=5). 7 patches applied: (1) client.ts 401 interceptor async + fetchAuthSession({forceRefresh:true}) + retry-on-success + signOut()+redirect-on-failure; (2) auth.ts _decodeIdTokenClaims now throws on malformed token; (3) auth.ts UserAlreadyAuthenticatedException recovery adds clearSession() before retry; (4) LoginPage Email label/placeholder/type=email; (5) main.tsx loginWith:{email:true}; (6) cognito.tf dead-ternary replaced with var.frontend_url + compact(); (7) test_cognito_provider.py kid header + token_use on 3 bare jwt.encode calls + new test_access_token_rejected. LoginPage.test.tsx: from-redirect test restored, label matchers updated /email/i. deferred-work.md T13 FR-SEC-12 + printed-name entries appended. .env.example VITE_COGNITO_* required warning added. Earlier patches (from impl): PyJWKClient(lifespan=3600,timeout=5) + token_use enforcement in validate_token(). FE gates: 320 tests ✓ tsc ✓ eslint ✓ TF validate ✓.
  7-4-cloud-observability-and-hipaa-compliance-baseline: done   # was 1-6. code-review 2026-06-30 (bmad-code-review, opus-4-8) → DONE. 3-layer adversarial (Blind+EdgeCase+Auditor, 0 layers failed): 3 decision + 5 patch raised, 4 dismissed. ALL THREE layers independently caught the DEAD BEAT — worker CMD had no -B, no beat service, false celery_app.py "-B" comment → publish_queue_depth never fired → CeleryQueueDepth metric/dashboard-widget/PutMetricData-IAM all dead (verified vs Dockerfile.worker + ecs.tf). EdgeCase (file access) found 2 net-new HIGH that Blind's diff-only pass missed: kombu rediss://→CERT_NONE TLS downgrade (bypasses the celery_app.py CERT_REQUIRED hardening) + MISSING CeleryQueueDepth alarm (docstring+dashboard claimed threshold 50, no alarm existed). Auditor spot-checked EVERY compliance-doc fact vs real TF (TLS policy/Cognito 8h-30d/RDS-S3-ElastiCache encryption/line-refs) — all accurate; AC6/7/8 clean. 3 decisions resolved w/ user: add queue-depth alarm; KEEP alarm tuning as spec'd (eval=1/FILL low-traffic FP risk accepted, dev-only); throughput INCLUDE task_failed. 7 PATCHES APPLIED+verified: (1) -B beat wiring in Dockerfile.worker+compose + corrected false comment + variables.tf >1-replica WARNING [dev=1 safe; staging=2/prod=3 dedicated-beat-split DEFERRED]; (2) kombu Connection(ssl=_redis_ssl_opts) CERT_REQUIRED; (3) fan-out job_id set_tag before early return (AC4); (4) extracted REAL execution_tasks._tag_sentry_job() called by run_skill + rewrote 3 tautological tests to call it (mutation-verified); (5) dashboard error-rate widget period 60→300 to match alarm; (6) NEW aws_cloudwatch_metric_alarm.queue_depth (>50,300s,Max→SNS); (7) throughput filter task_completed||task_failed, metric→WorkerTaskProcessed. 2 DEFERRED (→deferred-work.md): staging/prod beat-split before go-live; uncommitted 7.3 Cognito files + stray terraform/tfplan.review hygiene (commit 7.3 separately + gitignore *.tfplan*). Gates: ruff ✅ · pytest tests/unit/workers 38/38 ✅ (3 rewritten Sentry-tag tests) · terraform validate+fmt ✅ · 3 changed modules import clean. T7 terraform apply remains OPERATOR-GATED (not live-applied). EPIC 7 COMPLETE. Prior: dev-story 2026-06-30 → review (claude-sonnet-4-6). T1 Sentry tags + 3 tests. T2/T3 CloudWatch TF. T4-T6 compliance docs. T7 OPERATOR-GATED. T8 AC5 verified.
  epic-7-retrospective: optional

  # ─────────────────────────────────────────────────────────
  # Epic 8: Access Control & Client Portal   (was Epic 7)
  # ─────────────────────────────────────────────────────────
  epic-8: in-progress   # KICKED OFF 2026-06-30 (create-story 8-1). Access Control & Client Portal.
  8-1-hierarchy-scoped-rbac-enforcement: done   # code-review 2026-06-30 (3-layer adversarial) + architect ruling. Blind Hunter's CRITICAL "asyncpg <@ ANY(ltree[]) bind broken" EMPIRICALLY DISPROVEN on live PG16. REAL finding (EdgeCase): scope wired ONLY to hierarchy get/update/delete+list — create routes, invocations/invoke, jobs leaked. 11 patches APPLIED: scope wired into invocations/invoke/jobs/create-routes; narrow except; unique-constraint+idempotent grant; schema Literals; N+1 batch-resolve; granted_at cleanup. ARCHITECT (Winston) RATIFIED the `unrestricted` internal-role bypass as intentional authorization policy (ADR in core-architectural-decisions.md): internal roles are org-global operators, AC3 covers client role only; bypass keys on role not org (sibling Cognito claims = equal trust; gating on org buys no security); executable guard added — create_grant rejects ma_tech/consultant grantees (422 INTERNAL_ROLE_NOT_GRANTABLE) since scoped-internal users are NOT foreseeable (revisit-trigger documented). +5 review tests. Gates: ruff ✅ 91 affected + 7 invocation-scope ✅ migration down/up ✅. 2 deferred (brittle _postgres_reachable; future node-move). See 8-1 Review Findings + ADR + deferred-work.md. # implemented + tested 2026-06-30. UserAccessGrant model + migration 0016 + access_service + hierarchy_scope dep + access-grants admin routes.
  8-2-ip-protection-client-api-surface-enforcement: review   # implemented + tested 2026-06-30. reject_client→404 guard on all 8 internal routers; new app/api/v1/client.py (POST /client/invocations + GET /client/jobs); ClientJobRead independent BaseModel (no JobRead inheritance); queue_invocation reused verbatim; presign_download loop mirrored. 16 new integration tests (test_client_surface.py) + OpenAPI lock (test_openapi.py). 529 unit ✅ integration ✅ (3 pre-existing ingest infra failures unrelated). ruff ✅. NO migration.
  8-3-client-portal-authentication-and-shell: backlog
  8-4-client-portal-skill-discovery-and-invocation: backlog
  epic-8-retrospective: optional

  # ─────────────────────────────────────────────────────────
  # Epic 9: Audit Log, Usage & Analytics   (was Epic 8; expanded 2026-06-08)
  # ─────────────────────────────────────────────────────────
  epic-9: backlog   
  9-1-audit-log-write-path: backlog   
  9-2-audit-log-query-api: backlog   
  9-3-audit-log-ui: backlog
  9-4-usage-and-value-analytics-api: backlog   # NEW 2026-06-08 (analytics overview + per-user)
  9-5-usage-and-value-analytics-ui: backlog   # NEW 2026-06-08 (Overview + By-User tabs)
  epic-9-retrospective: optional
