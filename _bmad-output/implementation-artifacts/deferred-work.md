# Deferred Work

Items deferred during reviews — real but not actionable in their originating story.

## Deferred from: code review of 15-3-per-skill-and-per-user-cost-analytics (2026-07-22)

- **Fan-out parent `outcome` mismatch (docstring + test encode `outcome=NULL`, production emits `outcome="success"`).** `_invocation_where`'s docstring (`analytics_service.py:66-71`) and `test_overview_excludes_admin_and_fan_out_parent_rows` (`test_analytics.py:373`) both assume the fan-out parent audit row carries `outcome=NULL` and is filtered out. It does not — `execution_tasks.py:702` writes the parent with `outcome="success"`, so it IS counted in `runs`/`total_invocations`/`success_rate` and now the new cost sums. Story 15.3's cost sum avoids double-counting the children's dollars ONLY because the parent's `InvocationResult.cost_usd` is an explicit `0` (`execution_tasks.py:674-680`, terminal-guarded to one parent row). No live bug today, but: (a) the docstring is a latent trap — anyone who later prices the parent roll-up double-counts every fan-out job's cost; (b) the test seeds a row shape (`outcome=None`) production never produces, so it gives false confidence. Fix = correct the docstring + re-seed the test's parent with `outcome="success"` + `cost_usd=0` and assert the cost/count deltas. Pre-existing (predates 15.3); this story's new cost path merely inherits the reliance.
- **Cost `SUM` over a non-DISTINCT `InvocationResult` join could double-count under task redelivery.** The three new grouped/scalar cost sums (`_top_skills`, `list_users`, `_token_cost`) sum `InvocationResult.cost_usd` over a `job_id` outerjoin with no `DISTINCT` guard. If two `outcome IS NOT NULL` audit rows ever shared one `job_id` (retry/redelivery writing a second invocation-outcome row), the single result row's cost would be counted per audit row. Latent only: Celery autoretry is deliberately disabled (`execution_tasks.py:20-22`), and the identical join already backs `func.count()` (`runs`) in the same query — so the duplication risk is a pre-existing property of the audit-row model that `runs` shares; cost inherits it, does not introduce it. Not actionable in 15.3.
- **No regression test pins the fan-out parent's $0 cost contribution.** The "fan-out avoids double-counting" invariant that Story 15.3's cost sum leans on is asserted only in prose. A test seeding a fan-out parent (`fan_out=True`, `outcome="success"`, `InvocationResult.cost_usd=0`) alongside priced child rows, asserting the per-skill/per-user/platform cost sums each child once and the parent zero, would lock it. Folds into the first item.

## Resolved by: 13-6-close-remaining-unaudited-mutations (2026-07-16)

- **RESOLVED.** The 12.5 "hierarchy CRUD + skill-attachment audit gap" entry
  (originally logged as a `deferred-work.md` note from Story 12.5, tracked as
  16 `exempt="not yet audited — known gap"` registry entries in
  `test_audit_coverage_guard.py`) is now closed. Story 13.6 threaded
  `acting_user_id` through all 12 hierarchy CRUD service functions + `unattach_skill`,
  added `admin.hierarchy_created/updated/deleted` (Organization category) and
  `admin.skill_attached/detached` (Organization category), and flipped all 16
  registry entries from `exempt` to `audited`. See Story 13.6 for the full
  implementation.

## Deferred from: 13-6-close-remaining-unaudited-mutations (2026-07-16)

- **Sandbox hardening remains out of scope — the Python-monkeypatch sandbox is still escapable.** `code_sandbox.py`'s own docstring/harness comment admits the `socket.socket`/`socket.create_connection` monkeypatch network block can be bypassed via `ctypes -> libc` raw syscalls, and that Epic 7 was slated to replace the harness with a syscall-level sandbox (seccomp/nsjail/gVisor) — **Epic 7 shipped without that replacement.** Story 13.6 added detection + audit + alarm for the network block signal the *current* sandbox does raise (`security.sandbox_network_blocked`, CloudWatch `sandbox_network_blocked` metric filter), but a sufficiently motivated skill using `ctypes` to open a raw socket would bypass the block entirely and emit no signal at all — detection covers the monkeypatch's blind spot, not the sandbox's own escape hatch. Also carried forward from Story 3.4's review: no `RLIMIT_NPROC` / fork-bomb guard, and the child interpreter exposes the full worker venv's site-packages to untrusted code. Revisit when/if a kernel-level sandbox replacement (the original Epic 7 scope) is scheduled. [velara-api/app/services/code_sandbox.py]

## Deferred from: code review of 13-3-audit-read-path-phi-disclosure (2026-07-16)

- **De-dupe key omits `artifact_kind`.** `record_artifact_disclosure`'s de-dupe query (`velara-api/app/services/audit_service.py:426-441`) filters on `event_type`/`user_id`/`org_id`/`artifact_ref`(+time window) but not `artifact_kind`. Not currently exploitable — no two artifact kinds share a ref format today (`str(job.id)`, `f"{job.id}:{index}"`, `str(child.id)`, `f"{skill_id}:{version}"` are all distinguishable by shape) — but structurally, a future call site reusing a ref format under a different kind within the 15-minute window would have its disclosure silently swallowed. Revisit if a new artifact kind is ever added whose ref format could collide with an existing one.
- **Export route writes two audit events under different `user_id`s for what's framed as one coherent action.** `EVENT_ADMIN_SKILL_EXPORTED` (`velara-api/app/services/skill_export.py:247`) uses `skill.created_by_user_id` (the skill's original author); the new `access.artifact_disclosed` event on the same route (`velara-api/app/api/v1/skills.py:211`) correctly uses `user.user_id` (the actual requester). These can differ (user A exports a skill created by user B) — pre-existing in `skill_export.py`, not introduced or touched by Story 13.3. The disclosure event's attribution is correct; the export-mutation event's attribution predates this story. Revisit if an auditor ever needs the export mutation's actor to match the actual requester.
- **No documented backfill/retroactive-coverage asymmetry for PHI disclosures minted before Story 13.3 shipped.** Task 6's S3 access logging explicitly covers URLs minted before this story (independent of app-level state), but the new `access.artifact_disclosed` application audit trail obviously cannot retroactively record pre-deployment disclosures — this asymmetry (infra logging is retroactive, app-level audit is not) isn't called out anywhere for a compliance reviewer doing an accounting-of-disclosures request that spans a pre-deployment date range. Documentation gap only; revisit if a real accounting-of-disclosures request ever needs to span dates before this story's deploy.

## Deferred from: code review of 12-4-duplicate-run-cost-warning (2026-07-07)

- **`find_recent_duplicate`'s `LIMIT 20` candidate window is shared across all distinct input variants at a given (org, skill, version, hierarchy_path).** A real duplicate can be pushed out of the top-20-most-recent-completed and silently missed if 20+ completions of *any* input variant occur at that path within the window — not just 20 completions of the *same* input, as the code comment implies. `velara-api/app/services/job_service.py`. Documented tradeoff (advisory-only, not an exhaustive audit); revisit if a high-frequency skill/context combination shows real missed duplicates.
- **`check_duplicate` re-does a full skill lookup + hierarchy resolution + candidate query + Python hash loop on every debounced call (~every 400ms of active editing), with no server-side rate limit beyond the client-side debounce.** `velara-api/app/api/v1/invocations.py`. Pre-existing since the story's original implementation; revisit if this load becomes a real concern at scale (a user with dev tools open could trivially hammer this endpoint since it's unthrottled server-side).

## Deferred from: code review of 12-2-audit-log-context-names (2026-07-06)

- **Name resolution is live, not point-in-time.** `resolve_hierarchy_names` (`velara-api/app/services/hierarchy_service.py`) resolves entity names against *current* DB state, not a snapshot at event time — a renamed (not deleted) client/project/study/location will retroactively change how every past audit row displays its context. Deferred: this is the story's stated design intent (a display concern layered on already-scoped rows, not a historical-name capture), not a bug. Revisit only if compliance requirements demand point-in-time name fidelity.
- **Run Console's ad-hoc name-chain builder has no `(unknown)`-style placeholder for missing ancestor levels.** `[clientName, projectName, studyName].filter(Boolean).join(' › ')` in `velara-web/src/features/run/components/RunConsole.tsx` silently drops undefined levels (e.g. a study-only job renders just the study name), indistinguishable from "no client/project exist." Inconsistent with the backend resolver's explicit `(deleted <type>)` marker for the audit surfaces. Pre-existing pattern, not introduced by 12.2's AC4 fix. Cosmetic; revisit if it causes real confusion.
- **Raw hierarchy path demoted to a hover-only `title` tooltip is inaccessible on touch devices.** `velara-web/src/features/audit/components/AuditLog.tsx` and `AuditDetailPanel.tsx` keep the raw ltree path only as a `title` attribute for debugging/compliance — unreachable without a mouse hover. Explicitly endorsed in the story's Dev Notes/Task 7 as the intended tradeoff (names read better than raw UUIDs as primary display). Revisit only if tablet/touch use by compliance staff becomes a real workflow.
- **No test exercises the batched resolver with multiple rows sharing one `hierarchy_path`, or a large mixed-type batch.** `velara-api/tests/integration/services/test_audit_service.py`'s three new Story 12.2 tests each cover a single relevant row; the per-type batched `IN (...)` query assembly/reassembly under multi-row, multi-type conditions is untested beyond the single-path happy path. Coverage gap, not a known defect.

## Production gap found post-10.2 manual smoke test (2026-07-03) — first-login is broken end-to-end

- **NOT A REGRESSION — a scope gap between Story 7.3 (Cognito swap) and Epic 10 (provisioning) that nobody connected.** Logged in as `ma_tech`, created a consultant user via the new 10.2 Users screen, opened the Cognito invite email, and tried to log in with the temporary password: the app showed **"Invalid username or password."** with correct credentials.
  **Root cause 1 (FE, blocking):** every user provisioned via `AdminCreateUser` (10.1's default invite mode) starts in Cognito's `NEW_PASSWORD_REQUIRED` state. Amplify's `signIn()` resolves this as `nextStep.signInStep === 'CONFIRM_SIGN_IN_WITH_NEW_PASSWORD_REQUIRED'` — a normal resolution, not a thrown error. `login()` (`velara-web/src/shared/utils/auth.ts:147-168`) never inspects `nextStep`; it falls through to `fetchAuthSession()`, gets no real ID token, throws a generic error, which `LoginPage`'s AC3 blanket-catch (`LoginPage.tsx:57-59`) flattens to the single "Invalid username or password." string — actively misleading here, since the credentials were correct and the real state is "you must set a new password." No FE code anywhere calls Amplify's `confirmSignIn()`.
  **Root cause 2 (BE, lower severity):** the invite email is Cognito's bare default template (`CognitoAuthProvider.create_user`, `auth.py:627-652`, no `MessageAction`/`ClientMetadata`/custom message template) — it has zero product context (no "you've been invited to Velara," no login URL), just a raw username + temporary password.
  **Resolution:** tracked as new **Story 10.4** (epic-10 doc + sprint-status `10-4-first-login-password-challenge`, backlog). Recommend sequencing before/alongside 10.3 (Provisioning ↔ Grant Handoff) — 10.3 cannot be meaningfully manually verified end-to-end until a freshly-provisioned user can actually complete first login. [velara-web/src/shared/utils/auth.ts:147-168, velara-web/src/pages/LoginPage.tsx:57-59, velara-api/app/integrations/auth.py:627-652]

## Production gap found same session (2026-07-03) — forgot/reset password entirely absent

- **User-reported, confirmed by grep: zero forgot/reset-password code exists anywhere in the FE.** No route, no page, no "Forgot password?" link on `LoginPage.tsx`, no Amplify `resetPassword`/`confirmResetPassword` calls anywhere (`aws-amplify/auth` exposes both; `auth.ts` only imports `signIn`/`signOut`/`fetchAuthSession`). Distinct from the 10.4 gap above: that one is Cognito's *forced* first-login challenge for freshly-provisioned users; this is a *self-service*, user-initiated flow for an already-active user who forgot their password later — different Amplify calls, different trigger, independently buildable.
  **Resolution:** tracked as new **Story 10.5** (epic-10 doc + sprint-status `10-5-forgot-reset-password`, backlog). Independent of 10.4 — can be built/tested in parallel, though both touch `LoginPage.tsx`/`auth.ts` so adjacent sequencing avoids merge friction. Note flagged at create-story: the success state after requesting a reset code must not reveal whether the email exists (avoid a user-enumeration oracle — same finding class raised in the 10.2 code review for `resend-invite`). [velara-web/src/pages/LoginPage.tsx, velara-web/src/shared/utils/auth.ts]

## Deferred from: code review of 9-3-audit-log-ui (2026-07-02)

- **Pre-0020 admin audit rows are left with NULL org_id and are now invisible to `GET /api/v1/audit`.** Migration 0020's backfill (`velara-api/app/db/migrations/versions/0020_audit_log_org_id.py:75-80`) only populates `org_id` for rows joining `invocation_jobs` on `job_id`; admin events (`job_id IS NULL`: grant/lifecycle/certification) written before 0020 stay NULL, and `audit_service.list_entries` now fences `org_id == :org_id` (NULL matches no caller). Confirmed live: the dev DB currently holds exactly 1 such orphaned admin row. Deliberately scoped & documented (0018 DROP+recreated the table so no pre-9.1 rows survive; no production data — product-confirmed; every go-forward admin write passes a real org_id). The row is unrecoverable (no org column to backfill from). Accept as a bounded dev-data gap; nothing to do unless legacy dev admin rows must be visible — in which case they'd have to be re-created, not backfilled.
- **Migration 0020's `ALTER TABLE ... DISABLE TRIGGER` for the backfill is PG15+-dependent.** Without `ONLY`, the disable only recurses to child-partition cloned row-triggers on PostgreSQL 15+. The project is pinned to `postgres:16-alpine`, so it works today (migration verified applied clean; `trg_audit_log_append_only` verified back to ENABLED afterward). Latent portability risk only: if ever run against PG<15, the append-only trigger would block the backfill UPDATE and the migration would abort. Revisit only if the Postgres floor ever drops below 15; otherwise no action.

## Deferred from: code review of 9-2-audit-log-query-api (2026-07-02)

- **Org-global admin audit events (`hierarchy_path="org"`) are unqueryable via `GET /api/v1/audit` — root-cause fix in the 9.1 write path.** Skill lifecycle transitions (`velara-api/app/services/skill_service.py:542`) and certifications (`velara-api/app/services/certification_service.py:261`) write audit rows with the literal single-label `hierarchy_path="org"`, but 9.2's org-root fence requires `hierarchy_path <@ 'org_org_vitalief'` — and `'org' <@ 'org_org_vitalief'` is FALSE (empirically confirmed against live Postgres; ltree is whole-label, not prefix). So these two admin-event classes are invisible to every caller including internal operators (violates the AC1/AC6 intent + the Part 11 compliance trail). **Decision (Developer, 2026-07-02): fix the write path, not the 9.2 query fence** — lifecycle/certification (and any other org-global admin write) should record `_org_segment(org_id)` (the real org root) instead of the literal `"org"`, so the rows sit inside the org subtree and the fence works uniformly with no special-case. Follow-up scope: (1) change the write callers to use `_org_segment(org_id)`; (2) audit for other `hierarchy_path="org"` literals and the `"org"` convention (model comment at `app/models/audit.py:98-99` documents it); (3) a data backfill for existing `"org"` rows (or a transitional dual-match `... OR hierarchy_path = 'org'` in the query until backfilled); (4) add a test asserting an unrestricted internal caller sees an org-global admin row via the endpoint. Author as a small Epic 9 follow-up story before/with 9.3 (the Audit UI, which will otherwise never display lifecycle/certification events).
- Deep-page OFFSET pagination on `GET /api/v1/audit` is effectively unbounded: `page` capped at `le=100_000` × `per_page` up to 200 permits `.offset(~20,000,000)` (`velara-api/app/services/audit_service.py:239`), forcing a full-partition scan per deep-page request, plus a `func.count()` full-filtered-set scan on every page. Pre-existing pattern inherited verbatim from `list_jobs`/`jobs.py` (identical `le=100_000` + offset shape) — NOT introduced by 9.2. Fix org-wide (jobs + audit + any other offset list) if/when deep-page abuse or cost shows up: lower the `page` cap, switch to keyset/seek pagination on `(created_at, id)`, or cache the count.

## Deferred from: code review of 6-3-methodological-certification-workflow-ui (2026-06-27)

- `useCertifications` calls `listCertifications({ skill_id })` with no `per_page` → backend defaults to 50, ordered `certified_at ASC` (`velara-web/src/features/certification/hooks/useCertifications.ts:17-23`; `velara-api` list returns oldest-first). On a skill that has churned through >50 cert records, the *current* version's (newest) certs land on page 2+ and are absent from `items`, so `CertificationDetail` derives `isTechCertified`/`isMethCertified = false` and re-enables both keys → invites a duplicate POST (then 422 ALREADY_RECORDED). Pre-existing — hook signature unchanged by 6.3. Fix: request `per_page` high enough, or filter server-side by `current_version_id` for the detail view.
- Unguarded `new Date(cert.certified_at).toUTCString()` in the Part 11 manifestation lines (`velara-web/src/features/certification/components/CertificationScreen.tsx:401,411`) and `.toLocaleDateString()` in the KeyCard — a malformed/empty `certified_at` renders "Invalid Date" in a regulatory signature record rather than failing loudly. Server-controlled ISO value today (low risk). Guard with `Number.isNaN(d.getTime())`.
- The methodological/technical modal's `!user` ("Sign-in required") re-auth dialog has `role="dialog" aria-modal` but no focus move/trap into it on mount — keyboard/screen-reader focus can remain behind the modal. Verbatim clone of the 6.2 technical modal (pre-existing); fix both, or render a single dialog shell.
- Cert POST (`POST /api/v1/certifications`) returns an identical 201 whether or not auto-advance fired, including the D3 swallow path where the skill silently stays `internal_ready` (`velara-api/app/services/certification_service.py:206-230`). An API consumer can't distinguish "advanced to client_ready" from "still internal_ready" without a follow-up GET. Deferred to Epic 8 / 6.4 (resolved from decision-needed): response shape stays per the as-built contract; the FE re-reads via cache invalidation. Revisit if a non-FE consumer needs the signal, or to surface a "review required before client-ready" notice.

## Deferred from: code review of 1-2-velara-web-project-scaffold (2026-06-04)

- PHI sanitizer uses greedy substring key-match (`'name'`/`'mail'` matched via `includes()`) in `src/shared/utils/sentry.ts:7-12,24`, over-redacting legitimate Sentry diagnostic fields (`transaction`/breadcrumb `name`, `username`, `filename`, `hostname`). Deferred (resolved from decision-needed): kept for parity with velara-api `sanitize_phi`; **low real-world impact** — PHI safety outweighs losing some Sentry field detail.
- Bearer token stored in `sessionStorage` (XSS-exfiltratable); `src/api/client.ts` reads storage directly rather than through `src/shared/utils/auth.ts`, so the Amplify swap is not as isolated as the `auth.ts` docstring claims. Address when wiring the dev-auth shim (**Story 1-4**) — route the token through `auth.ts` so the later Cognito swap (**Story 7-3**) stays contained. _(Renumbered 2026-06-05; was "Story 1.5".)_
- No catch-all `path="*"` 404 route at the top level (`src/App.tsx`) or in the internal/client shells (`src/routes/internal.tsx`, `src/routes/client.tsx`) — unknown URLs render a blank screen. Add a NotFound element / default redirects.
- `baseURL: VITE_API_URL` in `src/api/client.ts:11` has no guard; when the env var is missing, requests silently hit the relative origin. Add a startup validation/warning.
- Sentry `browserTracingIntegration()` in `src/shared/utils/sentry.ts:42` has no `tracePropagationTargets`, so trace headers attach to all outbound requests (incl. cross-origin). Scope propagation targets.
- `tsconfig.node.json` is referenced by no npm script, so `vite.config.ts`/`vitest.config.ts` are never typechecked (documented workaround for TS6306/6310; AC7 was scoped to `src`). Consider a `tsc -b` / project-references setup to cover config files.
- The `@` path alias is defined three different ways across `tsconfig.json`, `vite.config.ts` (`'/src'`), and `vitest.config.ts` — resolution-divergence/maintenance risk. Unify on one mechanism.
- `vitest.config.ts` hand-rolls plugins/aliases instead of `mergeConfig`-ing `vite.config.ts` (no Tailwind plugin in tests; configs can drift). Merge from the base Vite config.
- ESLint uses legacy `.eslintrc.cjs` with no type-aware linting (`parserOptions.project` unset) → shallow lint. Consider flat config + type-aware rules.
- `src/pages/LoginPage.tsx` redirect reads `state.from.pathname` only (drops search/hash; no guard if `from === '/login'`). Address when building the login form for the dev-auth shim (**Story 1-4**); revisit at the Cognito swap (**Story 7-3**). _(Renumbered 2026-06-05; was "Story 1.5".)_

## Deferred from: code review of 1-3-local-dev-environment-and-provider-abstractions (2026-06-09)

- `SecretsManagerProvider` region is sourced from `S3_REGION` (`app/integrations/secrets.py:99`) — no dedicated AWS/secrets region setting. Cross-domain coupling that breaks if Secrets Manager and S3 live in different regions. Deferred: the `aws` secrets backend is unused until **Epic 7**, which will introduce real AWS region config — add a dedicated region setting (e.g. `AWS_REGION`/`SECRETS_REGION`) then.
- `S3StorageProvider.get()` does an unbounded `response["Body"].read()` into memory (`app/integrations/storage.py:92-94`) — memory/DoS risk for large HIPAA documents; the Protocol offers no streaming variant, forcing every consumer to materialize whole files. Deferred: storage consumers (ingest/output pipelines) are **Epic 3** (Stories 3.2/3.6); add a streaming `get`/`get_stream` when they arrive.

## Deferred from: code review of 1-4-dev-authentication-shim (2026-06-09)

- `get_auth_provider()` is `@lru_cache`'d with no cache-invalidation seam (`app/integrations/auth.py:1171`). Correct for immutable boot-time config and consistent with the storage/secrets provider pattern, but the Cognito swap (**Story 7.3**) and any hot-reload path will need an explicit `cache_clear()` hook. Revisit at 7.3.
- CORS config omits `expose_headers=["X-Request-ID"]` (`app/main.py:817-823`), so the browser cannot read the server-stamped request id from responses. The frontend only *sends* `X-Request-ID` today, so non-breaking — add `expose_headers` when client-side trace surfacing is wanted.
- `DevAuthProvider.validate_token` passes no `leeway=` to `jwt.decode` (`app/integrations/auth.py:1119-1125`) — zero clock-skew tolerance. Moot for the single-process dev shim; add tolerance for distributed **Cognito** validation in Story 7.3.
- Post-login `navigate(from)` target (`src/pages/LoginPage.tsx:89-96`) is unvalidated beyond the `from === '/login'` guard — no allow-list against external/malformed paths (open-redirect style). Low risk because `from` is captured internally by `RequireAuth`; harden if untrusted state can reach it.
- Request-id interceptor `crypto.randomUUID?.()` (`src/api/client.ts:20`) guards a missing *method* but not a missing global `crypto` object (insecure HTTP context / older JSDOM). Pre-existing (not introduced by this story); add a `typeof crypto` guard if non-secure contexts are in scope.
- `LoginPage` `dev-users` fetch ignores HTTP status — no `r.ok` check (`src/pages/LoginPage.tsx:99-105`), so a 500 is indistinguishable from an empty seed list. Works today only via the `?? []` fallback; add an explicit status check for clearer degradation.

## Deferred from: code review of 1-5-per-route-browser-tab-title (2026-06-09)

- No catch-all route + no unmount cleanup in `useDocumentTitle` → unmatched URLs (`/internal/unknown`, `/client/unknown`) keep a stale/default tab title, and a guarded page's title can survive into a no-title destination (`src/routes/internal.tsx`, `src/shared/hooks/useDocumentTitle.ts:21`). Explicitly acknowledged in Story 1.5's "Unknown-route edge" Dev Note. **Same root as the existing 404-route item above** — when the deferred `NotFound` route lands it should call `usePageTitle('Not Found')`, closing both.
- `analytics` route is declared `path="analytics"` (no `/*` splat) while every sibling uses `/*` (`src/routes/internal.tsx:49`), so `/internal/analytics/sub` matches nothing → stale title. Pre-existing (Story 1.5 only wrapped the existing element). Use `path="analytics/*"` for parity when analytics gains sub-routes.
- NavTabs selection is local `useState('engagements')` and never navigates, and tab id `registry` ≠ route segment `skills` (`src/routes/internal.tsx:24-29`, `src/shared/components/NavTabs.tsx`). Today the active tab and the routed title can disagree; once tabs are wired to the router, clicking "Skill Registry" would navigate to `/internal/registry` (no match → stale title). Align tab ids to route paths when wiring navigation.
- `buildTitle` filters falsy parts but does not `.trim()`, so a whitespace-only part yields padding artifacts (`'  '` → `'    · Velara'`; `buildTitle('Study','   ')` → `'Study ·     · Velara'`) — `src/shared/hooks/useDocumentTitle.ts:15`. Latent: the documented Epic-4 seam `usePageTitle(entity?.name, 'Study')` is exactly where an untrimmed backend `entity.name` enters. Trim parts before filtering when Epic 4 consumes the seam.
- Index `<Navigate>` redirect routes (`/internal`, `/client`, `/`) have no title assertion (`src/routes/internal.test.tsx`, `src/routes/client.test.tsx`). Leaf targets are already covered, so low value; add redirect-landing title tests if redirect regressions become a concern.

## Deferred from: code review of 1-6-apply-vitalief-brand-theme (2026-06-09)

- AppBar dark-bar accent colors are hardcoded hex literals (`#6a9eb8`, `#8ab8cc`, `#d5e8f2`, `#6da8bc`, and the status dots `#6fd6b8`/`#7cc0d8`) in `src/shared/components/AppBar.tsx` rather than `@theme` design tokens, so they are invisible to any future token-driven re-theme. Pre-existing pattern — the dark-bar text colors were already hardcoded literals before V3; this story only swapped the literal values (evergreen → blue-grey). Promote them to `@theme` tokens (e.g. `--color-onbar-*`) when the AppBar next sees structural work, so the next brand swap is a single-file token change like the rest of the palette.

## Deferred from: code review of 2-1-skill-data-model-and-registry-api (2026-06-09)

- Skill registry integration tests — the 13 DB-backed tests in `tests/integration/api/test_skills.py` (the executed coverage for AC 2/3/5/6) skip unless Postgres+MinIO are reachable; confirmed SKIPPED on the host (`DATABASE_URL`/`S3_ENDPOINT_URL` unset). The "99 tests passing" completion note only holds inside `docker compose exec api pytest`. The skip-guard itself is by-design (mirrors `test_storage_roundtrip.py` per the story's Dev Notes), so this is a verification gap, not a code defect: confirm the integration suite ran green in Docker (and ideally wire it into CI so the skip can't masquerade as a pass) before relying on the coverage claim.
- `Skill.updated_at` uses SQLAlchemy `onupdate` (ORM-only) with no DB-level trigger or `server_onupdate` (`app/models/skill.py:77-82`; migration `0002` creates a plain `DateTime` column). Any raw-SQL `UPDATE skills ...` (data migration, bulk op, manual fix) will leave `updated_at` stale. Acceptable while every write goes through the ORM service layer; add a DB trigger / `server_onupdate` if/when bulk or raw writes are introduced.

## Deferred from: code review of 2-2-skill-metadata-tags-and-visibility-designations (2026-06-09)

- `update_skill_metadata` has an unlocked read-modify-write window (`app/services/skill_service.py:472-484`): `get_skill` reads the row without `with_for_update`, the `lifecycle_state == "retired"` guard is checked against that stale read, and the commit can land after a concurrent transition to `retired`. Lost-update / guard-bypass window. Deferred as pre-existing pattern — the metadata PATCH mirrors the non-locking style used elsewhere; `create_version` already uses `for_update=True` and is the template if/when this is hardened.
- `name` and `author` are stored un-stripped (`app/services/skill_service.py:230`); a whitespace-only `name=" "` satisfies `min_length=1` and persists. Pre-existing from Story 2.1 (the 2.2 description-strip finding is patched separately). Deferred — apply the same trim treatment to name/author when 2.1's create path is next touched.

## Deferred from: code review of 2-3-paired-skill-derivation-lineage (2026-06-09)

- `parent_version` "0.0.0" defensive fallback can be silently persisted into a child's `derived_from` if a parent ever lacks a `current_version` (`app/services/skill_service.py:610`). Narrow trigger — a freshly `create_skill`-ed parent always has 1.0.0 — but the bogus lineage is stored rather than rejected. Deferred: narrow trigger / defensive path. Consider raising instead of defaulting if an inconsistent parent is ever observed.
- `get_derived_skills` lineage query is unbounded (no `LIMIT`) and runs on every `GET /{skill_id}` detail fetch (`app/services/skill_service.py:671`) — fan-out for a parent with many derived children. Deferred: consistent with other unbounded list endpoints in the codebase; revisit alongside registry pagination (Story 2.4/2.5).
- Acknowledge-during-publish race: `review_required` is a plain boolean with no version/sequence token, so an `acknowledge-parent-update` that lands after a *second* parent publish clears the flag for the newer un-reviewed change too (`app/services/skill_service.py:704`). Deferred: matches the AC5-defined boolean-clear semantics; adding sequencing is out of scope for this story.

## Deferred from: code review of 2-4-skill-registry-ui-browse-and-detail (2026-06-10)

- Non-404 API errors render a developer-facing message in the Skill Registry UI. `getErrorMessage` (`velara-web/src/shared/utils/errors.ts`) is still a stub that only unwraps `Error.message`, so a 500/network failure surfaces raw axios text (e.g. "Request failed with status code 500") in `SkillDetail`/`SkillRegistry`. The `SKILL_NOT_FOUND` branch is correct; only the generic-error fallback is affected. Deferred — the stub is a shared cross-cutting util from an earlier story; give it proper axios-error/code unwrapping when error handling is next hardened across the web app.
- No catch-all route under `/internal/*`; the skills route was narrowed from a `skills/*` splat to exact `skills` + `skills/:skillId` (`velara-web/src/routes/internal.tsx:57-58`). A malformed deep link such as `/internal/skills/<id>/extra` now renders a blank `<main>` — there is no app-wide `path="*"` 404. Deferred — no in-app link produces such a path; add a global not-found route as a separate cross-cutting concern.

## Deferred from: code review of story-3.1 (2026-06-10)

- No reaper/timeout for jobs stuck in `running` — if the worker process dies (OOM/SIGKILL/eviction) between `mark_running` and the terminal transition, the job is durably `running` forever. No `task_time_limit`/`task_soft_time_limit` on the Celery app, no scheduled sweep task, no `started_at`-staleness check in `get_job`. Deferred: infrastructure/operability concern beyond 3.1's scaffolding scope — add a Celery time limit + a reaper sweep (or rely on a future `running→cancelled` best-effort path) when execution runtimes land (3.3-3.5) or in an ops-hardening story. [app/workers/celery_app.py]
- `hierarchy_path` empty/malformed-ltree string → opaque DB `DataError` (500/task-failure) instead of a domain validation error — `create_job`/`record_entry` accept `hierarchy_path: str` with no ltree-format validation; `LtreeType` does no format checking. Latent: every current caller passes the literal `"org"`; real hierarchy paths only arrive in Epic 4. Deferred: add ltree-label validation when Epic 4 populates engagement-hierarchy paths. [app/services/job_service.py, app/services/audit_service.py]

## Deferred from: code review of 3-2-document-ingest-pipeline (2026-06-10)

- Bucket-default SSE (AES-256 encryption at rest) is not configured anywhere in this changeset. The per-request `ServerSideEncryption` header is correctly omitted from `presign_upload` (avoiding the `SignatureDoesNotMatch` trap on direct browser PUTs), but no bucket-default encryption is set — the MinIO bootstrap in `docker-compose.yml` runs only `mc mb --ignore-existing` with no `mc encrypt set`, and there is no IaC/`PutBucketEncryption` policy. Per Story 3.2 Dev Notes ("AES-256 server-side encryption"), the real bucket-encryption policy "lands with real bucket provisioning in Epic 7." Deferred — out of this story's file scope; ingest objects are unencrypted-at-rest until Epic 7 (or an environment-level bucket policy) supplies bucket-default SSE. [docker-compose.yml MinIO bootstrap / Epic 7 IaC]

## Deferred from: code review of 3-3-prompt-based-skill-execution (2026-06-11)

- Unbounded concatenated LLM context — `_run_prompt` appends every referenced document's full parsed text plus the stringified `inputs` dict into `user_content` with no token/byte cap or truncation before calling Claude. An oversized payload either exceeds the context window (API 400/413) or silently inflates input-token cost. Deferred — multi-file/context-size governance is later-story (3.7 fan-out) scope per the Story 3.3 Scope Boundary; phase-1 prompt skills are short-to-moderate. Revisit when adding a `CONTEXT_TOO_LARGE` fast-fail / token counting. [app/services/execution_service.py:156-178]
- `get_llm_provider()` is `lru_cache`d on a possibly-empty `ANTHROPIC_API_KEY` in dev — the provider is built once per process from `settings.ANTHROPIC_API_KEY`; with an empty key (allowed in dev, fail-fast in staging/prod) the first real `complete()` fails late as `AuthenticationError` (401) → mapped to `PROMPT_EXECUTION_ERROR`, and setting the env var later in the same process has no effect (cached). Deferred — dev-only (staging/prod is guarded), cosmetic error-code clarity. Consider a clearer "ANTHROPIC_API_KEY not set" signal or distinct 401 mapping. [app/integrations/anthropic_client.py:128-137]

## Deferred from: code review of 3-4-code-based-skill-execution (2026-06-11)

- No `RLIMIT_NPROC` and no block on `subprocess`/`os.fork` in the sandbox — untrusted skill code can fork-bomb the worker host; per-process RLIMIT_CPU/RLIMIT_AS do not bound aggregate child resource use, and a grandchild that re-`setsid`s can escape the `killpg` group. Deferred: kernel/process-level isolation (nsjail/gVisor/seccomp, RLIMIT_NPROC enforcement) is explicitly assigned to **Epic 7** by the Story 3.4 Scope Boundary. The Phase-1 contract is "no accidental/casual abuse," not adversarial syscall containment. [app/services/code_sandbox.py:121-137]
- Child interpreter exposes all installed third-party packages to untrusted code — the sandbox launches `sys.executable` (the worker venv), so even with the env scrubbed of credentials, untrusted skill code can `import boto3 / sqlalchemy / DB drivers`. `-I` blocks `PYTHONPATH`/user-site but not the venv's own site-packages. Deferred: restricting the importable module surface is kernel-sandbox/**Epic 7** territory (a minimal interpreter image or seccomp policy); env scrubbing already removes the credentials those packages would need. [app/services/code_sandbox.py:197]
- `RuntimeError` ("no current version") and `build_context_input` failures inside `_run_code` map to the generic `SKILL_EXECUTION_ERROR` rather than a distinct infra/version error code. Deferred, pre-existing: the same TOCTOU-only pattern is inherited from `_run_prompt` (Story 3.3); the invocation route pre-validates both the current version (`NoCurrentVersionError`) and file-ref readiness, so the window is narrow. Revisit if/when error-code granularity for infra-vs-skill failures is hardened across the execution service. [app/services/execution_service.py:274-295; app/workers/execution_tasks.py:251-295]

## Deferred from: code review of 3-5-hybrid-skill-execution (2026-06-11)

- No size cap on tool-result content fed back into the conversation — a multi-MB tool return is appended to `messages` and re-sent on every subsequent `create_message` (the whole `messages` list is resent each turn), causing quadratic token growth / 413 / cost blowup. The only bound is the 16MB framing cap in `_recv_frame`. Deferred — needs a result-size policy (truncate + signal truncation to Claude) that warrants its own design rather than an inline patch. [app/services/execution_service.py:535]
- `_send_frame` request-write has no timeout — only the read side (`_recv_frame`) is deadline-protected. A very large `tool_use.input` from Claude plus a wedged child (still inside a prior `func(**args)`, not back at its read loop) can block `os.write` on a full request pipe indefinitely, hanging the worker thread. Typical tool args are small (well under the pipe buffer), so this is latent. Deferred — needs a `select`-bounded / non-blocking write path symmetric with the read side. [app/services/code_sandbox.py:722-735]
- Diff under review bundles all of Story 3.4 as new code — baseline `7dd75cf` ("3-3-prompt-based-skill-execution") contains no `code_sandbox.py`, no `_run_code`, and the older Celery decorator; this changeset therefore introduces 3.4's sandbox primitives, `_run_code`, the `1260/1320` decorator, and the `EXECUTION_*` config alongside the 3.5 work. The 3.5 additions are correct and additive, but the spec framed `code_sandbox.py` as a 3.4 "MODIFY (additive)" — implying 3.4 was already merged. Not a 3.5 code defect; a commit-hygiene/baseline-tracking note. Deferred — confirm whether Story 3.4 was ever committed and commit/separate the 3.4 vs 3.5 work appropriately so the git history matches the sprint record. [app/services/code_sandbox.py (whole file); app/services/execution_service.py `_run_code`; app/workers/execution_tasks.py run_skill decorator]

## Deferred from: code review of 3-6-branded-output-generation (2026-06-11)

- `load_brand_assets` is wrapped in `@lru_cache(maxsize=1)` keyed on the process-singleton brand storage provider, so the first call's result lives for the worker's lifetime. In dev, `_load_asset_optional` swallows a *transient* S3 failure on that first branded job and returns an all-`None` `BrandAssets`, which is then cached permanently — every subsequent job in that worker silently renders unbranded (Helvetica, no logo, blank PPTX) even after S3 recovers, with no invalidation short of a worker restart. Prod is unaffected (the required-asset path raises before the `return`, so failures aren't cached). Deferred — the fix (a TTL, an error-bypass, or cache-clear hook) is a design choice rather than an unambiguous patch. [app/services/output_service.py:181]
- `generate_pptx` builds slides on `prs.slide_layouts[-1]` with the comment "last layout is typically blank" — an admitted assumption. With a real Vitalief slide template (the locked AC3 path), the last layout is whatever the template author placed last: if it carries title/content placeholders, the rendered slide shows empty placeholders *plus* the manually-added overlapping text boxes; a template shipping zero layouts raises `IndexError`. There is also no PPTX footer/logo. Deferred — depends on the Vitalief kickoff slide template, which is not yet provided; revisit when the template asset lands. [app/services/output_service.py `generate_pptx`]
- `_hex_to_rgb`/`_hex_fill` slice exactly 6 hex digits (`int(h[0:2],16)`, etc.) and `lstrip("#")` strips all leading `#`s. Brand colours are stable 6-digit `#RRGGBB` literals today, so this works; but `BrandAssets` exposes the colours as unvalidated `str` fields, so a future 3-digit (`#0a8`), `rgba(...)`, or named-colour brand token would raise `ValueError`/`IndexError` mid-render, surfacing only as a generic `OUTPUT_GENERATION_FAILED`. Deferred — hardening, not an active bug; add hex-token validation if/when the palette format changes. [app/services/output_service.py docx/pptx colour helpers]
- No size cap on the runtime's final `content` before rendering. Prompt/hybrid final text can be large; reportlab builds one `Paragraph` flowable per line and openpyxl one row per line (worksheet caps at 1,048,576 rows → raises on overflow), all in-memory in the Celery worker with no `MAX_OUTPUT_CHARS`/row cap and no render timeout. Deferred — same class as the existing "unbounded context size" item from Story 3.3; address with a shared output-size policy. [app/services/execution_service.py `_persist_output`; app/services/output_service.py generators]
- `update_skill_metadata` applies updates via a blind `setattr` loop with no `OutputFormat` validation. `output_format` is validated only at create time (via the `SkillCreate` Literal); if the metadata edit surface ever exposes `output_format`, an invalid value could be persisted (and `SkillRead.output_format` is typed plain `str`, so it would read back unvalidated). Deferred — the edit surface for `output_format` was intentionally scoped out of Task 1; add Literal validation when the edit route exposes the field. [app/services/skill_service.py `update_skill_metadata`]

## Deferred from: code review of 3-8-external-api-credential-and-connector-framework (2026-06-12)
- (F3) `S3IngestConnector.fetch` raises `NotImplementedError` — server-side async I/O (build_context_input) cannot be called from a sync connector method; full async wrapping deferred to Epic 5. [app/services/connectors.py:154-161]
- (F4) `S3OutputConnector.format` performs synchronous blocking I/O (brand storage load) from a sync Protocol method — pattern is acceptable while connectors are not yet invoked from async execution paths; revisit when connector invocation is wired to execution service. [app/services/connectors.py:192-207]
- (F5) Credential resolution count logged at INFO level — low severity; acceptable per spec R4 ("log names/counts only"). Revisit if log verbosity becomes a compliance concern. [app/services/execution_service.py:221]
- (F9) Connector registry is a module-level global with no test teardown — tests that register fake connectors permanently pollute the registry for the process lifetime; no production impact but could cause false test passes if a future test checks registry contents. Add a fixture to restore registry state. [tests/unit/services/test_connectors.py]
- (F12) `secrets_provider=None` is a silent bypass path — intentional design decision (Dev Agent Record §3) so unit tests need no mock; production always passes `get_secrets_provider()`. If a future direct caller omits the provider, skills with `requires` silently execute without credentials. Consider adding a guard log or assertion when `requires` is non-empty and provider is None. [app/services/execution_service.py:232]
- (F14) Skill code can exfiltrate injected credential values via stdout — R6 explicitly defers network/exfiltration gating and output-scrubbing to the Epic-7 kernel sandbox. In-scope for 3.8 only: credential is available; runtime stays network-blocked; credential scrubbing from output is Epic 7. [app/services/code_sandbox.py]

## Deferred from: code review of story-4.1 (2026-06-12)

- PATCH cannot null out optional fields, and `{"description": ""}` silently blanks the field while satisfying the "at least one field" rule. The `update_*` service functions use the `if x is not None:` pattern and the `_at_least_one` model validator only checks `is None`, so an empty-string body passes validation and overwrites data, while a `null` value can never clear an existing optional field. This is a broader PATCH-semantics design question — the skills route solves it with `model_dump(exclude_unset=True)` + a `NoFieldsToUpdateError`. Adopt that pattern across hierarchy update endpoints when partial-update fidelity is needed. [app/services/hierarchy_service.py:158-174 (update_client) and the project/study/location equivalents; app/schemas/hierarchy.py _at_least_one validators]

## Deferred from: code review of story-4.2 (2026-06-12)

- 422 `details` mapping only handles the `name` field; a `description` (or any non-`name`) server validation error is swallowed — `applyApiErrors` (EngagementsScreen.tsx:462-466) sets `nameError` only for `fe.name`, else a generic `formError`. Canonical `SkillForm.mapDetailsToFieldErrors` maps all known fields. Low impact for this 2-field form (description currently has no server constraint that 422s); revisit when more fields are added to the entity modal. [src/features/engagements/components/EngagementsScreen.tsx:462-466]
- Tree/modal accessibility semantics incomplete — tree rows are `role="button"` siblings without `role="tree"/"treeitem"/"group"` or `aria-controls`; modal form inputs use styled `<div>` captions instead of `<label htmlFor>`; validation error text has no `role="alert"`/`aria-live`. Not a regression (the skills feature has comparable gaps). Better addressed as a feature-wide a11y hardening pass than piecemeal here. [src/features/engagements/components/EngagementsScreen.tsx:616-684 (tree), 536-569 (modal form)]

## Deferred from: code review of 4-4-hierarchy-navigation-and-breadcrumb-context (2026-06-12)

- Client-side study/location cascade delete is non-atomic (`src/features/engagements/components/EngagementsScreen.tsx:1494-1497`). `confirmDelete` deletes child locations in a loop then the study; a mid-loop non-404 failure (403/500) leaves some locations permanently deleted, the study intact, and the user in a half-deleted state. Inherent to the current API (no server-side cascade endpoint) and the 4.3-established 404-tolerant R-F1/R-F2 pattern; a true fix requires a backend cascade endpoint, which is out of scope for this frontend-only story. Revisit if/when the hierarchy API gains cascade-delete support.

## Deferred from: code review of story-3.7 (2026-06-12)

- Fan-out parent job has no `celery_task_id` (the `fan_out` branch in `app/api/v1/invocations.py` returns before the `celery_task_id` assignment tail), so `cancel_job`'s best-effort `revoke` is a no-op for fan-out parents and never reaches the children (each has its own task id). Cancellation of an in-flight fan-out is effectively unsupported. Not an AC of story 3.7; cancel UX belongs to Epic 5 (Run Console). Revisit when wiring fan-out cancel.
- Skill `scope` (project/study) is not enforced against the requested study/location at the invocation endpoint — within an org, any caller can run a study/project-scoped skill against any location/study. Least-privilege gap the skill model intends. Deferred to Epic 8 (hierarchy-scoped RBAC enforcement), which is the explicit subject of that work; out of scope for the execution-engine story 3.7. Enforce alongside RBAC there. [`app/api/v1/invocations.py`]

## Deferred from: code review of 5-3-run-console-skill-first-mode (2026-06-13)

- `inputs`/`file_ref_ids` are never collected or sent by the Run Console, despite skills declaring an `input_schema`. Explicitly deferred per "5-3 DEFERS" (file-upload / schema-driven inputs → later story). No 5-3 AC requires them; wire when the run-config inputs card lands. [`src/features/run/components/RunConsole.tsx`, `src/api/jobs.ts`]
- `runMode` (Zustand `useRunStore`) is written only inside the mutation `onSuccess`, so until the first successful submit it holds whatever a prior run left in the module-global store. No consumer reads `runMode` before Story 5-4 (real job polling), which owns any store reshape — latent only. Set `runMode` on console mount when 5-4 wires the status panel. [`src/features/run/components/RunConsole.tsx:456,655`]
- `friendlyInvocationError` falls through to `getApiMessage(err)` / `err.message` without coercing to a string; a backend 422 whose `error.message` is a non-string (object/array) would render as `[object Object]` in the inline error. Pre-existing in the Story 5-2 `errors.ts` pattern, not introduced by 5-3; Uncertain whether the backend ever returns a non-string message. Coerce to string in a future error-handling hardening pass. [`src/shared/utils/errors.ts:51`]

## Deferred from: code review of 5-4-job-status-polling-and-output-display (2026-06-15)

- (F5) No pagination UI in Jobs History — `useJobs({ per_page: 50 })` is hardcoded; users with >50 jobs see `total` count but have no load-more/page controls to reach older jobs. Add pagination controls or infinite scroll when 5-6/5-7 pagination patterns land. [`src/features/run/components/JobsHistory.tsx`]
- (F9) No dismiss/clear affordance for completed or cancelled terminal jobs in `JobStatusPanel` — once a job completes, the panel is permanently occupied with no close button; only "Try again" (failed only) or submitting a new job clears it. Wire a dismiss button alongside cancel UI in Epic 8. [`src/features/run/components/RunConsole.tsx — JobStatusPanel`]
- (F11) `refetchInterval` test reimplements terminal-status logic inline rather than calling the actual hook — a regression in `useJob.ts` refetchInterval condition would pass green. Improve when hook internals can be exercised directly (e.g. via TanStack Query test utilities). [`src/features/run/components/RunConsole.test.tsx`]
- (F12) `useJobs` 30s `staleTime` means a newly submitted job won't appear in Jobs History for up to 30 seconds — no `invalidateQueries(['jobs'])` call after successful invocation mutation. Add invalidation in the mutation's `onSuccess` when tightening History freshness. [`src/features/run/hooks/useJob.ts — useJobs`]

## Deferred from: code review of 5-5-rest-api-documentation-and-openapi-spec (2026-06-25)

- CI `openapi` job uses unpinned `pip install -e ".[dev]"` (`.github/workflows/ci.yml:31`), so a FastAPI/Pydantic resolver bump can change `app.openapi()` output and flip the staleness gate red on unrelated PRs (or green on a version-mismatched spec). **Pre-existing/project-wide:** the `lint` and `test` jobs share the same unpinned install — not introduced by 5-5. A `uv.lock` exists in the working tree; a follow-up could pin all CI jobs against it for reproducible installs.

- **AC4 not yet met — `docs/api-spec.json` is untracked (not committed).** The spec, `scripts/export_openapi.py`, and `tests/integration/api/test_openapi.py` are `??` (untracked) in the working tree. `git diff --exit-code docs/api-spec.json` cannot fail on an untracked file (reproduced: a `version: 9.9.9-STALE` perturbation still passed the gate), so AC4's "committed spec + stale-spec-fails-build" requirement is inert until the 3 files are committed. Spec content is correct/byte-identical to a fresh export. Deferred to the dev's own commit step (reason: committing 5-5 files handled outside this review). Once committed, also apply the CI gate-hardening patch (`git add --intent-to-add` / `git status --porcelain`) to close the residual untracked blind spot.

## Deferred from: code review of 5-6-skills-list-api-pagination (2026-06-25)

- **COUNT and ROWS are two separate round-trips → `total` and `items` can disagree under concurrent writes** (`app/services/skill_service.py:364-374`). The filtered `func.count()` and the paginated rows query execute as independent statements under READ COMMITTED; a concurrent `create_skill` commit between them makes `total` reflect a different row set than the returned `items` (duplicate/missing rows when walking pages). **Inherent to offset pagination and shared with the mirrored `list_jobs` precedent** (`job_service.py`) — not introduced by 5-6 and not scoped by the story (no snapshot-isolation requirement). Real but low-impact; revisit only if pagination consistency under load becomes a requirement (would need both queries in a single REPEATABLE READ snapshot, or a switch to keyset/cursor pagination — the latter also fixes the related `created_at`-tie ordering instability flagged as a decision item on the story).

## Deferred from: code review of 5-7-skill-registry-ui-pagination (2026-06-26)

- `internal.test.tsx` carries Story 5-4 scope alongside the 5-7 change: beyond the legitimately-5-7 `useSkillsPage` mock (needed because the route test renders the real registry), the diff adds `useJob`/`useJobs`/`useRunStore` mocks and three `/internal/jobs` route tests, all self-labeled `// Story 5.4:`. This is scope-bleed from a working tree that holds both 5-4 and 5-7 WIP uncommitted — not a 5-7 defect (suite passes). Will resolve naturally when Story 5-4 is committed and reviewed on its own. [`src/routes/internal.test.tsx`]

- **[HIGH — needs backend] Registry free-text search is current-page-only, which is misleading now that the registry paginates.** Raised during 5-7 review. The inline registry search box (and Type/Visibility filters) filter only the rows on the *current page* — type a skill name that lives on another page and it silently shows nothing, even though the box looks like a global search. The 5-7 story locked this as client-side because no backend free-text param exists — **verified against source: `velara-api/app/api/v1/skills.py:88-95` (route) + `app/services/skill_service.py:341-362` (service) accept only `page`/`per_page`/`status`/`tag`; there is no `q`/`search`/`ilike`/full-text anywhere.** 5-7 (frontend-only) fixed the *honesty* (relabelled the box "Filter this page…", added a ⌘K whole-catalog hint on multi-page catalogs) but NOT the underlying capability. **Proper fix (backend + FE):** add a `q: str | None` query param to `GET /api/v1/skills` → `skill_service.list_skills` (name/description `ilike` or a tsvector/full-text index for scale), then wire the registry search box to send `?q=` server-side with a page-reset (same model as the `?status=` State filter). Until then, whole-catalog search remains the ⌘K palette's job. [`velara-api/app/api/v1/skills.py`, `velara-api/app/services/skill_service.py`, `velara-web/src/features/skills/components/SkillRegistry.tsx`]

## Deferred from: code review of 5-8-skills-free-text-search-api (2026-06-26)

- **Leading-`%` ILIKE forces a sequential scan — no supporting trigram index.** The `?q=` search builds `or_(Skill.name.ilike(f"%{q}%"), Skill.description.ilike(f"%{q}%"))` (`app/services/skill_service.py:368`). A leading-`%` LIKE pattern cannot use a B-tree index, and there is no `pg_trgm`/GIN index on `name` or `description` (confirmed: only `idx_skills_status`, `idx_skills_org_id`, and JSONB GIN indexes exist). Both the count and rows queries seq-scan within the org partition. Story 5.8 explicitly deferred this ("`ilike` substring is sufficient at current scale ~3,485 rows; a future `pg_trgm` GIN index can be added if latency becomes a concern"). Revisit when the catalog grows toward tens of thousands of rows, or if a `q` `max_length` bound is not added (an unbounded term compounds the seq-scan cost into a potential DoS surface). **Fix path:** add a `pg_trgm` extension + `gin_trgm_ops` index on `skills.name` and `skills.description` (requires a migration). [`velara-api/app/services/skill_service.py:368`, `velara-api/app/models/skill.py`]

## Deferred from: code review of 5.5-1-register-code-driven-hybrid-bundle-manifest (2026-06-26)

- **`egress` has no count or per-entry length bound (inconsistent with `requires`/`tags`).** `_validate_egress` (`app/services/code_driven_hybrid.py:98-113`) only strips entries and rejects empties/non-strings — no max count, no max per-host length. `requires` is bounded at 16 tokens / 128 chars and `tags` at 32 / 64 (`app/schemas/skill.py`). 5000 egress entries were accepted in testing, all landing in the GIN-indexed `egress` JSONB column. Epic 5.5 marks egress enforcement as "advisory at Phase 1" (network-layer enforcement deferred to Epic 7), so a count/length bound is a hardening follow-up, not blocking for 5.5.1. **Fix path:** add a count + per-host length bound mirroring the existing list-field discipline. [`velara-api/app/services/code_driven_hybrid.py:98-113`]
- **UTF-8 BOM on a code-driven manifest is misrouted to the LLM-driven validator (wrong error code).** `is_code_driven_manifest` and `parse_code_driven_manifest` decode with `raw.decode("utf-8")` (`app/services/code_driven_hybrid.py:132-136, 157-169`), which fails on a leading BOM (`\xef\xbb\xbf`). A BOM-prefixed valid code-driven manifest is therefore detected as not-code-driven, routed to `parse_hybrid_artifact`, and rejected with `INVALID_HYBRID_ARTIFACT "not valid JSON"` instead of the targeted `INVALID_CODE_DRIVEN_MANIFEST`. It is still rejected (no correctness hole) — only the error class is misleading. **Fix path:** decode with `utf-8-sig` (or strip the BOM) in both detection and parse. [`velara-api/app/services/code_driven_hybrid.py:132-136, 157-169`]

## Deferred from: code review of 5.5-3-code-driven-hybrid-executor-venv (2026-06-26)

- **Zip bundle extraction — skill code delivered as a real file bundle.** Phase 1 treats the stored manifest JSON as the artifact (`code_driven_executor.py` Step 2 is a no-op; `bundle_dir = workspace`) and relies on the skill package being installable via the `requirements` lockfile (local path). Real multi-file bundle extraction is **Story 5.5.4** (raw-file-by-reference). [`velara-api/app/services/code_driven_executor.py:172-177`]
- **Registration-time venv pre-bake.** Phase 1 builds a fresh per-skill venv and runs `pip install` on every invocation (`_VENV_CREATE_TIMEOUT_S=120` + `_PIP_INSTALL_TIMEOUT_S=300` of additive overhead per run, eating the Celery budget before the entrypoint starts). Pre-baking the venv at registration (or a cached venv keyed by lockfile hash) is deferred. [`velara-api/app/services/code_driven_executor.py:48-53`]
- **Network egress enforcement — Phase 1 is open egress.** The entrypoint subprocess has no socket block (the `preexec_fn` sets only `RLIMIT_AS`/`RLIMIT_NOFILE`); the manifest's declared egress destinations are advisory. Network-layer enforcement is deferred to **Epic 7**. Note the related hardening from this review: `_validate_requirements` now blocks pip remote/URL install directives, but bare local-path installs remain permitted (requires worker filesystem access to exploit) — a tighter install allowlist can land with the egress work. [`velara-api/app/services/code_driven_executor.py:12-16`]

## Surfaced during: create-story for 5.5-4-raw-file-input-to-hybrids — VERIFIED against the real client skill (2026-06-26)

> The actual `velara-protocol-extractor` bundle (deliverable 2026-06-15, `~/Downloads/velara-protocol-extractor/`) was read during 5.5.4 story creation. Two **breaking** contract mismatches between the real skill and the 5.5.3 executor were found. Neither is in 5.5.4's scope (5.5.4 only materializes the raw input file at `input_path`), but both BLOCK the 5.5.6 headline gate — logged here per the Epic 5 retro escalation discipline.

- **[BLOCKS 5.5.6 — runner arg shape] Entrypoint takes named kwargs, not a `params` dict.** The real entrypoint is `velara_extractor.plugin:run(input_path, output_dir=None, *, model=None, consensus_runs=1, enrich=True, emit_excel=True)` (`src/velara_extractor/plugin.py:26`). The 5.5.3 runner calls `func(input_path=..., output_dir=..., params=json.loads(argv))` (`code_driven_executor.py:_RUNNER_SCRIPT`), so against the real skill it raises `TypeError: run() got an unexpected keyword argument 'params'` — **the client skill never starts.** The skill's own `plugin.py` docstring + `plugin/manifest.json` both flag the contract as ASSUMED ("confirm/adjust against the real platform — BRD open question 1"). **Fix path (a 5.5.6 / 5.5.3-followup DECISION, not 5.5.4):** either (a) the runner splats invocation inputs as `func(input_path=..., output_dir=..., **params)` so manifest-declared inputs map to named kwargs, or (b) 5.5.6 ships a thin platform-adapter that maps the `{params}` envelope → the skill's named kwargs. Option (a) changes the contract every code-driven skill binds to, so it needs an explicit architect/PM call. [`velara-api/app/services/code_driven_executor.py` `_RUNNER_SCRIPT`; real `src/velara_extractor/plugin.py:26`]

- **[BLOCKS 5.5.6 — envelope shape, owned by 5.5.5] `artifacts` is a dict, not a list.** The real `run()` returns `"artifacts": {"canonical_json": path, "excel": path|None}` (a role-keyed dict) plus an extra top-level `"billing_grid"` key (`src/velara_extractor/plugin.py` return). The 5.5.3 `CodeDrivenResultEnvelope` declares `artifacts: list[dict]`, so validating the real envelope raises `CODE_DRIVEN_ENVELOPE_ERROR`. **This is Story 5.5.5's job** (multi-artifact persistence reshapes `InvocationResult` + the envelope), but it confirms 5.5.6 cannot pass until 5.5.5 reconciles the envelope to the real skill's shape (role-keyed artifacts + optional `billing_grid`). The real `schema_version` is `0.3.0` (`src/velara_extractor/model.py:33`), NOT the `0.1.0` in the stale bundled `plugin/manifest.json` — trust the code. [`velara-api/app/services/code_driven_executor.py` `CodeDrivenResultEnvelope`; real `src/velara_extractor/plugin.py`, `model.py:33`]

## Deferred from: code review of 5.5-4-raw-file-input-to-hybrids (2026-06-26)

- **Malformed/non-string `file_ref_id` would escape as the generic `SKILL_EXECUTION_ERROR`, not a stable `FILE_REF_*` code.** In the code-driven executor's materialization loop, `frid = _uuid.UUID(str(frid_str))` (`app/services/code_driven_executor.py:298`) sits outside the per-file try/except (which only wraps `ingest_storage.get`). A `file_ref_ids` entry that is not a valid UUID string raises `ValueError`, which propagates uncaught out of `run_code_driven_hybrid`; `_map_error_code` (`app/workers/execution_tasks.py`) has no `ValueError` branch, so it falls through to the catch-all `SKILL_EXECUTION_ERROR` instead of `FILE_REF_NOT_FOUND`. **Unreachable via the validated API** — the request schema is `file_ref_ids: list[uuid.UUID]` (`app/schemas/invocation.py:45`, 422 on bad input) and persistence writes `[str(fid) for fid in body.file_ref_ids]` (`app/api/v1/invocations.py:198`), so `job.inputs` only ever holds valid UUID strings. **Pre-existing + cross-cutting:** the sibling `_run_code` (`execution_service.py:552`) and `_run_hybrid` (`execution_service.py:736`) paths use the identical unguarded `_uuid.UUID(str(frid_str))` idiom, so hardening belongs in a single cross-runtime fix (wrap the parse and map `ValueError` → `FILE_REF_NOT_FOUND` in all three paths), not a 5.5.4-only divergence. [`velara-api/app/services/code_driven_executor.py:298`, `app/services/execution_service.py:552,736`, `app/workers/execution_tasks.py` `_map_error_code`]

## Surfaced during: create-story for 5.5-5-multi-artifact-canonical-output-blocked-state (2026-06-26)

- **[Deferred to the pipeline epic] Consumes-refusal of a `blocked` upstream output.** Epic 5.5 AC for 5.5.5 says "a downstream skill attempting to consume a `blocked` upstream output is refused before it executes." There is **no pipeline-consumption mechanism today**: `InvocationRequest` (`app/schemas/invocation.py:17-50`, `extra="forbid"`) carries `file_ref_ids` but NO `upstream_job_id`; the only request-time validation seam is `assert_file_ref_ready` (uploaded docs), with no analogue for upstream jobs. Building the refusal now = a net-new pipeline-consumption feature with no consumer until the downstream coding/coverage/budget skills exist (a future epic). **Decision (locked w/ user 2026-06-26): 5.5.5 ships only the PRODUCER side — the `blocked` terminal job state + artifact persistence + Run Console surfacing.** The refusal is deferred. **Prerequisites are now BOTH in place:** the `consumes` declaration (Story 5.5.2 — `app/models/skill.py:103-108`, shape `{schema_ref, schema_version}`) and the `blocked` state (this story). **Fix path (pipeline epic):** add `upstream_job_id` to `InvocationRequest`; add `assert_upstream_job_consumable(upstream_job, downstream_skill)` mirroring `assert_file_ref_ready` — refuse (422, new stable code e.g. `UPSTREAM_OUTPUT_BLOCKED`) if `upstream_job.status in {blocked, failed}`, and match `downstream_skill.consumes.schema_ref/schema_version` against the upstream producer's `result_metadata["canonical"].{schema_ref, schema_version}`; wire the FE error copy in `errors.ts JOB_ERROR_MESSAGES`. [`velara-api/app/schemas/invocation.py`, `app/api/v1/invocations.py`, `app/models/skill.py`]

## Deferred from: code review of story-5.5-5 (2026-06-26)

- **[Out of scope — code-driven fan-out epic] Blocked fan-out child silently rolls up to `completed`.** In `aggregate_results`'s child-classification loop (`velara-api/app/workers/execution_tasks.py:421-431`), a child in `JOB_STATUS_BLOCKED` is now in `JOB_TERMINAL_STATUSES` (so it skips the non-terminal branch at :421) but matches **none** of the `cancelled`/`failed` `elif` branches → it "contributes nothing" → the parent rolls up to `completed` with `outcome="success"`, silently discarding the QA egregious hold — the exact gate this story exists to enforce. **UNREACHABLE today:** a `blocked` status only originates from the code-driven executor's `is_releasable` QA gate, and no code-driven skill is `location_dependent`, so the (code-driven ∧ fan-out) intersection is empty. Story 5.5.5 §"Out of Scope" (L116) explicitly forbids touching `aggregate_results` ("NO fan-out changes — `aggregate_results` untouched; no code-driven fan-out skill exists"). **Fix path:** when the first code-driven fan-out (location-dependent) skill ships, add an `elif child.status == JOB_STATUS_BLOCKED` branch that surfaces the hold at the parent (e.g. a `FAN_OUT_BLOCKED` rollup code / a parent `blocked` state) rather than letting it fall through to success. [`velara-api/app/workers/execution_tasks.py:421-431`]
- **[Security hardening] Unbounded artifact read into memory.** The multi-artifact upload loop reads each file fully into RAM via `open(p, "rb").read()` with no size cap (`velara-api/app/services/code_driven_executor.py:517`); a skill that writes a very large artifact to `output_dir` could OOM the Celery worker. **Mitigated** by the subprocess `RLIMIT_AS` memory cap (:357) bounding what the skill can produce, and it is a **pre-existing pattern** — the Story 3.6 canonical read (:486) is byte-for-byte the same shape. Consistent with the team's deferral of the egress bound in Story 5.5-1. **Fix path:** stream-upload with a configurable per-artifact size ceiling (reuse/extend `settings.EXECUTION_MAX_OUTPUT_BYTES`), rejecting oversized artifacts as a `blocked`/`failed` outcome rather than reading them whole. [`velara-api/app/services/code_driven_executor.py:517,486`]
- **[Pre-existing / cross-cutting] Mid-loop artifact-upload failure orphans already-uploaded objects.** If `output_storage.put` fails on artifact N after the canonical JSON + artifacts 1..N-1 already uploaded, the executor raises `CodeDrivenExecutionError` → the job goes to `failed`, but the already-written objects remain in the output bucket with no result row referencing them; the `finally` block only `rmtree`s the local workspace, never the output bucket. No transactional all-or-nothing and no project-wide orphan-GC exists (same shape since the 3.6 single-file path). **Fix path:** a cross-cutting output-bucket orphan-GC (sweep `jobs/{job_id}/*` for jobs with no/failed result rows), or best-effort cleanup of this run's uploaded keys in the executor's failure path. [`velara-api/app/services/code_driven_executor.py:515-540,568-571`]

## Deferred from: code review of story-7.1 (2026-06-27)

- **[Gated on client account + BAA — staging] Staging RDS skips final snapshot on destroy/replace.** `skip_final_snapshot = !var.db_deletion_protection` and `final_snapshot_identifier = var.db_deletion_protection ? "...-final-snapshot" : null` couple the final-snapshot guarantee to *deletion-protection*, which is off for staging (`db_deletion_protection=false`). So once staging holds real client data (post-BAA), any `terraform destroy` or replace-triggering change drops the DB with **no** final snapshot. Dev (no data) and prod (protection on) are safe. **Fix path:** decouple — set `skip_final_snapshot=false`/author a `final_snapshot_identifier` for any non-dev env, or key it on `var.environment != "dev"` rather than on deletion-protection. [`velara-api/terraform/rds.tf:1483-1484`]
- **[Apply-dependent] Container Insights log groups bypass the 90-day retention (AC5 partial gap).** ECS cluster sets `containerInsights=enabled`, which auto-creates `/aws/ecs/containerinsights/<cluster>/...` log groups that are NOT declared in Terraform and default to **never-expire**. The app log groups (api, worker) correctly set `retention_in_days=90`, so AC5 holds for them but not for the Insights-generated groups. **Fix path:** either declare the Container Insights log groups explicitly with 90-day retention, or disable Container Insights if not needed in this story's scope. Revisit during AC5 live verification after apply. [`velara-api/terraform/ecs.tf:851-853`, `cloudwatch.tf`]
- **[Latent — input validation] No guard on subnet-CIDR list lengths / AZ count.** `az_count = length(var.private_subnet_cidrs)` is used to index `var.public_subnet_cidrs[count.index]`; a tfvars where `public` is shorter than `private` produces an out-of-range index (plan error), and a single-element list silently builds a single-AZ deployment that violates the DB subnet group's ≥2-AZ requirement (fails only at apply). All shipped tfvars are 2/2. **Fix path:** add `validation` blocks asserting `length(public_subnet_cidrs) == length(private_subnet_cidrs)` and `>= 2`. [`velara-api/terraform/vpc.tf:2018,2052`]
- **[Latent — input validation] `nat_gateway_count=0` indexes `[-1]`.** `aws_nat_gateway.main[min(count.index, var.nat_gateway_count - 1)]` errors at plan if `nat_gateway_count=0` (defaults ≥1, so latent). **Fix path:** `validation { condition = var.nat_gateway_count >= 1 }`. [`velara-api/terraform/vpc.tf:2107`]
- **[Dead/misleading config] velara-web `aws.us_east_1` aliased provider declared but unused.** `versions.tf` defines an `aws.us_east_1` aliased provider ("CloudFront requires an ACM cert in us-east-1") but nothing references it and no ACM cert resource is created in-repo (the cert is passed in as an ARN). No runtime impact (CloudFront is global) but it will trip a lint as an unused provider, and may signal a missing intended in-repo cert resource. **Fix path:** remove the unused aliased provider, or (if intended) create the `aws_acm_certificate` in `us-east-1` for the CloudFront viewer cert. [`velara-web/terraform/versions.tf`, `cloudfront.tf`]
- **[RESOLVED 2026-07-02] CloudFront distribution not created (new-account verification gate).** `terraform apply` for dev (2026-06-27) created the `velara-dev-frontend` S3 bucket but `aws_cloudfront_distribution.frontend` failed because AWS restricts CloudFront on **unverified new personal accounts**. NOT a code defect — the IaC is correct and `terraform validate`/`plan` are clean. **Resolution:** the AWS CloudFront restriction was lifted (verified live — `create-invalidation` now returns `NoSuchDistribution` instead of `403 AccessDenied`). Re-ran `terraform apply -var-file=dev.tfvars` in `velara-web/terraform/`; distribution `E3VRBA96BDW70I` created (domain `d2yo81lbjfacze.cloudfront.net`), S3 bucket re-locked to OAC-only, public-access-block re-enabled. velara-api CORS_ALLOW_ORIGINS + Cognito callback/logout URLs updated to the CloudFront origin (api task-def rolled to :13; CORS verified live). [`velara-web/terraform/cloudfront.tf`]
- **[Observability] Worker ECS container has no `healthCheck`.** The API container defines a `/health` CMD-SHELL healthcheck; the worker container has none, so worker crash-loops (including the D3 Redis-TLS connect failure this story guards against) are less observable. Worker has no port and a healthcheck is not spec-required, so deferred. **Fix path:** add a worker container healthCheck (e.g. `celery -A app.workers.celery_app inspect ping`) so ECS surfaces a wedged worker. [`velara-api/terraform/ecs.tf`]

## Deferred from: code review of story-6.1 (2026-06-27)

- **[Spec-sanctioned simplification] AC4 `RECERTIFICATION_REQUIRED` discriminator is coarser than spec intent.** `assert_certified_for_client_ready` distinguishes `RECERTIFICATION_REQUIRED` from `CERTIFICATION_INCOMPLETE` by checking whether ANY prior cert exists for the skill (across all versions) — not whether the skill "previously reached client_ready" as AC4 (story line 114) describes. False-positive case: a skill that recorded a single orphan cert on v1.0.0 (never both keys, never client_ready), then publishes v1.1.0, gets `RECERTIFICATION_REQUIRED` on advance instead of `CERTIFICATION_INCOMPLETE`. **Why deferred:** Task 5 (story line 114) explicitly authorizes this fallback — "if distinguishing prior client_ready history is too costly without a state-history table, document the simplification" — and there is no skill state-history table (only the current `lifecycle_state` scalar). Documented in completion notes (line 259). **Fix path:** revisit in Story 6.3 (auto-advance), ideally alongside a lifecycle state-history table so the "previously client_ready" check becomes exact. [`velara-api/app/services/certification_service.py:260-274`]

## Deferred from: code review of story-6.2 (2026-06-27)

- **[Locked Decision D2 — no display name until Epic 7] Raw `certifier_user_id` (UUID) rendered as the certifier display name in the cert badge/tooltip.** `CertKeyBadge` shows `· {certifier_user_id}` inline and "Certified by {certifier_user_id} on {date}" in the tooltip (CertificationScreen.tsx:198; CertKeyBadge.tsx:42,55), and the e-signature statement embeds the raw `user_id` into the Part 11 attestation. **Why deferred:** Decision D2 explicitly accepts `user_id`+`role` as the signer display because no User table / display name exists until Epic 7 Cognito; the story already carries a "verified printed-name binding is an Epic 7 forward-dep" note in the modal. **Fix path:** when Epic 7 identity management lands, resolve `certifier_user_id` → display name/email for the badge, tooltip, and the e-signature attestation string. [`velara-web/src/features/certification/components/CertKeyBadge.tsx:42,55`; `CertificationScreen.tsx:198`; `RecordTechnicalCertModal.tsx:108-112`]

## Deferred from: code review of story-6.4 (2026-06-27)

- **[Spec-sanctioned cap — story Trap 2 / D3] Certification History + re-cert indicator read only the first 200 cert records; no truncation indicator and no pagination fallback.** Both `CertificationHistory.tsx` and SkillDetail's `needsRecert` computation fetch `useCertifications(skillId, { per_page: 200 })` and render/derive only `data.items` — `page.total` is never read. On a skill with >200 cert records (>100 versions × 2 keys, or accumulated re-certs): (a) history rows beyond 200 silently vanish with no "showing first 200 of N" notice; (b) more seriously, if the current version's certs happen to fall on a later page, `currentVersionCerts.length === 0 && certItems.length > 0` evaluates true and the UI **falsely shows "Requires re-certification" on an actually-certified skill**. **Why deferred:** Story D3 + Trap 2 explicitly accept 200 as the Phase-1 bound (raised from the default 50 precisely to close the 6.3 page-1 truncation defer); a skill exceeding 200 cert records is out of Phase-1 scope and the in-code comment documents the cap. **Fix path:** when a skill realistically approaches 200 cert records, either paginate the history (read `page.total`, fetch subsequent pages) or at minimum surface a "showing first 200 of N — history truncated" indicator AND compute `needsRecert` from a current-version-scoped query rather than the first page. [`velara-web/src/features/certification/components/CertificationHistory.tsx:121`; `velara-web/src/features/skills/components/SkillDetail.tsx:362-366`]

## Deferred from: code review of story-7.2 (2026-06-29)

- **[RESOLVED 2026-07-02] CloudFront `cloudfront:CreateInvalidation` scoped account-wide (`arn:aws:cloudfront::<acct>:distribution/*`).** The web deploy role could invalidate any distribution in the account, not just the frontend one. Sanctioned interim per story Completion Notes fix #2 (the real distribution ARN couldn't be referenced while CloudFront was gated). **Resolution:** the gate was lifted and the distribution created, so the statement was re-scoped to `aws_cloudfront_distribution.frontend.arn` (least-privilege). Applied. [`velara-web/terraform/iam.tf`]
- **`/health/ready` readiness check is `SELECT 1` only — no schema/migration-version assertion.** This is why the `deployment_circuit_breaker` cannot detect schema drift: a stale-schema container goes "healthy" and the deploy reports green. The migration-on-deploy patch makes this non-blocking (migrate-first removes the drift window). **Fix path (defense-in-depth):** assert `alembic_version` is at the expected head in the readiness probe so the breaker can catch a migration mismatch. [`velara-api/app/api/v1/health.py:70-78`]
- **`force_destroy = true` on the versioned skill + brand S3 buckets.** A `terraform destroy` (or a replace-forcing change) deletes all object versions of registered skill bundles without prompting. Consistent with the existing ingest/output bucket pattern and dev-only, but skill bundles are system-of-record artifacts. **Fix path:** gate `force_destroy` off for staging/prod, or add a `prevent_destroy` lifecycle for the skill bucket. [`velara-api/terraform/s3.tf`]
- **OIDC provider thumbprint hardcoded to a single fixed value.** AWS uses its own trust store for the well-known GitHub provider, so the thumbprint is not load-bearing today — but the "no longer load-bearing" justification is an assumption (GovCloud / future re-tightening could break it). Documented value. [`velara-api/terraform/oidc.tf`]
- **Cross-root OIDC provider ARN is string-derived, not looked up.** `velara-web` `terraform apply` SUCCEEDS creating the deploy role even if `velara-api` (which creates the account-global provider) has not been applied — the role's trust policy then references a non-existent principal and fails only at runtime with an opaque `AssumeRoleWithWebIdentity` error. DELIBERATE (a `data` lookup would hard-fail web's plan before api applies); apply order velara-api → velara-web is documented in the README. **Fix path:** add a CI/operator preflight that asserts the provider exists before the first web deploy. [`velara-web/terraform/iam.tf`]
- **First-ever deploy `describe-task-definition` fails with an opaque error if Terraform wasn't applied first.** The register step has no preflight check; the README documents the apply-first order procedurally. **Fix path:** add a preflight that detects a missing task-def family and emits "apply Terraform first." [`velara-api/.github/workflows/deploy.yml`]
- **`aws ecs wait services-stable` 10-min default timeout makes red ambiguous.** A slow-but-healthy rollout or a mid-rollback breaker can time the waiter out (40×15s) and fail the job even though ECS ultimately settles. **Fix path:** add a post-wait `describe-services` `rolloutState` assertion and/or tune the waiter. [`velara-api/.github/workflows/deploy.yml`]
- **`VITE_API_URL` build injection has no trailing-slash / scheme normalization.** The guard only fails on empty; `http://alb/` yields `//api/v1/...` double-slash paths, and `https://` against the HTTP-only dev ALB fails every request. **Fix path:** trim a trailing slash and validate the scheme in the build step. [`velara-web/.github/workflows/deploy.yml`]
- **Re-registered task definitions drop tags.** `aws ecs describe-task-definition` omits tags without `--include TAGS`, so every CI-registered revision loses any task-def tags. Cosmetic today (the TF task defs define no tags). **Fix path:** add `--include TAGS` and re-attach if/when tagging is introduced. [`velara-api/.github/workflows/deploy.yml`]


## Deferred from: code review of story-7.3 (2026-06-30)

- **[Pre-existing sessionStorage auth pattern] `isAuthenticated()` guard in App.tsx not reactive to mid-session changes.** The `/login` route evaluates `isAuthenticated()` once at render time, not as a reactive hook. This is inherited from the pre-existing sessionStorage auth architecture used across the whole app (`RequireAuth.tsx`). Making it reactive requires a global auth state store, which is out of scope for 7.3. **Fix path:** introduce a global React auth context/store that wraps sessionStorage reads and triggers re-renders on session change. [`velara-web/src/App.tsx:22`; `velara-web/src/shared/components/RequireAuth.tsx`]
- **[Phase 2 / out of scope] SAML attribute mapping maps `custom:org_id` to Azure AD groups claim — would produce group GUIDs, not Velara org IDs.** The Cognito federation slot's attribute mapping routes the Azure AD `groups` claim to `custom:org_id`. Azure AD group GUIDs (e.g., `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`) don't match Velara org_id strings. A Pre-Token-Generation Lambda is needed to translate group membership to the correct org_id. This is explicitly a Phase 2 activation step (the slot is `count = 0` / not active). **Fix path:** before activating federation, author a Pre-Token-Generation Lambda that maps Azure group membership to `custom:org_id` and `custom:role`. [`velara-api/terraform/cognito.tf`, federation slot resource]
- **[Architectural] `get_auth_provider` lru_cache + synchronous PyJWKClient has no cache TTL.** PyJWKClient caches JWKS keys indefinitely. On key rotation (new `kid`), PyJWKClient will auto-fetch on cache-miss, so rotation is handled. The risk: if the JWKS endpoint is down at cache-miss time, all requests get 401 until recovery. No timeout is set on the PyJWKClient instance. **Fix path:** pass `lifespan` (e.g., 3600) to `PyJWKClient()` so keys are periodically refreshed, and add a `timeout` to the underlying HTTPS call. [`velara-api/app/integrations/auth.py`, `CognitoAuthProvider.__init__`]
- **[Dev UX] MFA_configuration="OFF" for dev Cognito pool.** Dev uses personal account with no PHI (D1 scope). MFA=OPTIONAL is the staging/prod posture. Upgrade to OPTIONAL when promoting to staging. [`velara-api/terraform/cognito.tf`]

## Deferred from: code review of story-7.3 T13 forward-deps (2026-06-30)

- **[Epic 6 forward-dep — FR-SEC-12] E-sig re-authentication on certification record.** The Part 11 e-sig modal (Stories 6.2/6.3) has a `!user` guard that shows a "Sign-in required" dialog — but it does not trigger a real Cognito re-auth challenge. FR-SEC-12 requires the user to re-enter their password before the e-sig is recorded (21 CFR Part 11 §11.200(a)(1)). **Deferred from 7.3 per T13 / epic-6-retro.** **Fix path:** on e-sig modal open, call Amplify `signIn()` as a re-auth step (not `fetchAuthSession` — a fresh credential challenge is required); gate the `Record Certification` submit on re-auth success. [`velara-web/src/features/certification/components/RecordTechnicalCertModal.tsx`; `RecordMethodologicalCertModal.tsx`]
- **[Epic 7 / Cognito forward-dep] Printed-name resolution for certifier in Part 11 e-sig attestation.** The e-sig statement reads "I, {user_id} ({role}), certify…" using the raw UUID from the JWT. FR-SEC-12 requires a human-readable printed name. **Deferred from 7.3 per T13 / epic-6-retro (Decision D3 in Story 6.2 explicitly accepted user_id+role as the interim signer display until Epic 7 Cognito).** **Fix path:** expose the user's display name / email from the Cognito ID token (the `name` or `email` claim, or a `given_name`+`family_name` pair); store the printed name in `CertificationRecord.certifier_printed_name` (new column, migration); render it in the e-sig string. Coordinate with the Cognito user-pool schema (the `name` attribute must be required/verified). [`velara-api/app/models/certification.py`; `velara-web/src/features/certification/components/RecordTechnicalCertModal.tsx`; `RecordMethodologicalCertModal.tsx`; `CertKeyBadge.tsx`]

## Deferred from: code review of story-7.4 (2026-06-30)

- **[LIVE for staging/prod] Embedded `-B` beat double/triple-fires `publish_queue_depth` at >1 worker replica.** The dead-beat patch (code review of story-7.4) added `-B` to the worker launch command so beat now runs embedded in the worker. Safe at **dev (`worker_desired_count=1`)** but a real over-counting bug at **staging (`=2`)** and **prod (`=3`)**: every replica runs its own beat and publishes `CeleryQueueDepth` N times/min → over-counted datapoints, misleading dashboard/alarm, possible PutMetricData throttling. Acceptable now because Story 7.4 is dev-only and staging/prod are gated on the client AWS account + BAA. **Fix path BEFORE staging/prod go live:** split beat into a dedicated single-replica ECS service (`celery -A app.workers.celery_app beat`) and drop `-B` from the worker CMD so the worker can scale freely. Guard comment lives in `variables.tf:worker_desired_count`. [`velara-api/docker/Dockerfile.worker`, `velara-api/terraform/ecs.tf`, `velara-api/terraform/variables.tf`]
- **[Process / hygiene — not a 7.4 defect] Story-7.3 Cognito work uncommitted and commingled in the working tree.** `terraform/cognito.tf`, `terraform/dev.tfvars`, `app/integrations/auth.py`, and `tests/unit/integrations/test_cognito_provider.py` are uncommitted alongside 7.4 (and outside 7.4's declared File List), plus a stray binary `terraform/tfplan.review`. Story 7.3 was marked `done` but its changes appear never to have been committed. **Fix path:** commit 7.3's changes under a 7.3 commit (separate from 7.4); add `terraform/tfplan.review` (and `*.tfplan*`) to `.gitignore` and remove the stray artifact. [`velara-api/` working tree]

## Deferred from: code review of story-8.1 (2026-06-30)

- `_postgres_reachable()` in `tests/integration/api/test_access_grants.py:30-43` parses `DATABASE_URL` with `split("@")`/`rsplit(":")` under a bare `except Exception: return False`. A URL with no auth-`@` or no explicit port raises and is swallowed → the ENTIRE access-grants integration suite (the only coverage of the `<@` scope-filter path) silently skips. In a misconfigured CI this ships the scope feature green with zero scoped-route coverage. Pre-existing test-infra pattern (mirrors `test_hierarchy.py`), not introduced by 8.1. Fix: parse with `urllib.parse.urlsplit` (handles default port / no auth) and make reachability failure LOUD (xfail/error) rather than a skip in CI. [`velara-api/tests/integration/api/test_access_grants.py:30-43`]
- Grant resolution (`access_service._resolve_node_hierarchy_path`) trusts that a node's org membership is immutable — it re-walks parents to the Client and checks `client.org_id` live, but there is no move/re-parent endpoint today, so a node cannot change orgs. Latent risk only: if a future "move node" / re-parent feature lands without revisiting grant resolution, a grant could resolve to a node that has since changed org/path. No code change for 8.1; flag for the move-node story. [`velara-api/app/services/access_service.py:55-115`]

## Deferred from: code review of story-8.2 (2026-06-30)

- Fan-out parent client poll degrades to a permanently empty `output_files` + an unhelpful parent status. A `location_dependent` + `fan_out=true` skill invoked by a client returns a parent `job_id` whose `result_metadata` carries a `"children"` roll-up (per-child job_id/location_id/status/output_file_key), NOT a top-level `"output_files"` key — so `client.py`'s output loop (which keys on `"output_files"`) finds nothing and returns `output_files: null`, and the parent status never summarizes child completion. Confirmed NO leak (the client loop never reads `"children"`, and `ClientJobRead` excludes `result_metadata`), but the client gets no downloadable output for fan-out skills. Story 8.2 explicitly defers full client fan-out output aggregation to Story 8.4 — wire parent→children output aggregation (scope-filtered, internals-free) on the client surface there. NOTE: the no-leak property is currently load-bearing on the metadata key name being `"children"` not `"output_files"`; 8.4 should make the exclusion explicit rather than incidental. [`velara-api/app/api/v1/client.py:120-160`]

## Deferred from: code review of story-8.3 (2026-07-01)

- **Engagements endpoint is unbounded — no pagination + N+1 `list_projects`.** `GET /api/v1/client/engagements` calls `list_clients` (no LIMIT) then one `list_projects` per resolved client in a loop (`app/api/v1/client.py:109-131`); the whole engagement tree is serialized with no page cap. Pre-existing pattern — inherits the org-wide no-LIMIT design shared across `hierarchy_service.list_clients`/`list_projects`. Client grant counts are small in practice, so impact is low; revisit alongside any hierarchy-service pagination work (would apply a page cap + a single batched project query keyed by the resolved client ids). [`velara-api/app/api/v1/client.py:109-131`, `app/services/hierarchy_service.py:200-213,296-312`]
- **Stale-grant vs no-grant empty list is indistinguishable (support diagnosability).** A client whose grant rows all point to deleted/re-pathed/cross-org nodes has every path dropped by `resolve_scope_paths` (fail-closed, `access_service.py:207-216`) → empty `scope_paths` → `list_clients([])` returns `[]` → the portal shows "No engagements assigned yet.", identical to a truly ungranted user. The fail-closed behavior is correct security-wise, but there is no signal (meta flag / warning log) distinguishing "no access" from "access misconfigured," making support diagnosis hard. Deferred — rooted in the 8.1 resolver, not introduced by 8.3. Fix path: surface a diagnostic signal (structured log or non-IP meta flag) when grants exist but all resolve stale. [`velara-api/app/api/v1/client.py`, `app/services/access_service.py:207-216`]

## Deferred from: code review of story-8.6 (2026-07-01)

- **The 3 pre-existing `test_client_surface.py` failures are a seed-data bug, NOT stale DB state (8.6's Dev-Notes hypothesis disproven).** `test_client_completed_job_returns_presigned_output_no_internals`, `test_client_blocked_job_withholds_output`, and `test_client_out_of_scope_same_org_job_is_403` fail with `asyncpg ForeignKeyViolationError: fk_invocation_jobs_skill_id` — they INSERT an `invocation_jobs` row whose `skill_id` was never seeded into `skills`. During 8.6 review these were re-run against a **freshly created + migrated `velara_test`** (after `docker compose down -v`), and they **still fail** — so the story's "reset the test DB to fix" note is wrong; a DB reset does not help. This is a real seed-setup defect in the Story 8.2 test file, unrelated to skill attachments (8.6 touches none of `invocation_jobs`). Confirmed the other suites (`test_skill_attachments`=22, `test_openapi`=12, `test_hierarchy`, `test_access_grants`) all pass on the same fresh DB. **Fix path (belongs to whoever owns the 8.2 client-surface tests):** seed the referenced `skill` row (or use an existing seeded skill_id) in these three tests' setup before inserting the `invocation_jobs` row. Do NOT alter production code. [`velara-api/tests/integration/api/test_client_surface.py`]

- **`attach_skill` returns 500 on a true concurrent duplicate insert (idempotency is sequential-only).** `attach_skill` does select-then-insert with no `IntegrityError` catch (`velara-api/app/services/skill_attachment_service.py:783-813`); two simultaneous attaches of the same `(skill_id, node_id, node_type, org_id)` both miss the `scalar_one_or_none()` existence check and both insert — the second hits the `uq_skill_attachment_skill_node_org` UNIQUE constraint → unhandled `IntegrityError` → generic 500. The docstring/tests claim idempotency "under a duplicate/race," but only the *sequential* case is idempotent. Inherited 1:1 from `create_grant` (`app/services/access_service.py:257-292`), which has the identical gap — this is why it's deferred rather than patched inside 8.6: the fix (catch `IntegrityError` → rollback → re-select and return the existing row) should land on BOTH `attach_skill` and `create_grant` as one cross-cutting change so the mirrored pattern stays in sync, not forked. Low real-world likelihood (a double-click is debounced client-side; the true race needs two concurrent requests within the select→insert window). [`velara-api/app/services/skill_attachment_service.py:783-813`, `app/services/access_service.py:257-292`]

## Deferred from: code review of story-8.4 (2026-07-01)

- **Internal-role inconsistency on the new client drill-down routes.** `client_list_project_studies` / `client_list_study_locations` (`velara-api/app/api/v1/client.py`) pass `scope_paths=None` for an `unrestricted` caller, so an internal (ma_tech/consultant) token — which reaches these routes because the client router has no `RejectClient` guard — gets ALL studies/locations under the node, while the sibling `GET /client/skills` and `GET /client/engagements` short-circuit unrestricted callers to an EMPTY list. Org-scoped (`get_project`/`get_study` 404 cross-org), so not a cross-org leak — a consistency/policy question, not a security hole. Decide the intended internal-role behavior on the `/api/v1/client/*` surface holistically (relates to the 8.7 admin-role work). [`velara-api/app/api/v1/client.py` `client_list_project_studies`/`client_list_study_locations`]
- **`scope_paths[0]` audit-attribution granularity for non-LD client invocations.** In the D1 non-LD branch (`velara-api/app/api/v1/invocations.py`), a scoped client with exactly one grant and no `study_id` is attributed to `scope.scope_paths[0]` — which may be a client- or project-level ltree path, coarser than the study-grain path the LD path records. It is valid, in-scope, and satisfies AC6's "not `org`", so no crash/leak — but downstream audit/analytics that assume study-grain invocation paths will get a coarser path. Revisit alongside the Epic 9 audit/analytics path-grain decision. [`velara-api/app/api/v1/invocations.py` non-LD branch]

## Deferred from: Epic 8 retrospective (2026-07-02)

- **[Deliberately deferred — revisit only if needed] Story 8.4 D1 non-LD client-invocation widening merged UNDER A WAIVER (no genuine architect ADR).** 8.4's D1 widened the non-LD invocation path so a scoped `client` token resolves a real engagement `hierarchy_path` (from `study_id` or a single-grant fallback) instead of being 403'd on the org-global path — revising the 8.1 "internal-roles-only run org-global invocations" boundary. The story's own gate required "architect sign-off recorded before merge"; it was **self-asserted by the dev agent, NOT a real Winston ADR** in `core-architectural-decisions.md`. **Project Lead decision (Epic 8 retro, 2026-07-02): PARK IT — does not block Epic 9; revisit later only if needed.** Not silently dropped — this is the tracked record. Resolution options when revisited: (a) Winston authors the genuine ADR revising the 8.1 org-global boundary for client non-LD runs, or (b) revert to LD-only fallback for client tokens. Related latent item: the `scope_paths[0]` audit-attribution granularity note above — if Epic 9 analytics ever require study-grain attribution for client runs, D1 resurfaces there. [`velara-api/app/api/v1/invocations.py` non-LD branch; `core-architectural-decisions.md` (no D1 ADR present)]

- **[Planning-process lesson, not a code defect] Epic 8's admin-authority surface (Stories 8.5/8.6/8.7) was a PLANNING MISS absorbed mid-sprint.** Original Epic 8 scoped enforce→protect→experience (8.1–8.4) but not the *administrative* counterpart the grant model implies: a grant-management UI (8.5), a skill-attachment model (8.6), and an internal-role tier to gate them (8.7). All three were inserted via correct-course 2026-07-01, forcing a resequence (8.6 before 8.4), a mid-story UX redesign (8.5), and a review-born new story (8.7) — clean delivery, but wasted time. **Action (owner: John/PM): apply a story-creation checklist item to Epic 9 — "does this introduce state / a boundary / an enforcement point that needs a management UI, a reader/consumer, or a role to operate it, and is each covered by a story?"** Epic 9's 9.1→9.5 chain (audit write → query → UI → analytics API → analytics UI) is where a missed consumer would compound.

## Deferred from: code review of story-9.1 (2026-07-02)

- **DEFAULT-partition trap + no ongoing partition automation.** Migration 0018 provisions only 2026-06…2026-10 monthly partitions plus a `DEFAULT` catch-all. Every audit row written from 2026-11 onward lands in DEFAULT (unbounded growth, defeats partition pruning). Worse: once any out-of-window row sits in DEFAULT, `CREATE TABLE ... PARTITION OF audit_log_entries FOR VALUES FROM (<that month>)` fails on a scan conflict, and because the append-only trigger rejects DELETE/TRUNCATE on DEFAULT too, the blocking row cannot be removed without disabling the trigger. Documented as Phase-2/out-of-scope, but tracked here because the append-only-on-DEFAULT makes it a hard operational lock, not just deferred convenience. Resolution: Phase-2 automation that pre-creates the next month's partition before its first insert (cron/pg_partman/app-startup check). [velara-api/app/db/migrations/versions/0018_partition_audit_log.py]

- **Future partitions lack TRUNCATE protection.** The statement-level `BEFORE TRUNCATE` trigger does not propagate from the partitioned parent to partitions (PG limitation); 0018 attaches it per-partition to the 6 it creates, but any partition created later (the Phase-2 automation above) will be born without a TRUNCATE guard and is therefore truncatable — silently breaking the append-only / Part-11 invariant for that month. AC5 ("across all partitions") holds only for the 6 currently-shipped partitions. Resolution: the Phase-2 partition-creation routine must also attach the per-partition TRUNCATE trigger (reuse `_create_partition_with_truncate_guard`). [velara-api/app/db/migrations/versions/0018_partition_audit_log.py]

- **Dead sync wrappers.** `record_invocation_sync` and `record_admin_action_sync` in audit_service.py have no callers anywhere in app/ or tests/ (the latter is net-new dead code from this story; the former inherited from the pre-rename `record_entry_sync`). Each spins its own `session_scope()` + `asyncio.run()`. Delete, or wire up if a sync entry point is actually intended. [velara-api/app/services/audit_service.py]

## Production bug fixed post-9.3 (user-reported, 2026-07-02) — internal-caller invocations always attributed to "org"

- **RESOLVED — `queue_invocation`'s non-LD branch hardcoded `hierarchy_path="org"` for EVERY unrestricted (internal) caller, ignoring any project/study context.** `app/api/v1/invocations.py`'s non-location-dependent branch had `if scope.unrestricted: hierarchy_path_str = "org"` with no check of `body.study_id` at all — this was the deliberate, documented behavior from Story 8.4 D1 ("Internal (`unrestricted`) callers keep the existing `"org"` behavior unchanged"). In practice this meant every internal user's run of a non-LD skill from the Engagements screen or Run Console — regardless of the Study/Project they were visibly scoped into — silently landed at the org root, breaking audit attribution (the exact symptom that surfaced via the 9.3 Audit Log UI: a real invocation showed `hierarchy_path="org"` instead of the study/project it was run from). Compounding it: the FE (`RunConsole.tsx`, both context-first and skill-first modes) never sent `study_id`/`project_id` in the request payload for non-LD skills in the first place — the `showLocationSelector`-gated payload construction only populated location fields for location-dependent skills, so even fixing the backend alone would not have helped until the FE started sending the context.
  **Fix (2026-07-02):** (1) `InvocationRequest` schema gained `project_id`/`client_id` alongside the existing `study_id` (no prior field could express "run at Project/Client level" — only Study). (2) `queue_invocation`'s non-LD branch now resolves `body.study_id`/`body.project_id`/`body.client_id` (most-specific first) via `hierarchy_service.get_study`/`get_project`/`get_client` for BOTH unrestricted and scoped callers — falling back to `"org"` only when no context is supplied at all (e.g. invoked from the Skill Registry with nothing selected). (3) `RunConsole.tsx`'s two `handleRun()`s (context-first + skill-first) now send the resolved `study_id`/`project_id` for non-LD skills instead of `{}`. Regression tests added: `test_non_location_skill_internal_caller_attributes_to_study`, `..._to_project`, `..._no_context_still_defaults_to_org` (BE); `RunConsole.test.tsx` payload assertions rewritten to expect the real context instead of `{}`. This also resolves (supersedes) the "D1 non-LD client-invocation widening merged UNDER A WAIVER" item from the Epic 8 retrospective below — the underlying 8.1 org-global boundary question is now moot for BOTH scoped and unrestricted callers, since context resolution is symmetric across both. [`velara-api/app/api/v1/invocations.py` non-LD branch; `app/schemas/invocation.py`; `velara-web/src/features/run/components/RunConsole.tsx`]

## Deferred from: code review of story-9.4 (2026-07-03)

- **Unbounded all-time aggregate scans + unpaginated analytics users list.** `_counts`, `_top_skills`, `_token_cost` (a JSONB-extract-and-CAST sum), and the `list_users` group-by all scan the entire append-only `audit_log_entries` table with no `created_at` floor on every dashboard load (only the weekly series is windowed), and `GET /api/v1/analytics/users` returns an unpaginated flat list. Spec-accepted at current volume (the story explicitly chose all-time totals + a flat list), but the endpoints degrade monotonically as the audit log grows. Resolution: add a time floor or materialized/cached rollups, and paginate the users list, when volume warrants. [velara-api/app/services/analytics_service.py]

- **Blocking sync Cognito `ListUsers` pagination inside async analytics handlers.** `get_auth_provider().list_users()` paginates the whole pool synchronously on the event loop for every `GET /analytics/users` and `/analytics/users/{id}` request — a pre-existing 8.5 pattern (`users.py` does the same), newly reached from two more read endpoints; `user_detail` fetches the full directory to resolve a single display name. Resolution: `run_in_executor` or a short-TTL directory cache, and/or a `get_user(user_id)` method on `AuthProvider`. [velara-api/app/services/analytics_service.py:251,313]

## Deferred from: code review of story-10.2 (2026-07-03)

- **`>200` access grants silently truncates the Engagement-access column.** `UsersScreen.tsx`'s `grantsByUser` map is built from a single unpaginated `useAccessGrants({ per_page: 200, page: 1 })` call with only a code comment acknowledging the cap. Past 200 grants org-wide, later users silently show `— (grant pending)` even when they have access. Deferred as a scoped, accepted limitation at current volume per the story's own Dev Notes ("the directory and grant set are small today"). Resolution: paginate or aggregate server-side when volume warrants. [velara-web/src/features/admin/components/UsersScreen.tsx]

- **No per-org fencing on `resend_invite`; distinct 404/409/200 responses enable email-existence/status enumeration pool-wide.** `POST /api/v1/users/resend-invite` takes a bare `email` with no check that the target belongs to the caller's org, matching `GET /users`'s existing pool-wide (not org-scoped) directory semantics — this is the current single-tenant-pool architecture, not a regression introduced by this story. The story's own Dev Notes explicitly instruct "Do NOT invent org fencing on resend; it would diverge from the list this screen renders." Resolution: revisit if/when true multi-tenant org fencing is introduced across the whole users directory (not just resend). [velara-api/app/api/v1/users.py, app/services/provisioning_service.py]

- **Dev-shim `_INVITED_EMAILS` is a bare module-level mutable set, no concurrency guard.** Extends the pre-existing `_SEED_USERS`/`_SEED_EMAILS` pattern in `DevAuthProvider`; parallel test workers (pytest-xdist) sharing this module state could corrupt each other's fixtures. Not introduced by this story, just extended. [velara-api/app/integrations/auth.py]

- **Route-gating "trap" comment is prose-only, not test-enforced.** `resend_invite`'s docstring warns "Do NOT add a route-level RejectNonGrantor here" but nothing asserts the router's dependency list itself excludes it — a future contributor could still copy-paste it in. Same convention gap already exists for 10.1's `create_user`. Resolution: consider a lightweight test asserting `router.dependencies` composition, if this trap bites twice. [velara-api/app/api/v1/users.py]

- **Unmapped `boto3.client()` construction failures could surface as an unhandled 500.** If the boto3 client constructor itself raises (bad region/malformed credentials) before `admin_create_user` is called, the exception isn't caught by the existing `BotoCoreError`/`ClientError` handling, bypassing the documented 404/409/502 contract. Pre-existing gap already present in 10.1's `create_user`; this story's `resend_invite` shares the same code shape. [velara-api/app/integrations/auth.py]

- **`AddUserOverlay` has no initial autofocus/focus-trap on open.** Focus is restored to the previously-focused element on unmount, but nothing moves focus into the dialog on mount, and Tab can escape the modal — a WAI-ARIA dialog pattern gap shared with the pre-existing `DetachDialog`/`RevokeGrantDialog` this component's a11y mechanics were copied from. [velara-web/src/features/admin/components/AddUserOverlay.tsx]

- **`test_resend_already_active_user_returns_409` is tautological.** It manipulates the `_INVITED_EMAILS` module global directly to fabricate an "already active" state rather than exercising a realistic activation transition, so the 409 branch's correctness rests entirely on internal state matching the implementation's own internal state. Test-quality concern, not a code defect. [velara-api/tests/integration/api/test_users.py]

## Deferred from: code review of story-10.1 (2026-07-03)

- **Blocking sync boto3 `AdminCreateUser` inside async `provision_user`.** Same pre-existing pattern as the 8.5 `ListUsers` item deferred from the 9.4 review (above) — this diff adds a write-path occurrence. The Cognito HTTPS round-trip (plus botocore retries under throttling) freezes the event loop for all in-flight requests. Resolution: `asyncio.to_thread`/executor offload or make the `AuthProvider` seam async — fix `create_user` and `list_users` together. [velara-api/app/services/provisioning_service.py:33, app/integrations/auth.py:513]

- **Typo'd invite email mints a pre-verified stranger account with no API cleanup.** `email_verified=true` (AC2-mandated so the invite delivers) skips ownership proof; if an admin mistypes a deliverable address, the recipient gets a working temp-password login and forgot-password immediately. No disable/delete endpoint exists (IAM already grants `AdminDisableUser`; nothing calls it) — cleanup is console-only. Resolution: Story 10.2 Users-screen disable/resend actions. [velara-api/app/integrations/auth.py:544]

## Deferred from: code review of 10-4-first-login-password-challenge (2026-07-06)

- **Non-challenge `signIn` nextStep values (MFA codes/setup, `RESET_PASSWORD`, `CONFIRM_SIGN_UP`) still fall through to the blanket "Invalid username or password."** Pre-existing fall-through — before 10.4, `login()` discarded the `signIn()` result entirely, so ALL non-DONE steps hit the same "no ID token" throw; 10.4 handled exactly one (`CONFIRM_SIGN_IN_WITH_NEW_PASSWORD_REQUIRED`) per AC3's "only new path" constraint. Not reachable today: MFA is OFF in dev / OPTIONAL elsewhere with no forced enrollment. Resolution: handle `RESET_PASSWORD` in Story 10.5 (forgot-password); handle MFA steps if/when MFA is turned on for staging/prod. [velara-web/src/shared/utils/auth.ts]

- **`completeNewPassword` ignores `confirmSignIn`'s returned nextStep — a chained challenge would surface the raw "no ID token" error on the new-password form.** Unreachable today (no forced MFA setup on an OPTIONAL/OFF pool; a brand-new FORCE_CHANGE_PASSWORD user cannot have enrolled a factor). The story's Dev Notes explicitly scoped chained challenges/missingAttributes out. The related partial-success trap (confirmSignIn succeeds, `fetchAuthSession` fails, resubmit hits a dead challenge session) is mitigated by the "Back to sign in" affordance added in this review's patches. Resolution: assert `nextStep.signInStep === 'DONE'` with a clear error when MFA handling lands. [velara-web/src/shared/utils/auth.ts]

- **No tests for unhandled nextStep values / chained challenges.** Travels with the two items above — add when MFA/reset handling lands. [velara-web/src/shared/utils/auth.test.ts]

## Deferred from: code review of 10-3-provisioning-grant-handoff (2026-07-06)

- **ClientCombobox free-typed text masks the real selection.** Pick a client, then type into the combobox without clicking an option — the input shows the typed text while the old selection stays live and submittable; there is also no way to clear a selection back to null. Pre-existing shared-component behavior, identical in Access Control's grant form. Fix in the component so both consumers benefit. [velara-web/src/features/admin/components/AccessControl.tsx:602-615]

- **`useClients` loading/error states swallowed on the AddUserOverlay grant step.** `const { data: clients = [] } = useClients()` discards `isLoading`/`error`, so a loading/errored/genuinely-empty clients list all present as an empty combobox with a permanently disabled Grant button and no explanation. Mirrors the reference grant form exactly (AC5 mandated mirroring) — improve both forms together. [velara-web/src/features/admin/components/AddUserOverlay.tsx:72]

- **`useProjects` loading not surfaced on the grant step.** Immediately after picking a client the Project select is enabled showing only "Whole client" while the projects query is in flight — an admin intending a narrower project grant can submit an over-broad whole-client grant in that window. Same mirroring rationale as above. [velara-web/src/features/admin/components/AddUserOverlay.tsx:73,306-320]

- **Focus drops to `<body>` on AddUserOverlay step transitions.** Advancing 0→1 (create success) or 1→2 (grant/skip) unmounts the focused button with no focus management for the new step; keyboard users re-tab from the top and screen readers announce nothing. Pre-existing pattern from the 2-step 10.2 overlay. [velara-web/src/features/admin/components/AddUserOverlay.tsx:113-116,140-144]

## Deferred from: code review of story-12.1 (2026-07-06)

- **`set()` helper widened to `string | Scope | boolean` with no per-key type discrimination.** `SkillForm.tsx:217` — `function set(key: keyof FormFields, value: string | Scope | boolean)` accepts any value type for any key; TypeScript will not catch a call like `set('name', true)` or `set('location_dependent', 'x')`. No such mismatched call exists today. Deferred — fixing requires per-key overloads (a bigger structural change than this story's scope); revisit if `FormFields` grows more non-string field types. [velara-web/src/features/skills/components/SkillForm.tsx:217]

## Deferred from: story 11.8 (Schema-Driven Run Inputs Form, 2026-07-07)

- **No server-side validation of `inputs` against the skill's `input_schema` (locked decision D2).** The FE renders a schema-driven form and validates client-side (required/type/min/max/enum), but the backend still accepts `inputs` as an opaque free-form dict — `jsonschema` is not a project dependency and `queue_invocation` performs no schema check (`app/api/v1/invocations.py:261-266` just stores `body.inputs` verbatim into `job.inputs["inputs"]`). This is acceptable for now: `inputs` is advisory data, not a security boundary — a malformed inputs dict makes a skill compute on bad data, it cannot breach the IP/PHI boundaries. **Fix path (defense-in-depth for the REST API surface, not just the UI):** add the `jsonschema` dependency, validate `body.inputs` against `skill.input_schema` in `queue_invocation` before `create_job`, and reject a mismatch with a new stable code (e.g. `422 INVALID_INPUTS`); decide strictness (reject unknown keys? coerce types?) at that time. Also revisit the FE first-cut scope limit: nested object / array / `oneOf`/`anyOf`/`allOf`/`$ref` schema properties are silently skipped by both `parseInputFields` (FE) and `derive_run_fields` (BE) — a full JSON-Schema form renderer (repeatable array rows, nested groups, variant switches) is a separate effort. [velara-api/app/api/v1/invocations.py; velara-web/src/features/run/inputsSchema.ts; velara-api/app/schemas/client.py derive_run_fields]

## Deferred from: code review of 11-1-multi-file-zip-bundle-upload-and-extraction (2026-07-08)

- **Staged bundle ZIPs under `bundle-staging/` are never garbage-collected.** The staging object is deleted only after a successful create/version confirm; every 422 rejection (INVALID_BUNDLE, INVALID_CODE_DRIVEN_MANIFEST, HYBRID_SHAPE_MISMATCH), every abandoned upload, and every FE "Remove" leaves the ZIP in the skill bucket indefinitely — the 15-minute presign TTL expires the URL, not the object. Deleting on 422 in code would break legitimate retry (e.g. a shape-mismatch confirm against the wrong skill reuses the same staged key), so the proper fix is an S3/MinIO bucket lifecycle rule expiring `bundle-staging/*` after ~24h (infra/Terraform, not story code). [velara-api/app/api/v1/skills.py; terraform s3 lifecycle]

- **Ingest's `putToS3` never checks `response.ok` (pre-existing twin of the 11.1 bundle-client fix).** `fetch` resolves normally on S3 403/400/5xx, so a failed document PUT silently proceeds to confirm and fails later with a confusing error. Same defect class fixed in `putBundleToS3` for 11.1 — apply the identical `if (!res.ok) throw` guard here when touching ingest next. [velara-web/src/api/ingest.ts]

- **Running a bundle skill fails generically until Story 11.2 lands (user decision 2026-07-08: accept, no interim guard).** The executor fetches `current_ver.artifact_key` as a single object key, but for `is_bundle` versions it is a directory prefix → botocore NoSuchKey → unmapped generic job failure; nothing blocks certifying/publishing/invoking a bundle skill in the window. Reason: executor consumption of bundles IS Story 11.2 (next in epic order) — a throwaway invocation guard was declined. 11.2 MUST close this (replace the code_driven_executor Step-2 no-op with real bundle extraction into bundle_dir). [velara-api/app/services/execution_service.py:429,543,698-700]

## Deferred from: code review of 11-2-standardized-entrypoint-contract-and-registration-validation (2026-07-08)

- **Case-colliding bundle member paths silently clobber on case-insensitive filesystems.** `pkg/Mod.py` and `pkg/mod.py` are distinct paths (pass the 11.1 duplicate guard) but write to the same file during executor materialization on macOS/Windows — each member's sha256 verifies against its own fetched bytes BEFORE the write, so the overwrite is undetected and the entrypoint imports whichever landed last. Prod (Linux containers) is unaffected; this only bites local dev validation runs. Fix if it ever matters: reject casefold-colliding paths in `extract_bundle`. [velara-api/app/services/bundle_extractor.py:186-196; app/services/code_driven_executor.py Step 2]

## Deferred from: code review of story-11.3 (2026-07-09)

- **`_extract_entrypoint_module`'s "last top-level `FunctionDef` wins" walk is narrower than `validate_entrypoint_contract`'s broader last-binding walk.** The propose flow's helper only tracks `ast.FunctionDef` nodes matching the callable name; the registration validator it's meant to mirror also considers a later `ClassDef`/`Assign`/`ImportFrom` rebinding the same name as Python's actual import-time resolution would. Deferred — only affects which signature the LLM is prompted with (proposal quality), never registration safety, since the final approved bundle is always independently re-validated by the unchanged registration path. [velara-api/app/services/skill_integration_assistant.py:226-229 vs app/services/entrypoint_contract.py:174-189]
- **`updated_manifest` returned to the FE for review is never validated against `CodeDrivenHybridManifest` before display.** `_parse_llm_response` only checks `isinstance(updated_manifest, dict)`; a malformed AI-proposed manifest field (missing `entrypoint`, bad `requirements` shape, etc.) ships to the review screen unvalidated and only surfaces as an error later at registration-time. Deferred — awkward UX gap, not a correctness/security issue, since registration re-validates regardless. [velara-api/app/services/skill_integration_assistant.py:277]
- **`max_tokens` is a hardcoded local default (4096) instead of `settings.ANTHROPIC_MAX_TOKENS`.** Within Task 1's literal guidance ("generous, e.g. 4096") but diverges from the Dev Notes' "exact call shape to reuse" precedent (`_run_prompt_hybrid` sources `max_tokens` from config). Deferred — config-consistency nice-to-have, not a bug; revisit if a second call site needs the same constant. [velara-api/app/services/skill_integration_assistant.py:292]

## Deferred from: code review of 11-9-ai-assisted-manifest-generation (2026-07-09)

- Prompt-injection surface via untrusted ZIP member paths/source interpolated into the synthesis prompt (`_build_module_listing` → `_SYNTHESIS_SYSTEM_PROMPT` user content). Mitigated by static re-validation of the entrypoint + mandatory human review of the adapter blob; inherent to the 11.3 design (module source already flows into the 11.3 prompt), not widened by 11.9.
- Two nested manifests (`a/manifest.json` + `b/manifest.json`) classify as "no manifest" — `find_manifest` returns `None` on ambiguity (bundle_extractor.py:296-301), so 11.9 now reports `MISSING_BUNDLE_MANIFEST` and the synthesis path runs with both real manifests riding along in `core_files_unchanged`. Pre-existing 11.1 ambiguity semantics; consider a distinct "ambiguous manifest" message.

## Deferred from: code review of 11-6-author-new-skill-versions-from-ui (2026-07-09)

- **Staged ZIP orphaned on a failed/rejected in-place draft-content update.** The new `PUT /skills/{id}/draft-content` route runs `_safe_delete(body.bundle_key)` only on the success branch (no `try/finally`), so every rejected update (422 HYBRID_SHAPE_MISMATCH / INVALID_BUNDLE / ENTRYPOINT_CONTRACT_VIOLATION / MISSING_BUNDLE_MANIFEST, or 409 SKILL_NOT_DRAFT) leaves the staged ZIP in `bundle-staging/` indefinitely. Pre-existing pattern: `create_version`'s route [app/api/v1/skills.py:437] has the byte-identical success-only cleanup; 11.6 faithfully mirrors it. This is the SAME orphan class already tracked above ("Staged bundle ZIPs under `bundle-staging/` are never garbage-collected") — the new draft-content route is simply a second producer of the same orphans. Fix belongs with the staging-GC S3 lifecycle rule (delete-on-reject would break legitimate retry), not this story. [velara-api/app/api/v1/skills.py:497-508]

## Deferred from: code review of story-11.7 (2026-07-10)

- Pinned version matched with exact `==`, no whitespace normalization [velara-api/app/api/v1/invocations.py:93] — `_resolve_target_version` compares `v.version == requested_version` with no `.strip()`; an external API caller sending `"1.0.0 "` gets 404 SKILL_VERSION_NOT_FOUND though the version exists. FE only sends exact option values so the UI is safe; 404-on-unresolvable is arguably acceptable. Low value.
- Stale `selectedVersion` survives a shrinking versions list [velara-web/src/features/run/components/RunConsole.tsx:510,577,779,868] — `selectedVersion` is reset only on skill change (never in skill-first locked mode) and never revalidated against the fetched `skillVersions`; if the list shrinks so the pinned string vanishes, React state still threads the stale string into the payload, 404ing on submit. Version set is stable within a single skill, so very low likelihood.

## Deferred from: story 12-5 (Audit Coverage for Skill-Authoring & Ingest Mutations, 2026-07-13)

- **Hierarchy CRUD (12 routes) and skill-attachment (4 routes) mutations are entirely unaudited.** `POST/PATCH/DELETE` on `/api/v1/clients`, `/api/v1/projects`, `/api/v1/studies`, `/api/v1/locations` (12 routes), plus `POST/DELETE /api/v1/projects/{id}/skills` and `POST/DELETE /api/v1/studies/{id}/skills` (4 routes) — someone can create, rename, or delete a client/project/study, or attach/detach a skill to/from a study, with zero audit trail. Surfaced by this story's AC4 route-walk guard (`tests/unit/test_audit_coverage_guard.py`), which registers all 16 as `exempt="not yet audited — known gap, tracked in deferred-work.md"` per an explicit Story 12.5 scope decision (auditing all 16 would have roughly doubled the story; genuinely out of the epic's ACs). **This is a real, admin-mutation-shaped compliance gap** — not a false positive — and should become its own story when hierarchy/attachment coverage is prioritized: reuse the exact `record_admin_action` best-effort pattern this story established (see `transition_lifecycle`/`create_skill` in `skill_service.py`), add ~16 new `EVENT_ADMIN_*` constants (or a smaller set with an `entity_type` metadata field to avoid one-constant-per-route), and flip each registry entry from `exempt` to `audited` as it's covered — the guard test enforces the flip is real (`test_registry_integrity` requires the referenced constant to actually exist). [velara-api/app/api/v1/hierarchy.py (both hierarchy CRUD and the skill-attachment routes live here); velara-api/tests/unit/test_audit_coverage_guard.py]

## Deferred from: code review of 11-5-in-app-environment-promotion (2026-07-14)

- **`SkillPromoteResponse` success path is entirely untested.** No test exercises a `PromotionResult` success response shape (`promoted_skill_id` non-null) because only `DisabledPromotionProvider` ships in this story — every request today terminates in `PROMOTION_NOT_CONFIGURED` before that code runs. Correct to defer to the Phase-2 story that adds `RemotePromotionProvider`, per this story's explicit L1 scope lock ("no transport this story"). [velara-api/app/api/v1/skills.py:369-373]
- **Non-`PromotionNotConfiguredError` exceptions from `promotion.promote(...)` skip the audit write.** The `try/except PromotionNotConfiguredError` in `promote_skill` only catches that one type; any other exception from the provider would propagate without writing `_audit_promote`. Not reachable today — `DisabledPromotionProvider` only ever raises `PromotionNotConfiguredError` — but worth a broader `except Exception` + re-raise wrapper when `RemotePromotionProvider` (with real network/timeout failure modes) ships in Phase 2. [velara-api/app/api/v1/skills.py:339-357]

## Deferred from: code review of story-13.4 (2026-07-16)

- **Advisory-lock keys use Python's randomized `hash()` at 3 REMAINING call sites (1 pre-existing + 2 from 13.4).** `PYTHONHASHSEED` is unpinned and the API runs as multiple ECS tasks (separate OS processes, each with `uvicorn` single-process, no `--workers`) — two tasks handling concurrent requests for the same dedupe key compute different `lock_key` values, so the `pg_advisory_xact_lock` does not actually serialize them across tasks, reopening the check-then-insert race it exists to prevent. Introduced by Story 13.3 (`record_artifact_disclosure`), propagated (not created) by 13.4's `record_auth_event`/`record_access_denial`. Fix: replace `hash(...)` with a stable hash (e.g. `hashlib.sha256(...).digest()` truncated to 63 bits) at all three sites. [velara-api/app/services/audit_service.py:417, 497, 569]
  - **UPDATE 2026-07-16 (13.6 code review):** the pattern's 4th potential site — `record_audit_log_access` (the new `audit.log_accessed` de-dupe, AC3) — was FIXED at birth during 13.6 review: it uses `hashlib.sha256(f"{event}:{org_id}:{user_id}")` truncated to 63 bits (and folds `org_id` into the key). The 3 sites above remain on the broken `hash()` and are still owed the same fix. [velara-api/app/services/audit_service.py:639]
- **`record_auth_event`'s `dedupe_key` param is optional and unenforced** — nothing guards against a future hot-path caller forgetting to pass it and flooding the append-only table, the exact failure mode Trap 3 exists to prevent. Both current hot-path callers (token-rejection, denials) correctly pass it. [velara-api/app/services/audit_service.py:463-470]
- **`_record_login_failed`'s de-dupe key (`sub or source_ip`) can collide across distinct principals behind shared NAT/CGNAT** — an unauthenticated/garbage-token request with no `sub` is keyed purely on `source_ip`; multiple distinct legitimate users behind the same corporate NAT/mobile carrier CGNAT collapse into one de-dupe bucket for 15 minutes. Doesn't defeat the security purpose (still captures the pattern from that IP) but weakens per-user audit completeness for that population. [velara-api/app/core/dependencies.py:177]
- **CloudWatch `security_events` alarm threshold (20 events/5min) is asserted but not derived from observed traffic** — no baseline cited for current 401/404 rate in dev/staging; the metric also counts ordinary role-guard 404s from normal UI navigation (consultants/clients hitting grantor-only routes), not just attacks, so the threshold could be too noisy or too loose. Terraform is plan-only per story convention; operator should validate against real traffic before/after apply. [velara-api/terraform/cloudwatch.tf]
- **Failed-token `reason` classification via substring match on exception message text** (`"expired" in str(exc).lower()`) — both `DevAuthProvider` and `CognitoAuthProvider` happen to use the literal word "expired" today, but nothing enforces that coupling or tests the message contract itself; a future wording change silently reclassifies expired-token denials as `"invalid"`. [velara-api/app/core/dependencies.py:230]
- **Stale module docstring in auth.py** still lists only `/login`, `/me`, `/dev-users` and claims these endpoints are "replaced, not extended" when Story 7.3's Cognito integration lands — but the new `/logout` route is explicitly designed to survive into the Cognito world (branches on provider type, calls `global_sign_out` for prod). Cosmetic. [velara-api/app/api/v1/auth.py:1-13]
- **Idempotent re-call of `deprovision_user` on an already-disabled user writes a duplicate, non-deduped `auth.session_revoked` row** — no `dedupe_key` passed at this call site, unlike the hot-path auth-failure/denial writes. Low impact: deprovision is an admin-only, rare action, not attacker-triggerable. [velara-api/app/services/provisioning_service.py:167-183]
- **`record_access_denial`'s `dedupe_key` (method+path only) omits `denial_code`** — two different denial reasons hitting the same route within the 15-minute window collapse into one row, losing the earlier `denial_code`. Narrow edge case (a route rarely produces two distinct denial codes for the same principal in one window). [velara-api/app/services/audit_service.py:696-713]
- **A raw Starlette `HTTPException(403, ...)` or any non-`VelaraHTTPException` 403 would bypass `access.denied` auditing entirely** — the handler only intercepts `VelaraHTTPException` with `code == "FORBIDDEN"`. Verified no current call site raises a 403 this way, so this is a latent gap, not a present miss — worth a lint/test guard if a future 403 is ever raised outside the `VelaraHTTPException` convention. [velara-api/app/core/exceptions.py:90, 138-149]
- **No integration test exercises a query-bearing request to prove `record_access_denial` strips the query string** — the code is correct by inspection (uses `request.url.path`, never `request.url` or the raw query string), but the one test asserting the denial metadata key-set hits a route with no query string at all, so it can't actually demonstrate the stripping behavior it implicitly guards. [velara-api/tests/integration/api/test_auth_and_authz_auditing.py]
- **`client_source_ip` trusts the first `X-Forwarded-For` hop unconditionally, with no validation that the ALB is configured to overwrite/prepend rather than trust client-supplied XFF values** — standard pattern for ALB-fronted apps and correct assuming standard ALB behavior, but nothing in this diff documents or asserts that assumption; a misconfigured ALB could let a client spoof `source_ip` in the audit trail. Correctness depends on infra config outside this story's scope. [velara-api/app/core/middleware.py:418-433]
- **`test_repeated_denials_are_deduped` (and the other new denial tests) are not repeatable within a 15-minute window because `audit_log_entries` is append-only (DB trigger blocks DELETE) and the test's `dedupe_key` is built from a fixed literal route (`/api/v1/skills`) with no per-run uniqueness.** Discovered while verifying the 3 applied patches: re-running the integration test suite against the same `velara_test` database within 15 minutes of a prior run makes `test_repeated_denials_are_deduped` see a leftover row from the earlier run's de-dupe window and assert `len(rows) == 1` against 2 real rows. Confirmed pre-existing and unrelated to the patches (root-caused via direct `psql` inspection of `velara_test`, not `velara` — the row's `created_at` predated the patch-verification session). Fix: parameterize the route/dedupe key per test run (e.g. `uuid4()`-suffixed query-string-free path segment) so repeated CI/local runs against a not-yet-truncated `velara_test` don't collide with their own prior runs. [velara-api/tests/integration/api/test_auth_and_authz_auditing.py:433-453]

## Deferred from: code review of story-13.5 (2026-07-16)

- **CloudTrail bucket has no versioning or Object-Lock while log-file-validation advertises tamper-evidence.** The trail sets `enable_log_file_validation = true` (digest files), and the bucket comment claims tamper-evidence is "the control an auditor looks for" — but without S3 versioning / Object-Lock / MFA-delete, a principal with `s3:DeleteObject` on the bucket can still remove log objects. Every other retained bucket in the repo (ingest/output/skill/tfstate) enables versioning; the CloudTrail bucket omits it. Hardening beyond the AC's literal ask; worth adding versioning + a retention Object-Lock at prod scale. [velara-api/terraform/cloudtrail.tf:69-104]
- **`aws_guardduty_detector.main` fails at apply if GuardDuty was ever enabled in the account/region** (one detector per region per account → `BadRequestException: a detector already exists`). No data-source lookup / import guard. Apply-time operator concern rather than a code defect; relevant only if the target account already has GuardDuty on. [velara-api/terraform/guardduty.tf:10-15]
- **ALB access-log bucket policy's newer-region service-principal statement grants only `s3:PutObject`, omitting `s3:GetBucketAcl`** that the `logdelivery.elasticloadbalancing.amazonaws.com` delivery form also requires. The legacy account-ID statement (used by us-east-1, the configured `var.region` default) is complete, so this is latent — it only bites if the deployment moves to a region that uses the service-principal delivery mechanism. Region-conditional; not triggered at the configured region. [velara-api/terraform/cloudtrail.tf:274-288]

## Deferred from: code review of story-13.6 (2026-07-16)

- **Mid-flight `session.commit()` on the shared execution session at the sandbox network-block audit site.** `_run_code`'s best-effort `security.sandbox_network_blocked` write calls `record_admin_action(session=session, ...)`, which does `session.commit()` on the *shared execution session* right before `raise CodeExecutionError()`. Safe today: the caller's `except` opens a fresh `fail_session` for failure handling, and nothing but the audit row is pending on `session` at that point (job-state mutations happen in earlier, separate `session_scope` blocks). Latent fragility only — a future mutation added to `_run_code` before this line would be prematurely committed on the block path. Consider giving the sandbox-event write its own `session_scope()` (as the failure handler does) to make it independent-by-construction. [velara-api/app/services/execution_service.py:660]
- **No de-dupe on `security.sandbox_network_blocked`.** Every other new/existing best-effort writer in `audit_service` de-dupes within a window; this one does not. Celery at-least-once redelivery or a fan-out child re-run of a skill that deterministically hits the network boundary writes one `security.sandbox_network_blocked` row per attempt. Low impact (network-block is rare, each attempt is arguably a real event, and the CloudWatch alarm reads the `code_execution_failed` log line rather than this row so alerting is unaffected) — but the durable count inflates. [velara-api/app/services/execution_service.py:660]
- **Audit-read de-dupe window omits the filter shape from its key.** `record_audit_log_access` de-dupes on `(user_id, org_id, event_type)` within 15 min regardless of `filter_shape`. An admin who runs a broad "all events" query then a narrowly-filtered compliance query 2 minutes later produces only ONE `audit.log_accessed` row (the first), whose `filters` metadata reflects only the first query — the second, differently-scoped read is invisible on a surface whose whole point is "who looked at what." Spec makes the filter-shape metadata explicitly optional, so this is an accepted narrowing, not a violation; same class as the already-tracked `record_access_denial` "dedupe_key omits denial_code" item. Consider keying the window on the filter shape if per-query audit-read fidelity is later required. [velara-api/app/services/audit_service.py:642]

## Deferred from: code review of story-13.7 (2026-07-16)

- **Retention-table story-attribution + var-vs-literal precision nit (compliance docs).** `data-handling-policy.md` §3's retention table attributes the CloudWatch API log-group retention bump (90→365) to Story 13.5, but the `security_events` metric filter/alarm that motivates keeping that log group long-lived is Story 13.4 (13.5 only introduced `var.log_retention_days`). Separately, the table lists the 13.3 S3 access-log bucket alongside the 13.5 detective-log buckets that are driven by `var.log_retention_days`, implying a uniform variable — but the 13.3 access-logs bucket hardcodes `expiration { days = 365 }` (`s3.tf:266`). (The var-vs-literal half was clarified inline during 13.7's code-review Patch 1; the 13.5-vs-13.4 story attribution on the CloudWatch row remains.) Same value today (var defaults to 365), so no present inaccuracy — purely a traceability-precision cleanup for a future docs pass. [velara-api/docs/data-handling-policy.md:32]

## Deferred from: code review of story-14.2 (2026-07-20)

- **`requirements: null`/non-string blocks a valid-lockfile backfill.** `_backfill_requirements_from_lockfile` skips the backfill whenever `"requirements" in data`, regardless of value — so a manifest declaring `requirements: null` (or `""`, or a non-string) while shipping a valid `requirements.txt` lockfile is still rejected as invalid rather than repaired. This matches the story's documented "present, even if invalid → no-op, real error reported" design choice, so it is an accepted narrowing, not a violation. Consider only skipping the backfill when `isinstance(data.get("requirements"), str)` if lenient repair of malformed-but-present requirements is later wanted. [velara-api/app/services/skill_service.py:455]
- **Pre-existing `mapDetailsToFieldErrors` mis-call in `SkillForm.tsx`.** `mapDetailsToFieldErrors(apiDetails)` passes the details array where the function signature expects the error object (it reaches into `.response.data...` internally), so field-error mapping on this form has been dead since before Story 14.2. Not introduced by this change — flagged only so it isn't mistaken for a new regression. [velara-web/src/features/skills/components/SkillForm.tsx:212]

## Deferred from: code review of story-15.1 (2026-07-21)

- ~~**`Decimal(str(float_rate))` re-imports the binary-float drift the design promises to avoid — for a *future* non-clean rate.**~~ **RESOLVED 2026-07-21** (during the pricing-table completion follow-up, see status log). The pricing table was expanded to the full current Anthropic lineup, and rates are now built via exact Decimal division (`Decimal(in_rate) / Decimal(1_000_000)` in `_MODEL_PRICING`) instead of a `Decimal(str(float_literal))` — so a non-clean rate (e.g. Haiku 3.5 $0.80/MTok = 8e-7) carries zero drift. A regression test (`test_rates_are_exact_decimals_no_binary_float_drift`) pins the exactness and proves it differs from the old float path. [velara-api/app/core/pricing.py]
- **`Numeric(12, 6)` silently truncates any future model whose per-token cost is finer than 6 decimal places.** `compute_cost_usd` returns full-precision Decimal but the column stores 6 fractional digits. Verified no loss today (claude-opus-4-8 products land exactly on a 6-dp boundary; the largest realistic single run ≈ $30 vs the column's ~$999,999 ceiling). Only bites when a model priced below ~$1/MTok is added, where a small run's true cost can quantize toward `0.000000` on store. The "sub-cent precision permanent money fact" claim is currently true only for the one priced model — nothing guards it for the next. Re-check the scale when a cheaper model lands, and add a >6-dp round-trip test at that point. [velara-api/app/models/invocation.py:40]
  - **RE-VERIFIED SAFE 2026-07-21** for the newly-added full lineup: the cheapest per-token rate now in the table is Haiku 3.5 input at $0.80/MTok = `8e-7`/token, which rounds to the 6-dp floor `0.000001` even for a single token (truncation-to-zero would need <0.6 tokens, impossible with integer counts). So `Numeric(12,6)` still holds for every current model. Only revisit if a model priced below ~$0.50/MTok input is ever added.
- **Write path (Decimal, from `invocation_results`) and analytics read path (float, from `audit_log_entries`) now compute the same cost two different ways and can diverge — BY DESIGN, deferred to 15.3.** 15.1 persists a structured `cost_usd` per row using Decimal, but `analytics_service._token_cost` still derives its platform aggregate independently from the audit log using float accumulation, and 15.1 deliberately left that query logic unchanged (only its pricing-table import source moved). The spec's AC4 scope note assigns all read-layer rewiring — reading the new column, COALESCE of absent rows — to Story 15.3. Flag for 15.3's author: once both surfaces show cost, the per-execution figure (Decimal, result row) and the platform aggregate (float, audit rows) computed over the same runs can show non-matching totals, and the two paths disagree on unknown-model handling (read path contributes $0, write path stores NULL). [velara-api/app/services/analytics_service.py:197-209]
- **`SUM(cost_usd)` silently collapses NULL (priced-but-unknown-model) vs 0 (nothing to price) → under-reports true spend with no signal.** The design deliberately encodes three states — real cost, explicit `0` (code/fan-out), and `NULL` (known tokens, unrecognized model). Defensible, but SQL `SUM` skips NULL and counts `0`, so an aggregate over a period containing unknown-model runs under-reports with no indication data is missing — the same "silent under-count" class the story was written to fix on the fabrication side, reappearing on the aggregation side. Deferred to Story 15.3's `_token_cost` rewrite (which owns the read-layer aggregation): suggest it also surface a "runs with unknown model / NULL cost" count so unpriced spend isn't invisible on an authoritative-looking dashboard. [velara-api/app/workers/execution_tasks.py:135-147]

## Deferred from: code review of story-15-2 (2026-07-21)

- **`Decimal(12,6)` → `float` wire cast can drift near the 6-dp boundary.** `JobResult.cost_usd`/`JobSummary.cost_usd` carry a `@field_serializer` casting `Decimal | None → float | None` for the wire (a deliberate, spec-endorsed convention matching `AnalyticsOverview.token_cost: float` so the FE consumes a plain `number`). For the only priced model today (`claude-opus-4-8`, values landing exactly on a 6-dp boundary) the cast is exact — no drift on the wire now. Same class the 15.1 review already deferred (`Numeric(12,6)` truncation, `Decimal(str(rate))` drift): latent only when a future model prices below a clean 6-dp boundary. The precise figure always lives in the DB/API as `Decimal`; only the JSON wire form is float. Re-check when model #2 lands. [velara-api/app/schemas/job.py:87]

## Deferred from: code review of story-15-5 (2026-07-21)

- **Skill-supplied dated/aliased model ids price NULL — no normalization before the exact-key pricing-table lookup.** `CodeDrivenUsage.model` is a free-form skill-supplied string crossing the sandbox boundary into `compute_cost_usd`'s exact-key `_MODEL_PRICING` lookup. A skill pinning a dated API snapshot id (e.g. `claude-opus-4-8-20260115`) or padding case/whitespace prices NULL on every run even though the base model's rate is in the table — a new third-party-controlled surface feeding a table designed for platform-controlled ids. Exact-key NULL-not-fallback is the table's deliberate policy ([[project-llm-pricing-table]]), so this is deferred, not patched; the 15.5 review adds the exact-id requirement to the `CodeDrivenUsage` docstring instead. Revisit (normalize/longest-prefix match before lookup) if a real skill pins snapshot ids. [velara-api/app/core/pricing.py:83]
- **`CodeDrivenUsage` has no cache-token fields — a conforming cache-heavy skill structurally underreports its spend.** The usage contract models only `input_tokens`/`output_tokens`/`model`; Anthropic cache fields (`cache_creation_input_tokens`, `cache_read_input_tokens`) have nowhere to go and are silently dropped by Pydantic's default lax parsing, so even a perfectly on-contract skill using prompt caching reports (and is priced) below its true spend. Future contract extension: add the cache fields to `CodeDrivenUsage` + thread them through `_extract_token_metadata`/`compute_cost_usd` (which would also need cache rates in the pricing table). [velara-api/app/services/code_driven_executor.py:102-114]
- **`reports_usage: false` cannot yet price an explicit $0 — needs a tri-state parse.** The manifest docstring's original "priced as $0-code" promise for a declared-`false` code-driven hybrid is unimplemented and UNIMPLEMENTABLE as-is: at execution time a parsed `False` is indistinguishable from a LEGACY manifest that predates the declaration (same model default), so threading the flag to the pricing seam would fabricate $0 for every legacy skill — the exact bug class 15.5 exists to kill. Docstrings corrected in review to state declared-`false` runs price NULL. To implement honestly: change the parsed model to `reports_usage: bool | None = None` (None = undeclared/legacy → NULL; False = declared no-LLM → explicit $0; True → price envelope usage), thread it into `result_metadata`, and branch in `_extract_cost_fields`. Registration already guarantees NEW bundles declare a bool, so the tri-state only ever sees None for pre-15.5 versions. [velara-api/app/services/code_driven_hybrid.py:86]
- **Assistant usage-emission prompt additions need a live-LLM smoke test.** The 15.5 review added instructions to `_SYSTEM_PROMPT`/`_SYNTHESIS_SYSTEM_PROMPT` (lift client-exposed token usage into a top-level envelope `usage` key; declare `reports_usage` honestly). Offline review verified only prompt text + parse/guard behavior — nobody has run a real propose → approve → execute round-trip to confirm an LLM-authored adapter actually emits `usage`. Operator: exercise this when re-uploading velara-protocol-extractor (the AC5 plan). [velara-api/app/services/skill_integration_assistant.py:208]


## Deferred from: code review of story-15-3-per-skill-and-per-user-cost-analytics (2026-07-22)

- `_invocation_where` comment falsely claims the fan-out PARENT audit row has `outcome=NULL` and is excluded [velara-api/app/services/analytics_service.py:66-71]. In reality `execution_tasks.py:667,681` writes it with `outcome="success"`/`"failure"`, so the parent IS included in every analytics aggregate (`runs`, `invocations`, cost sum). Cost double-counting is avoided only because the parent's `InvocationResult.cost_usd=0` (terminal-guarded, one parent row). Latent trap: if anyone ever prices the parent roll-up non-zero, every fan-out job's cost double-counts silently. The guarding test seeds `outcome=None`, a shape production never emits. Pre-existing; not introduced by 15.3. Fix: correct the comment + re-seed the test with a real `outcome="success"` parent row.
- New `_token_cost` broadens the platform-wide `token_cost` to include `blocked`-run cost the old JSONB (`event_metadata.isnot(None)`) path excluded [velara-api/app/services/analytics_service.py:173-190]. Plausibly the intended, more-correct behavior per AC3, but unremarked and unpinned by any test. Add a test asserting a `blocked` row's `cost_usd` lands in `overview().token_cost` (or explicitly confirm exclusion is not intended).
- Non-DISTINCT `InvocationResult` join could double-count cost if two terminal audit rows ever shared one `job_id` (retry/re-completion) [velara-api/app/services/analytics_service.py:131-154,173-190,254-283]. Latent only — Celery autoretry is disabled and the identical join already backs `func.count()` (`runs`); cost inherits a pre-existing audit-row-model property. Pre-existing; not introduced by 15.3.
