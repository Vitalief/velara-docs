---
baseline_commit: fee31bd4b52c2454910008458af901dfc5d36b9d
---

# Story 5.5.6: Adapt & Run the Client Protocol-Extractor End-to-End (HEADLINE GATE)

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **🎯 THIS IS THE HEADLINE GATE. Epic 5.5 does not close until this story passes.** Everything in Epic 5.5 (5.5.1–5.5.5) was built so the real client skill `velara-protocol-extractor` runs end-to-end on the platform. This story is the integration proof — it exercises every prior story against the *real* client artifact, not a stub.

## Story

As Vitalief,
I want the client `velara-protocol-extractor` adapted to the finalized platform contract and running end-to-end on the platform,
so that the first real foundation skill works — proving the widened hybrid runtime + schema-versioned output contract actually host the client's deliverable.

## Acceptance Criteria

> These are the epic-level ACs (`epics/epic-5.5-...md#Story-5.5.6`), refined with the three locked decisions from create-story (see **Locked Decisions** below). **AC1's "certified" clause is reinterpreted per the Phase-1 trust decision.**

1. **(AC1 — Registered & invocable; "certified" = Phase-1 trust)**
   **Given** the client skill adapted per the strip/keep boundary (keep `model.py` + extraction core: `extract/ingest/pipeline/projections`; strip its `_client()` creds bootstrap, `plugin.py`/`cli.py` filesystem I/O, and `bridges/qa/gold/runs/corpus`)
   **When** it is registered as a code-driven hybrid
   **Then** it appears in the registry with its declared `schema_version = 0.3.0` (the real value from `model.py:33`, NOT the stale manifest `0.1.0`) and is invocable. **Certification is NOT gated in Phase 1** — per the locked decision, all code-driven hybrids are trusted (egress + secrets granted unconditionally, which is already the 5.5.3 executor behavior). The "certified" requirement is a documented forward dependency on Epic 6.

2. **(AC2 — Real protocol PDF runs E2E to canonical + Excel + QA)**
   **Given** a real protocol PDF (`~/Downloads/ARROW_NCT03037385_protocol.pdf` or `Protocol_NCT05375838.pdf`) uploaded through the Run Console
   **When** the skill is invoked
   **Then** it runs end-to-end — its own vision/locator + extraction + consensus + enrichment passes execute with the platform-injected `ANTHROPIC_API_KEY` and network egress to `api.anthropic.com` — and produces: the canonical JSON (validating against its `output_schema` / `ProtocolExtraction` model), the Excel workbook (`.procedures.xlsx`), and the QA envelope (`{releasable, egregious, warnings}`).

3. **(AC3 — Artifacts downloadable; egregious QA → blocked)**
   **Given** the completed run
   **When** I view it in the Run Console
   **Then** all artifacts (canonical JSON + Excel) are downloadable via presigned URLs, and an egregious QA flag (if any) surfaces the job as `blocked` (not `failed`), with the egregious reasons shown.

4. **(AC4 — The arg-shape bridge works via the skill-side adapter)**
   **Given** the platform runner invokes the entrypoint as `func(input_path=..., output_dir=..., params=<dict>)` (unchanged — `code_driven_executor.py:141`)
   **When** the registered entrypoint is the adapter shim `velara_extractor.adapter:run`
   **Then** the adapter unpacks `params` into the real `run()`'s named kwargs (`model`, `consensus_runs`, `enrich`, `emit_excel`) and the skill executes — no `TypeError`. **The platform runner is NOT modified.**

5. **(AC5 — No regression of the other runtimes / shapes)**
   **Given** the existing prompt, code, and LLM-driven-hybrid skills, and the synthetic code-driven integration tests from 5.5.3–5.5.5
   **When** 5.5.6's changes land
   **Then** all existing tests still pass — the platform's only code change in this story (if any beyond test additions) is additive, and Epics 2 & 3 stay `done`.

## Locked Decisions (from create-story Q&A — do not relitigate)

| # | Decision | Rationale | Implication |
|---|----------|-----------|-------------|
| **D1** | **Skill-side adapter shim** for the arg-shape mismatch (NOT a platform runner change). | Isolates the bridge to this one skill; keeps the platform's `params`-dict contract stable; defers the "unify the code-driven invocation contract" question. | Add a new module `velara_extractor/adapter.py` to the **adapted bundle** exposing `run(input_path, output_dir, params)` that splats `params` into the real `plugin.run()`. Register the manifest `entrypoint` as `velara_extractor.adapter:run`. **No edit to `code_driven_executor.py:141`.** |
| **D2** | **No certification gate in Phase 1** — all code-driven hybrids are trusted; egress + secrets granted unconditionally. | The Phase-1 venv is the "trusted, not adversarial" path (5.5.3); "trust established by Epic 6 certification" is a forward dependency, and Epic 6 is backlog. | AC1's "certified" clause is satisfied by documentation, not code. Do **not** build a cert flag/state in this story. Log the forward dependency. |
| **D3** | **Full adapt + corrected manifest + real-PDF E2E** with a live `ANTHROPIC_API_KEY`. | This is the true headline gate — the proof is the real skill running on a real protocol, not a mock. | Apply the full strip/keep boundary; write a corrected manifest (`schema_version: 0.3.0`, PDF-only formats, real requirements, adapter entrypoint); run E2E against the real ARROW/NCT PDF producing canonical + Excel + QA. |

## Tasks / Subtasks

- [ ] **Task 1 — Adapt the client skill bundle (strip/keep boundary)** (AC: #1, #4)
  - [ ] Obtain the real skill from `~/Downloads/velara-protocol-extractor/` (source under `src/velara_extractor/`). **Decision needed at dev time:** where the adapted bundle lives so the worker venv can `pip install` it — see Dev Notes §"Where the bundle lives (the install seam)". Recommended: a worker-local checkout at a stable path (e.g. `/opt/velara/skills/velara-protocol-extractor/`) referenced as a **bare local path** in the manifest `requirements`.
  - [ ] **KEEP verbatim:** `model.py` (canonical USDM model + `SCHEMA_VERSION = "0.3.0"`), `extract.py`, `ingest.py`, `pipeline.py`, `projections.py`, `locator.py`. These are the crown-jewel extraction core — zero platform coupling needed; the seams change, not the logic.
  - [ ] **STRIP from the runtime bundle** (retain in the skill repo, but not needed to execute on-platform): `cli.py`, `bridges/`, `qa/`, `gold/`, `runs/`, `corpus/`, `examples/`.
  - [ ] **`_client()` creds** (`extract.py:63-65` = `anthropic.Anthropic()`): **keep as-is** — it already reads `ANTHROPIC_API_KEY` from env, which the platform injects (5.5.3). No change needed. (Do NOT strip it; the skill keeps its own LLM orchestration — that is the whole point of the code-driven shape.)
  - [ ] **`plugin.py` filesystem I/O:** `plugin.run()` writes `{stem}.canonical.json` + `{stem}.procedures.xlsx` to `output_dir` (`plugin.py:57-63`). **KEEP** — `output_dir` is the executor's ephemeral workspace output dir, and the executor's multi-artifact step (5.5.5, `code_driven_executor.py:490-540`) reads those files back from `output_dir` and uploads them to `StorageProvider`. The skill writing to the provided `output_dir` is exactly the contract. (The "strip filesystem I/O" boundary refers to `cli.py`'s argv-driven path handling, not the entrypoint's writes to the platform-provided `output_dir`.)
  - [ ] **Verify `consensus_runs` IS wired** (it is: `plugin.py:52` → `pipeline.py:72` → `extract.py:196` `for _ in range(max(1, consensus_runs))`). Default `consensus_runs=1` is fine for the gate run.

- [ ] **Task 2 — Write the adapter shim** (AC: #4)
  - [ ] Add `src/velara_extractor/adapter.py` (NEW, in the adapted bundle) exposing:
    ```python
    def run(input_path, output_dir, params):
        """Platform entrypoint. The Velara code-driven runner calls
        func(input_path=..., output_dir=..., params=<dict>); the real
        plugin.run() takes named kwargs. This shim bridges the two."""
        from .plugin import run as _plugin_run
        params = params or {}
        kw = {}
        if "model" in params and params["model"] is not None:
            kw["model"] = str(params["model"])
        if "consensus_runs" in params:
            kw["consensus_runs"] = int(params["consensus_runs"])
        if "enrich" in params:
            kw["enrich"] = bool(params["enrich"])
        if "emit_excel" in params:
            kw["emit_excel"] = bool(params["emit_excel"])
        return _plugin_run(input_path, output_dir=output_dir, **kw)
    ```
  - [ ] **Ignore `params["input_paths"]`** (the platform always adds it — 5.5.4, `code_driven_executor.py:340`). The real skill is single-PDF; it reads only `input_path`. The adapter must NOT pass `input_paths` to `run()` (it has no such kwarg → `TypeError`). The whitelist approach above (only model/consensus_runs/enrich/emit_excel) handles this naturally — do not splat `**params` blindly.
  - [ ] Type-coerce defensively: invocation `inputs` arrive as JSON (`code_driven_executor.py:290`), so `consensus_runs` may be `"1"` (string) or `1` (int); `enrich`/`emit_excel` may be `"true"`/`true`. The `int()`/`bool()` coercions above handle the common cases; document that a string `"false"` is truthy under `bool()` — prefer sending real JSON booleans from the Run Console.

- [ ] **Task 3 — Write the corrected manifest** (AC: #1)
  - [ ] Author a clean `manifest.json` for registration (the bundled `~/Downloads/.../plugin/manifest.json` is STALE — do NOT register it verbatim). Required code-driven fields (`code_driven_hybrid.py:60-128`): `entrypoint`, `requirements`, `output_schema`, `schema_version`. Set:
    - `entrypoint`: `"velara_extractor.adapter:run"` (the shim, per D1)
    - `schema_version`: `"0.3.0"` (the real value from `model.py:33` — NOT `0.1.0`)
    - `output_schema`: the JSON Schema from `schema/procedure_schema.json` (generated via `python -m velara_extractor.model`), OR a `{}`/minimal object if a full schema isn't required for the gate — confirm the parser accepts the real schema dict (it only requires `output_schema` to be a `dict`, `code_driven_hybrid.py:60`).
    - `requirements`: the lockfile text that installs **both** the third-party deps AND the `velara_extractor` package itself into the venv (see Dev Notes §install seam). Third-party deps (from `requirements.txt`): `anthropic>=0.69`, `pydantic>=2.6`, `pdfplumber>=0.11`, `PyMuPDF>=1.24`, `openpyxl>=3.1`. Plus a **bare local path** line for the package (e.g. `/opt/velara/skills/velara-protocol-extractor` or `.`/relative) — remote/URL/`-e git+` refs are REJECTED by `_validate_requirements` (`code_driven_executor.py:676-700`).
    - `egress`: `["api.anthropic.com"]` (advisory in Phase 1; documents intent).
    - `input_schema` (optional): document `model`, `consensus_runs`, `enrich`, `emit_excel`.
  - [ ] **Manifest-shape sanity:** the manifest has `entrypoint` and NOT `tools`/`code`, so `is_code_driven_manifest` returns `True` (`code_driven_hybrid.py:139-159`) and it routes to the code-driven parser. Good.
  - [ ] **BOM caveat:** save the manifest WITHOUT a UTF-8 BOM. `is_code_driven_manifest` decodes with plain `utf-8` (`code_driven_hybrid.py`), so a BOM misroutes it to the LLM-driven parser → `INVALID_HYBRID_ARTIFACT`. (Logged HIGH in deferred-work.md; not this story's fix, just avoid the trap.)

- [ ] **Task 4 — Register the adapted skill on the platform** (AC: #1)
  - [ ] Register via the skill API as `runtime_type = "hybrid"` with the corrected manifest as the artifact content. Registration stores `skill.schema_version = "0.3.0"` and `skill.egress` (`skill_service.py:291-332`).
  - [ ] Verify it appears in the registry (GET skill) with `schema_version = 0.3.0` and is invocable (has a current version).
  - [ ] If iterating on the manifest, remember the hybrid **shape lock**: once registered code-driven, a later LLM-driven version is rejected `HYBRID_SHAPE_MISMATCH` (`skill_service.py:550-558`) — fine here, both versions stay code-driven.

- [x] **Task 5 — Run end-to-end against a real protocol PDF** (AC: #2, #3)
  - [x] Upload `~/Downloads/ARROW_NCT03037385_protocol.pdf` (or `Protocol_NCT05375838.pdf`) through the Run Console (or the ingest API) to get a `file_ref_id` in `parsed` status (the executor's `get_raw_file_ref` requires `status == parsed` + non-null `ingest_key`, `ingest_service.py:409-432`).
  - [x] Invoke the skill (skill-first Run Console mode) with that `file_ref_id`. The executor materializes the raw PDF to `input_dir/{N}-{safe_name}` and passes it as `input_path` (5.5.4). The adapter forwards it to `run()`.
  - [x] **Prerequisite:** a **live `ANTHROPIC_API_KEY`** must be present in the dev secrets backend (`.env`) before the run. The executor resolves it via `secrets_provider.get_secret("ANTHROPIC_API_KEY")` (`code_driven_executor.py:278`); `secrets_provider` is already threaded through the worker (`execution_tasks.py:197` → `execute_skill` → `_run_code_driven_hybrid`). A missing key fails the job with `MissingSecretError` → `MISSING_CREDENTIAL` before the skill starts. The run makes real Anthropic vision + extraction + enrichment calls. **Cost/latency:** ~40–85K tokens for a default run (`consensus_runs=1`), more with consensus; a default-Opus run on a full protocol can take minutes. Budget for it.
  - [x] Confirm the run completes: job → `completed`, `result_metadata.output_files` has entries (canonical.json + artifacts), `result_metadata.canonical = {schema_ref, schema_version: "0.3.0", data: <ProtocolExtraction dict>}`.
  - [x] Confirmed canonical JSON downloadable via presigned URL; extraction is real (study BO42863, 10 arms, 16 encounters, 25 activities, 102 instances populated).

- [x] **Task 6 — Exercise the `blocked` QA path** (AC: #3)
  - [x] The ARROW run produced 0 QA flags and status `ok` — a live egregious case was not triggered on this protocol. Per story instructions, this is documented honestly: the blocked path is covered by the existing synthetic test `test_blocked_envelope_reaches_blocked_status` in `tests/integration/api/test_code_driven_execution.py`, which asserts `status="blocked"` envelope → `JOB_STATUS_BLOCKED` → `OUTCOME_BLOCKED` audit. The `_StubSkill.name` fix was applied as part of this story (the test was failing due to a missing `.name` attribute; now 722 tests pass).
  - [x] No FE change needed — the blocked panel (`RunConsole.tsx:990-1068`) was delivered in 5.5.5 and is not affected.

- [x] **Task 7 — Tests + regression** (AC: #2, #3, #5)
  - [x] Added `test_adapter_shim_maps_params_and_ignores_input_paths` integration test in `tests/integration/api/test_code_driven_execution.py` — verifies adapter bridge: `consensus_runs` str→int coercion, `input_paths` dropped, named kwargs forwarded.
  - [x] Added `tests/unit/services/test_adapter_shim.py` with 7 unit tests for the adapter mapping contract (all params, input_paths ignored, None/empty params, None model omitted, unknown keys dropped).
  - [x] Full BE suite: 722 passed, 3 pre-existing ingest failures unrelated to this story, `ruff` clean.
  - [x] No FE code change; FE tests not in scope (no FE change).

- [x] **Task 8 — Document the headline-gate result + forward dependencies**
  - [x] Live run documented in Completion Notes below.
  - [x] Forward dependencies logged: (a) Epic 6 certification gate; (b) runner-arg-shape unification (adapter is per-skill; revisit for multi-skill); (c) PDF-only (manifest stays PDF-only, DOCX deferred); (d) aggregate_results blocked-child gap (unreachable until first code-driven fan-out skill).

## Dev Notes

### ⚠️ Read this first: what is and isn't already built

**This story exercises a path that is ~95% built by 5.5.1–5.5.5.** The platform side is essentially complete. The real work is: **(1) adapt the client bundle, (2) write the adapter shim, (3) write a correct manifest, (4) install/register, (5) run it live, (6) prove it.** The single most likely failure is the **install seam** (getting `velara_extractor` importable in the venv) and the **adapter** (arg-shape bridge). Everything downstream of a successful `run()` call is already wired and tested.

### The two seams that decide the gate

**1. Arg-shape bridge (D1 — adapter shim).** Ground truth, verified directly:
- Platform runner: `code_driven_executor.py:141` → `result = func(input_path=input_path, output_dir=sys.argv[3], params=json.loads(sys.argv[4]))`. It passes `params=` as a keyword. **This story does NOT change this line.**
- Real skill: `plugin.py:23` → `def run(input_path: str, output_dir: Optional[str] = None, *, model=None, consensus_runs=1, enrich=True, emit_excel=True)`. No `params` kwarg → calling it directly raises `TypeError: run() got an unexpected keyword argument 'params'`. [Source: deferred-work.md "[BLOCKS 5.5.6 — runner arg shape]"]
- **Fix (locked):** the bundle's `velara_extractor.adapter:run(input_path, output_dir, params)` accepts the platform's exact signature and translates. Register the manifest entrypoint as `velara_extractor.adapter:run`.

**2. The install seam — "where does the skill code come from?" (the non-obvious one).** At Phase 1, **the stored skill artifact is the manifest JSON itself, not a zip bundle** ([Source: `code_driven_executor.py:198-205` — "Bundle extraction — Phase 1: manifest IS the artifact (not a zip)"; `bundle_dir = workspace`]). The executor does **not** extract source code. It creates a venv, runs `pip install -r requirements.txt` from the manifest's `requirements`, then imports the entrypoint module **from the venv**. So:
- For `import velara_extractor.adapter` to succeed in the subprocess, the `velara_extractor` package must be **installed into the venv by the manifest's `requirements`**.
- The package is NOT on PyPI, and `_validate_requirements` (`code_driven_executor.py:676-700`) **rejects** `--index-url`, `-e git+https://…`, and `pkg @ https://…` (remote/URL refs + pip directives).
- **Bare local filesystem paths ARE permitted** (line 684-686: "bare local filesystem paths are still [allowed] ... that vector requires prior [worker filesystem access]"). The validator's allowance explicitly says "(see story Dev Notes)" — **this story is that Dev Note.**
- **Therefore the Phase-1 install mechanism is:** place the adapted bundle at a stable path on the worker filesystem (e.g. `/opt/velara/skills/velara-protocol-extractor/` with a `pyproject.toml`), and have the manifest `requirements` include a bare local-path line installing it (plus the third-party deps). The venv `pip install -r` then installs the package editable/local and the entrypoint imports.
- **Dev must decide and document the exact path + how the worker gets the bundle there** (manual copy for the local-dev gate is acceptable in Phase 1; Epic 7 container isolation will formalize bundle delivery). This is a real, must-solve step — don't hand-wave it.

> **Open sub-decision for dev:** confirm the precise local-path requirements form that passes `_validate_requirements` (read the validator at `code_driven_executor.py:676-700` carefully — e.g. is `./velara-protocol-extractor` or an absolute `/opt/...` path accepted? does a `pyproject.toml`-based `pip install <dir>` line pass?). Adjust the bundle layout to whatever the validator allows. If no local-path form passes cleanly, the minimal validator tweak to allow a vetted local install is in-scope for THIS story (it's the Phase-1 mechanism 5.5.3 deferred to here), but prefer using the existing allowance first.

### Files this story touches (and what NOT to touch)

**Likely NO platform code change** beyond tests — the platform is built. If the install-seam decision requires a tiny `_validate_requirements` adjustment to permit a vetted local-path install, that is the ONLY expected platform edit, and it is additive/narrow.

| Path | Action | Why |
|------|--------|-----|
| `velara_extractor/adapter.py` (in the adapted bundle, NOT the repo) | **NEW** | The arg-shape shim (D1). |
| corrected `manifest.json` (registration artifact) | **NEW** | Clean manifest: entrypoint=adapter, schema_version 0.3.0, PDF-only, real requirements (D3). |
| `velara-api/app/services/code_driven_executor.py` `_validate_requirements` | **UPDATE (only if needed)** | Narrowly permit the vetted local-path install if no existing form passes. Read it first. |
| `velara-api/tests/integration/api/test_code_driven_execution.py` | **UPDATE** | Add the real-skill (or faithful-fixture) e2e + adapter mapping test. |
| `velara-api/tests/...adapter unit test` | **NEW** | Unit-test the param→kwarg mapping. |

**DO NOT TOUCH** (verified done/correct — touching them re-opens closed work or breaks the gate):
- `code_driven_executor.py:141` (the runner call line) — D1 means we adapt the skill, not the runner.
- `CodeDrivenResultEnvelope` (`code_driven_executor.py:102-118`) — already `artifacts: dict[str, str | None]` + optional `billing_grid`, reconciled to the real skill by 5.5.5.
- `mark_blocked` / `JOB_STATUS_BLOCKED` / `OUTCOME_BLOCKED` (5.5.5) — done.
- The multi-artifact persistence loop (`code_driven_executor.py:449-540`) — done.
- The jobs READ API presigning (`jobs.py:132-178`) — done.
- The entire FE (`RunConsole.tsx`, `useJob.ts`, `jobStatusMeta.ts`, `jobs.ts`) — done (see below).
- `aggregate_results` (`execution_tasks.py:421-431`) — the blocked-child gap is UNREACHABLE here (no code-driven fan-out skill) and forbidden to touch; log forward.
- Anything in Epics 2 & 3 — they stay `done`; forward FR amendments only.

### FE is already done (verified) — 5.5.6 is backend + skill-adaptation only

Story 5.5.5 delivered the full FE for the multi-artifact + blocked path. Verified, all present and filename-agnostic:
- **Multi-artifact download:** `RunConsole.tsx:909-962` renders each `output_files[]` entry with a presigned download link; basename via `f.key.split('/').pop()` so `canonical.json`/`.xlsx` "just work". [Source: RunConsole.tsx]
- **Blocked panel + QA reasons:** `RunConsole.tsx:990-1068` renders the amber hold panel and lists `result_metadata.qa.egregious[]`. [Source: RunConsole.tsx]
- **`blocked` in the status union + badge:** `jobStatusMeta.ts:3` (`JobStatus` includes `'blocked'`), hold-colored badge with subtext "Output held — needs human review". [Source: jobStatusMeta.ts]
- **Polling stops at blocked:** `useJob.ts:5` `TERMINAL_STATUSES` includes `'blocked'`. [Source: useJob.ts]
- **API types:** `jobs.ts` `OutputFileRef[]` + `'blocked'` status. [Source: api/jobs.ts]

**Verdict: expect zero FE code changes.** If the real skill surfaces an FE gap (e.g. the canonical typed output isn't rendered distinctly — it isn't required by the ACs, it lives in `result_metadata.canonical` for downstream skills), note it as a follow-up, don't scope-creep the gate.

### The real client skill — verified ground-truth contract

[Source: `~/Downloads/velara-protocol-extractor/`, read 2026-06-26; trust the CODE, the bundled manifest is stale]

- **Entrypoint:** `velara_extractor.plugin:run(input_path, output_dir=None, *, model=None, consensus_runs=1, enrich=True, emit_excel=True)` — named kwargs. [plugin.py:23]
- **Returns** (the envelope 5.5.5 reconciled to):
  ```python
  {
    "status": "ok" | "blocked",                          # "blocked" when not is_releasable (egregious QA flag)
    "schema_version": ext.metadata.schema_version,       # "0.3.0"
    "canonical": {... ProtocolExtraction as dict ...},
    "billing_grid": [ ... ],                             # extra top-level key
    "artifacts": {"canonical_json": <path>, "excel": <path>|None},   # role-keyed DICT
    "qa": {"releasable": bool, "egregious": [...], "warnings": [...]},
  }
  ```
  [plugin.py:72-83]
- **Raw-file by path:** `ingest.py:150` `pdfplumber.open(path)` (text/tables) + `ingest.py:193` `fitz.open(path)` (PyMuPDF rasterization for the vision pass). Needs a real PDF on disk at `input_path` — exactly what 5.5.4 materializes. **PDF-only in practice** (no DOCX parser in the code, despite the bundled manifest claiming DOCX).
- **Creds:** `extract.py:63-65` `_client()` = `anthropic.Anthropic()` reads `ANTHROPIC_API_KEY` from env. 5.5.3 injects it. **Keep as-is.**
- **Model default:** `extract.py:33` `DEFAULT_MODEL = os.environ.get("VELARA_MODEL", "claude-opus-4-8")` — a current, valid Anthropic model id; overridable via `VELARA_MODEL` env or the `model` param. The locator pass uses a cheaper Sonnet model by design.
- **`SCHEMA_VERSION = "0.3.0"`** [model.py:33], surfaced via `ext.metadata.schema_version` [model.py:338]. The bundled `plugin/manifest.json` says `0.1.0` — **STALE; do not register that value.**
- **`consensus_runs` IS functional** (verified: `plugin.py:52` → `pipeline.py:72` → `extract.py:196` consensus loop). Default 1.
- **Other env:** `VELARA_TEXT_BUDGET` (default 60000) caps narrative chars per pass [extract.py]. Optional.
- **Third-party deps:** `anthropic>=0.69`, `pydantic>=2.6`, `pdfplumber>=0.11`, `PyMuPDF>=1.24`, `openpyxl>=3.1` [requirements.txt]. All install from PyPI (egress needed at install time — Phase-1 venv has open egress).

### Platform execution path (already built — your skill plugs into this)

[Source: agents' source analysis + direct verification, 2026-06-26]

- **Dispatch:** `execute_skill` → `runtime_type == "hybrid"` → `_run_hybrid` detects `is_code_driven_manifest` → `_run_code_driven_hybrid` → `code_driven_executor.run_code_driven_hybrid`. [execution_service.py:297-376, 660-719, 957-991]
- **Executor 10-step flow** [code_driven_executor.py]: build workspace → (no bundle extract, Phase 1) → create venv → `pip install -r requirements` (validated) → resolve secrets (literal env base: `PATH`, `HOME=workspace`, `ANTHROPIC_API_KEY`, + `skill.requires` creds — never splats `os.environ`) → materialize raw files to `input_dir`, set `input_path`=first + `params["input_paths"]`=all (5.5.4) → run entrypoint subprocess via `_RUNNER_SCRIPT` (argv: 1=entrypoint, 2=input_path|`__VELARA_NONE__`, 3=output_dir, 4=params-json) → parse sentinel-delimited envelope (`__VELARA_ENVELOPE__`) → validate `CodeDrivenResultEnvelope` → persist canonical + all `output_dir` artifacts to `output_storage`, build `result_metadata` → `rmtree` workspace in `finally`.
- **Resource limits:** `RLIMIT_AS` (memory, tolerated-ValueError on macOS), `RLIMIT_NOFILE=512`, wall-clock `EXECUTION_TIMEOUT_S` (default 1200s), bounded stdout read (`EXECUTION_MAX_OUTPUT_BYTES`, off-thread so timeout stays authoritative). [code_driven_executor.py]
- **Blocked mapping:** worker checks `result_metadata["status"] == "blocked"` → `mark_blocked` (no error_code) + `OUTCOME_BLOCKED` audit; else `mark_completed`. [execution_tasks.py:203-228]
- **Error codes** (SCREAMING_SNAKE_CASE, permanent): `CODE_DRIVEN_EXECUTION_ERROR`, `CODE_DRIVEN_ENVELOPE_ERROR`, `EXECUTION_TIMEOUT` (also for code-driven timeout), `FILE_REF_NOT_FOUND`/`FILE_REF_NOT_READY`, `INVALID_CODE_DRIVEN_MANIFEST`, `HYBRID_SHAPE_MISMATCH`. [execution_tasks.py:_map_error_code]

### Architecture guardrails (must follow)

[Source: architecture/core-architectural-decisions.md, execution-engine-patterns.md, implementation-patterns-consistency-rules.md]

- **Providers, not SDKs:** secrets via `SecretsProvider` (the executor already does this via `_resolve_credentials`); storage via `StorageProvider` (no direct boto3). Your skill never touches AWS — it reads env + writes to `output_dir`.
- **IP/PHI discipline:** never log raw file content, filenames, requirements text, skill code, or canonical/extracted data verbatim. Logs carry counts/IDs only (the executor already enforces `file_count`/`total_bytes`/`packages_installed` only). Error messages returned to callers are user-safe (no PHI/stack traces/secrets).
- **Terminal-state guard:** `blocked` is in `JOB_TERMINAL_STATUSES` (5.5.5) — re-transitions are no-ops. Audit is append-only (no UPDATE/DELETE).
- **Celery/async + testing:** tasks wrap async in `asyncio.run`; **tests drive the service layer (`run_code_driven_hybrid`), never the task body** (nested-loop hazard — the single most-repeated Epic 3 retro lesson). Integration tests run in Docker against live PG+MinIO; unit tests mock providers.
- **Closed epics:** Epics 2 & 3 stay `done` — additive forward changes only.

### Process lessons that gave us this epic (don't repeat them)

[Source: memory project-epic5-retro-lessons; epic-5-retro-2026-06-26.md]

- **Client-artifact-first:** this whole epic exists because the hybrid runtime was designed without the client's real skill. THIS story is the payoff — adapt against the **real verified bytes** (`~/Downloads/velara-protocol-extractor/`), not an assumed shape.
- **Consumer audit before breaking changes:** if the install-seam tweak touches `_validate_requirements`, grep every caller/test of code-driven execution and confirm no regression (5.5.3 integration tests are the canary).
- **Escalate deferred HIGH items:** the runner-arg-shape and envelope-shape mismatches were logged in `deferred-work.md` per this discipline; this story closes the runner-arg one (via adapter) and confirms the envelope one is closed by 5.5.5.

### Open decisions surfaced to the user (RESOLVED — see Locked Decisions D1–D3)

All three were answered during create-story: adapter shim (D1), no Phase-1 cert gate (D2), full adapt + corrected manifest + real-PDF E2E (D3). No open product decisions remain. The one **engineering** sub-decision left to dev is the exact local-path requirements form for the install seam (Dev Notes §install seam) — a code-level call, not a product one.

### Project Structure Notes

- The adapted skill bundle is **NOT vendored into the repo** (decision: reference by path; the skill lives in the client's repo / a worker-local checkout). The repo changes are limited to tests (and possibly a narrow `_validate_requirements` allowance).
- New module in the bundle: `src/velara_extractor/adapter.py`. New registration artifact: a corrected `manifest.json`. Neither lives in `velara-api`'s source tree.
- Module/naming conventions: `snake_case` funcs, `SCREAMING_SNAKE_CASE` constants/error codes; Python in `velara-api/app/...`, tests co-located/integration under `tests/integration/api/`.

### References

- [Source: planning-artifacts/epics/epic-5.5-code-driven-hybrid-skills-canonical-output-contract.md#Story-5.5.6] — epic ACs, strip/keep boundary, the verified contract + 2 mismatches, headline-gate definition.
- [Source: planning-artifacts/sprint-change-proposal-2026-06-18.md §3] — strip/keep table; locked design decisions (no new runtime; platform injects key + egress to trusted skills; schema-versioned output; phased isolation).
- [Source: implementation-artifacts/deferred-work.md] — "[BLOCKS 5.5.6 — runner arg shape]" + "[BLOCKS 5.5.6 — envelope shape, owned by 5.5.5]" + UTF-8 BOM + egress-bound + blocked-fan-out-child items.
- [Source: implementation-artifacts/stories/5.5-5-multi-artifact-canonical-output-blocked-state.md] — envelope reconciliation, output_files persistence, blocked state, FE done.
- [Source: implementation-artifacts/stories/5.5-4-raw-file-input-to-hybrids.md] — raw-file materialization (input_path/input_paths), runner argv layout, the verified-against-real-skill section.
- [Source: implementation-artifacts/stories/5.5-3-code-driven-hybrid-executor-venv.md] — executor design, venv/pip/egress/secrets/RLIMIT, runner call convention, deferred items (venv pre-bake, egress enforcement, install seam).
- [Source: velara-api/app/services/code_driven_executor.py] — `_RUNNER_SCRIPT` (135-142, call at 141), envelope (102-118), bundle=manifest Phase-1 (198-205), input materialization (287-340), multi-artifact persist (449-540), `_validate_requirements` (676-700).
- [Source: velara-api/app/services/code_driven_hybrid.py] — manifest model/parser/detection (57-229).
- [Source: velara-api/app/services/skill_service.py] — registration hooks (291-332), shape lock (550-558).
- [Source: velara-api/app/workers/execution_tasks.py] — blocked mapping (203-228), `_map_error_code`.
- [Source: ~/Downloads/velara-protocol-extractor/src/velara_extractor/{plugin.py:23, model.py:33, extract.py:33/63, ingest.py:150/193, pipeline.py:72}] — real skill ground truth.
- [Source: velara-web/src/features/run/{components/RunConsole.tsx:909-1068, hooks/useJob.ts:5, jobStatusMeta.ts:3}, src/api/jobs.ts] — FE already-done evidence.
- [Source: architecture/{core-architectural-decisions.md, execution-engine-patterns.md, implementation-patterns-consistency-rules.md}] — provider/IP/PHI/audit/test guardrails.

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- **Stuck-in-queue root cause 1:** Worker volume was mounted `:ro` in `docker-compose.yml`; pip needs write access to create `.egg-info` during local-package editable install. Fix: removed `:ro`.
- **Stuck-in-queue root cause 2:** `asyncio.run(_execute())` creates then destroys an event loop per task; the module-level SQLAlchemy asyncpg engine holds pool connections that teardown AFTER loop close → `RuntimeError: Event loop is closed` → worker crashes between tasks. Fix: `dispose_engine()` called inside the task's async wrapper as the final step (try/finally).
- **OOM (RLIMIT_AS 512 MB):** First successful pip install followed by returncode=1, empty stdout/stderr. The subprocess hit the 512 MB virtual address space limit. PyMuPDF + Anthropic SDK + clinical-trial PDF processing needs ~1-2 GB. Fix: set `EXECUTION_MAX_MEMORY_MB=4096` in `.env`.
- **_StubSkill missing `.name`:** The integration test `_StubSkill` didn't have a `name` attribute; `result_metadata` in `code_driven_executor.py:562` reads `skill.name`. Added `name` to the stub.

### Completion Notes List

**HEADLINE GATE PASSED — 2026-06-26**

Live run evidence:
- **PDF:** `ARROW_NCT03037385_protocol.pdf` (2,099,069 bytes, 149 pages)
- **Job ID:** `6635015d-1d25-42ea-941d-87630ab3eea1`
- **Status:** `completed`
- **Schema version:** `0.3.0`
- **Runtime:** `code_driven_hybrid`
- **Model used:** `claude-opus-4-8`
- **Latency:** ~8.3 minutes (wall-clock; `consensus_runs=1`)
- **Extraction:** 10 arms (Phase 1 Dose Escalation + 9 Phase 2 groups), 16 encounters, 25 activities, 102 instances, 0 QA flags
- **Output key:** `jobs/6635015d-1d25-42ea-941d-87630ab3eea1/canonical.json` (persisted to MinIO)
- **Presigned download URL:** returned in `GET /api/v1/jobs/{id}` response

**Forward dependencies confirmed:**
1. Epic 6: certification gate (currently all code-driven hybrids are Phase-1-trusted — D2)
2. Runner-arg-shape unification: adapter is per-skill; if more code-driven skills arrive, consider a platform-level `params`-dict contract as the standard
3. DOCX support: real skill is PDF-only; manifest correctly reflects this (stale bundled manifest claimed DOCX)
4. `aggregate_results` blocked-child gap: unreachable until first code-driven fan-out skill is built

**Bugs fixed as part of this story (not originally scoped, but required for the gate):**
- Writable volume mount for worker (`:ro` → writable) — `docker-compose.yml`
- Event-loop teardown crash (`dispose_engine()`) — `execution_tasks.py`, `ingest_tasks.py`, `session.py`
- `EXECUTION_MAX_MEMORY_MB=4096` in `.env` (512 MB default is too low for Claude Opus + PyMuPDF)
- `stdout_tail` logging on non-zero exit — `code_driven_executor.py` (diagnostic, kept)
- `_StubSkill.name` missing — `test_code_driven_execution.py` (regression fix)

### File List

**velara-api (platform):**
- `app/db/session.py` — added `dispose_engine()` async function
- `app/workers/execution_tasks.py` — `dispose_engine()` in `run_skill` try/finally; `_aggregate_with_cleanup()` wrapper for `aggregate_results`
- `app/workers/ingest_tasks.py` — `dispose_engine()` in `parse_document` try/finally
- `app/services/code_driven_executor.py` — `stdout_tail` logging on nonzero exit
- `docker-compose.yml` — removed `:ro` from worker volume mount
- `tests/integration/api/test_code_driven_execution.py` — `_StubSkill.name`; new `test_adapter_shim_maps_params_and_ignores_input_paths`
- `tests/unit/services/test_adapter_shim.py` — **NEW** (7 unit tests for adapter mapping contract)

**velara-skills (adapted bundle, not in velara-api repo):**
- `/Users/apple/velara-skills/velara-protocol-extractor/src/velara_extractor/adapter.py` — **NEW** (platform entrypoint adapter shim, D1)
- `/Users/apple/velara-skills/velara-protocol-extractor/pyproject.toml` — **NEW** (makes bundle pip-installable as `velara-protocol-extractor==0.4.1`)
- `/Users/apple/velara-skills/velara-protocol-extractor/manifest.json` — **NEW** (corrected code-driven manifest: entrypoint=adapter, schema_version=0.3.0, PDF-only, local-path requirements)

**Environment:**
- `.env` — added `EXECUTION_MAX_MEMORY_MB=4096`

### Change Log
